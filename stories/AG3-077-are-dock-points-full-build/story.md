# AG3-077: ARE-Vollausbau (4 Dock-Points, AreClient-HTTP + Konfig-Konstruktion/Injektion, are_bundle.json, StoryAreLink-Schreibwege) — die vier Andock-Punkte real statt Stubs

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `requirements-and-scope-coverage` (BC `requirements_coverage`) — ARE-Integration und Anforderungsvollstaendigkeit. Die vier Andock-Punkte sind Top-Surface-Methoden, keine eigenstaendigen Komponenten; `AreClient` und der Scope-Resolver sind BC-interne Subs (FK-40 §40.4/§40.5).
**Quell-Konzepte (autoritativ):**
- `FK-40 §40.5.1` — Andock-Punkt 1 `link_requirements`: `are_get_recurring(scope, story_type)` + story-spezifische Verlinkung via `are_list_requirements(story_id, scope)`; INSERT in `StoryAreLink`
- `FK-40 §40.5.2` — Andock-Punkt 2 `load_context`/`load_are_bundle()`: ARE-Kontext laden, als Content-Plane-Artefakt `are_bundle.json` (`ArtifactClass.QA`, Producer `qa-are-context-loader`) persistieren, `are_bundle`-Signal in den Phase-State; FAILED -> Setup bricht ab
- `FK-40 §40.5.3` — Andock-Punkt 3 `submit_evidence`: Evidence pro Anforderung (test_report/commit_ref/artifact_ref/manual_note); optionale `kind`-UPDATE (`addresses` -> `partial`)
- `FK-40 §40.5.4` — Andock-Punkt 4 `check_gate`: Soll-Ist-Abgleich aller verknuepften `must_cover`-Items; `CoverageVerdict` PASS/FAIL mit Liste unbelegter Anforderungen; Ergebnis-Artefakt `_temp/qa/{story_id}/are_gate.json`
- `FK-40 §40.5b` — `StoryAreLink`-Edge-Tabelle: Lifecycle (INSERT Andock-Punkt 1, optionale `kind`-UPDATE Andock-Punkt 3), Schreibwege-Matrix §40.5b.3, Eindeutigkeit `(story_id, are_item_id, kind)`, Stale-Behandlung §40.5b.5
- `FK-40 §40.2` — Aktivierung ueber `features.are` + `are.rest_base_url` (Projekt-Konfig); bei `features.are: true` ist die Top-Surface aktiv und benoetigt einen aus der Konfig konstruierten `AreClient`
- `FK-40 §40.4` — `AreClient` als BC-interner REST-Adapter (HTTP-Body), direkt gegen die ARE-REST-API (analog GitHub-REST-Adapter FK-12); `ScopeMapping`/`AreIntegration` als interne Delegationsziele der Top-Surface. Die Konstruktion aus `ProjectConfig.are` und Injektion in die Real-Pfade ist Composition-Root-/Adapter-Verdrahtung (R-Code)
- `FK-22 §22.4b` — ARE-Bundle-Laden als deterministischer Setup-Schritt **nach der Context-Berechnung und vor der Story-Typ-Weiche**: bei `features.are: true` Bundle laden + persistieren, Statusmodell LOADED/SKIPPED/FAILED, Ergebnis im Phase-State, Stopppunkt bei FAILED

---

## 1. Kontext / Ist-Zustand (belegt)

Der ARE-BC ist als Vertragsgeruest vorhanden, aber im aktiven (ARE-enabled) Pfad funktionslos: sowohl die vier Top-Surface-Andock-Punkte als auch der darunterliegende REST-Adapter sind reine Stubs.

- **Alle vier Top-Surface-Methoden** werfen im enabled-Pfad `AreCapabilityNotImplementedError("... full body in follow-up story")`: `src/agentkit/requirements_coverage/top.py:104-106` (`link_requirements`), `:129-131` (`load_context`), `:153-155` (`submit_evidence`), `:180-182` (`check_gate`). Nur der disabled-Pfad (`status=SKIPPED`, `top.py:99-100,124-125,148-149,175-176`) und die fail-closed Konfig-Pruefung (`AreConfigurationError` bei enabled-ohne-Client, `top.py:101-102,126-127,150-151,177-178`) sind funktional.
- **Der REST-Adapter `AreClient` ist ebenfalls vollstaendig Stub:** alle fuenf Methoden werfen `NotImplementedError(... is follow-up)` — `are_client.py:50` (`list_requirements`), `:65` (`get_recurring`), `:79` (`load_context`), `:103` (`submit_evidence`), `:117` (`check_gate`); der Modul-Docstring (`are_client.py:1-6`) sagt explizit „The full HTTP implementation is deferred to follow-up stories". Ohne HTTP-Body koennen die Andock-Punkte keine echten ARE-Ergebnisse liefern; ein Real-Machen der Top-Surface ohne den Adapter waere nur ueber verdeckte Fallbacks oder Test-Attrappen moeglich (Verstoss gegen ZERO DEBT / NO ERROR BYPASSING). Der Adapter gehoert demselben BC (`requirements_coverage`, FK-40 §40.4) und ist Teil dieses Cuts — es existiert **keine** separate Owner-Story dafuer (`_STORY_INDEX.md` Welle 3, AG3-077 ist die einzige ARE-Story).
- **Layer-1-Konsument-Pfad (wichtig — die fruehere Story-Annahme war falsch):** Der Layer-1-Check `check_are_gate` liest **keine** `are_gate.json`-Datei. Er erhaelt ein bereits aufgeloestes `CoverageVerdict`-Objekt als Parameter (`verify_system/structural/checks/are_gate.py:35-41`, `coverage_verdict: CoverageVerdict | None`). Der Dispatcher loest dieses Verdikt **im Speicher** auf: `checker.py:448-450` ruft `are.coverage_verdict(c.story_id, c.project_key)`; der Provider `_RequirementsCoverageAreProvider.coverage_verdict` (`bootstrap/composition_root.py:2395-2401`) delegiert direkt an `RequirementsCoverage.check_gate(story_id, project_key)`. Eine `are_gate.json`-Datei wird im gesamten Produktivcode **nirgends gelesen** (verifiziert: kein Treffer auf `are_gate.json` in `src/agentkit/`). `are_gate.json` ist also das in FK-40 §40.5.4 vorgesehene **Ergebnis-/Audit-Artefakt** (`_temp/qa/{story_id}/are_gate.json`), **nicht** der Input des Layer-1-Checks.
- **`CoverageVerdict` traegt die FK-40-Felder noch nicht:** Das reale Modell erlaubt nur `status` und `verdict` und ist `extra="forbid"` (`requirements_coverage/contract.py:149-161`). FK-40 §40.5.4 verlangt die Liste der unbelegten Anforderungen; der disabled-Doc-Pfad `top.py:171-172` skizziert zudem `CoverageVerdict(status=FAIL, reason="are_gate_unavailable")` — beide Felder (`uncovered_requirements`, `reason`) fehlen heute.
- **`AreEvidence` traegt keinen Teilabdeckungs-Indikator:** `requirements_coverage/contract.py:89-105` hat `requirement_id`/`evidence_type`/`evidence_ref`/`produced_by`, aber kein Feld, das die `kind`-UPDATE `addresses -> partial` (FK-40 §40.5.3/§40.5b.3) ausloest. Der Ausloeser ist heute nicht entscheidbar.
- **`are_bundle`-Phase-State-Signal hat kein Zielmodell:** `SetupPayload` erlaubt aktuell nur `phase_type` und ist `extra="forbid"`, `frozen=True` (`story_context_manager/models.py:74-77`); `PhaseState.payload` ist typisiert gegen die diskriminierte Union `PhasePayload` (`models.py:279-284`, `:436-440`). Es gibt also keinen typisierten Platz fuer `{status, requirement_count}`.
- **Setup-Einhaengepunkt existiert, ARE-Schritt fehlt:** `SetupPhaseHandler.on_enter` baut den Context (`_build_enriched_context`) und persistiert ihn (`governance/setup_preflight_gate/phase.py:174-178`), **bevor** die Story-Typ-Weiche/Worktree-Erstellung folgt (Worktree-Setup `:188-193`, green-main `:182-186`). Der Handler wird ueber `build_setup_phase_handler` verdrahtet (`bootstrap/composition_root.py:1317-1323`). Ein `load_are_bundle()`-Aufruf gibt es hier nicht.
- **Persistenz-Infrastruktur ist da:** `StoryAreLinkRepository`-Protokoll (`requirements_coverage/repository.py:11-35`) mit `add`/`update_kind`/`remove`/`list_by_story` und State-Backend-Adapter (`state_backend/store/story_are_link_repository.py:21-87`, inkl. Eindeutigkeits-/Konflikt-Pruefung). `StoryAreLinkKind` hat `ADDRESSES`/`PARTIAL`/`DERIVES_FROM`/`RECURRING` (`requirements_coverage/models.py:10-16`). Wird produktiv nicht befuellt, weil die schreibenden Andock-Punkte 1/3 Stubs sind.
- **`are_bundle.json` existiert nur als Dateiname-Konstante** (`core_types/plane_artifact_names.py`); `load_are_bundle`/`AreBundleResult` und der Producer `qa-are-context-loader` sind nirgends produktiv auffindbar (Gap-Report `gap-fk-13-25.md` §FK-22 §22.4b, `gap-fk-36-45.md` §FK-40 §40.5.2).
- **Der `AreClient` wird im realen Lauf nie aus der Projekt-Konfig konstruiert/injiziert (Kern-Luecke, ohne die `features.are: true` funktionslos bleibt):** Der Layer-1-Resolver `_resolve_structural_evidence_ports` reicht **unbedingt** `None` als Client in `build_structural_are_provider(None, pipeline)` (`implementation/phase.py:580-582`); der Provider macht daraus `RequirementsCoverage(None, pipeline_config)` (`composition_root.py:2370-2371`). Damit ist `is_enabled` zwar `True` (`top.py:71`), aber der enabled-ohne-Client-Pfad greift und `check_gate` liefert dauerhaft `are_gate_unavailable` — selbst wenn `features.are: true`. Die Konfig traegt die noetigen Werte bereits: `ProjectConfig.are: AreConfig | None` (`config/models.py:443`) mit `rest_base_url: str | None`/`auth_token: str | None` (`:59-60`); der Konstruktor ist `AreClient(base_url, auth_token=None)` (`are_client.py:33`). Der Config-Validator erzwingt nur die **Praesenz** der `are`-Sektion bei `features.are=true` (`config/models.py:445-454`), **nicht** ein gesetztes `rest_base_url` — ein enabled-Lauf ohne `rest_base_url` passiert die Validierung, kann aber keinen Client bauen. Es fehlt also (a) die produktive `AreClient`-Konstruktion aus `ProjectConfig.are` und (b) ihre Injektion in den Layer-1-Resolver **und** in den Setup-seitigen `load_are_bundle`-Collaborator. Ohne diese Verdrahtung ist der gesamte Real-Pfad — trotz realem HTTP-Body und realen Andock-Punkten — funktionslos.
- **`StoryContext` traegt die Resolver-Eingaben:** `story_type: StoryType` (`models.py:315`) und `participating_repos`/`project_key` (`:312,348`) — daraus laesst sich der ARE-`scope` ableiten; ein dedizierter Scope-Resolver fehlt noch.

Konform (Abgrenzung, nicht erneut bauen): `is_enabled`-Aktivierung (§40.2), fail-closed bei enabled-ohne-Client (§40.9, vier Methoden), `RequirementsCoverage`-Top-Surface-Struktur, `AreClient`-Konstruktor/Signaturen (REST-Vertrag-Form), `StoryAreLink`-Repository + Konflikt-Pruefung, `are.gate`-Trust-Klasse-A-Layer-1-Konsument inkl. Provider-Verdrahtung (`composition_root.py:2376-2401`), Stage-Registry-Eintrag der `are.gate`-Stage.

## 2. Scope

### 2.1 In Scope

1. **Andock-Punkt 1 `link_requirements(story_id, project_key)`** real (FK-40 §40.5.1): wiederkehrende Pflichtanforderungen via `AreClient.get_recurring(scope, story_type)` automatisch verlinken (`kind=recurring`); story-spezifische via `AreClient.list_requirements(story_id, scope)` (`kind=addresses`). **INSERT** in `StoryAreLink` ueber das bestehende `StoryAreLinkRepository.add`, Eindeutigkeit `(story_id, are_item_id, kind)` (Duplikat -> `StoryAreLinkConflictError` idempotent abfangen). Rueckgabe `LinkResult` mit echtem Status.
2. **Scope-/StoryType-Aufloesung als typisierter BC-interner Resolver** (FK-40 §40.4 `ScopeMapping`, behebt die fehlende Resolver-Quelle): `link_requirements` benoetigt fuer `get_recurring(scope, story_type)` einen `scope`-String und den `story_type`. Quelle:
   - `story_type` stammt aus dem `StoryContext` (`models.py:315`, `StoryType`), das die Top-Surface zum `story_id` aufloest (vorhandener StoryService/State-Backend-Lesepfad).
   - `scope` wird ueber einen neuen `ScopeMapping`-Sub des BC aus den vorhandenen autoritativen Story-Feldern abgeleitet (`participating_repos`/`project_key`, `models.py:312,348`) — kein neues Persistenzfeld, keine zweite Wahrheit. Die oeffentliche Andock-Signatur `(story_id, project_key)` bleibt unveraendert (FK-40 §40.5 Top-Surface-Vertrag); Scope/StoryType sind BC-interne Delegation, nicht zusaetzliche Aufrufer-Parameter.
3. **`AreClient`-HTTP-Body real** (FK-40 §40.4, behebt den Adapter-Stub): die fuenf Methoden (`list_requirements`/`get_recurring`/`load_context`/`submit_evidence`/`check_gate`) erhalten echte REST-Aufrufe gegen `self._base_url`/`self._auth_token`. Fehler werden als typisierte Client-Fehler (`AreClientError`-Familie in `requirements_coverage/errors`) gemeldet, nicht als nackte `NotImplementedError`. Kein verdeckter Fallback auf Leerantworten; HTTP-/Decode-Fehler propagieren fail-closed an die Andock-Punkte (FAILED-Bundle bzw. `are_gate_unavailable`).
4. **Andock-Punkt 2 `load_context(...)` + `load_are_bundle()`** real (FK-40 §40.5.2, FK-22 §22.4b): `AreClient.load_context` -> `are_bundle.json` ueber den `ArtifactManager` persistieren (`ArtifactClass.QA`, Producer `qa-are-context-loader`, Felder `schema_version`/`story_id`/`fetched_at`/`must_cover`). `are_bundle`-Statussignal (`{status, requirement_count}`) in den Phase-State schreiben. Statusmodell **LOADED/SKIPPED/FAILED**; FAILED -> Setup-Stopppunkt (Status in Phase-State, Setup bricht ab, kein Worker-Spawn).
5. **`are_bundle`-Phase-State-Modell typisiert** (behebt das fehlende Zielmodell): ein neues frozen Pydantic-Modell `AreBundleSignal` (Felder `status: AreBundleStatus`, `requirement_count: int`) mit `AreBundleStatus`-StrEnum (`LOADED`/`SKIPPED`/`FAILED`) im kanonischen Control-Plane-Modell. Das Signal wird als optionales, typisiertes Feld am `SetupPayload` gefuehrt (`story_context_manager/models.py:74-77`; das `extra="forbid"` wird respektiert, indem das Feld real ins Modell aufgenommen wird — **kein** Schattenfeld, **kein** Aufweichen auf `extra="allow"`). Persistiert wird ueber `HandlerResult.updated_state` -> `PhaseState.payload` (diskriminierte Union, `models.py:279-284`).
6. **`load_are_bundle()` als deterministischer Setup-Schritt** (FK-22 §22.4b): in `SetupPhaseHandler.on_enter` **nach** `_build_enriched_context`/Persistenz (`phase.py:174-178`) und **vor** der Story-Typ-Weiche/Worktree-Erstellung (`phase.py:188`) einhaengen, ohne LLM. Der Collaborator (`RequirementsCoverage`-Top-Surface bzw. eine schmale `AreBundleLoaderPort`) wird ueber `build_setup_phase_handler` (`composition_root.py:1317-1323`) injiziert — der truth-boundary-geschuetzte Handler baut ihn nicht selbst. Bei `features.are: false` -> SKIPPED, kein Artefakt, Setup laeuft weiter; FAILED -> `HandlerResult` mit Abbruch (analog zur green-main-Fehlerbehandlung `phase.py:184-186`), Signal im Phase-State.
7. **Andock-Punkt 3 `submit_evidence(story_id, evidence)`** real (FK-40 §40.5.3): `AreClient.submit_evidence` mit typisierten Evidence-Typen (`test_report`/`commit_ref`/`artifact_ref`/`manual_note`, `EvidenceType` bereits vorhanden `contract.py:34-40`); Validierung, dass Evidence nur fuer verknuepfte ARE-Items zulaessig ist (Lesen aus `StoryAreLink` via `list_by_story`); bei dokumentierter Teilabdeckung optionale **`kind`-UPDATE** (`addresses` -> `partial`) via `StoryAreLinkRepository.update_kind`. Rueckgabe `EvidenceSubmitResult`.
8. **Partial-Ausloeser fuer `submit_evidence` modellieren** (behebt die nicht entscheidbare Teilabdeckung): `AreEvidence` (`contract.py:89-105`) erhaelt ein typisiertes, optionales Feld `coverage: EvidenceCoverage = EvidenceCoverage.FULL` (`EvidenceCoverage`-StrEnum `FULL`/`PARTIAL`). `coverage=PARTIAL` ist der einzige, explizite Ausloeser fuer die `kind`-UPDATE `addresses -> partial`; `FULL` laesst `kind` unveraendert. Kein impliziter Heuristik-Pfad, keine String-Erkennung. (`model_config extra="forbid", frozen=True` bleibt; das Feld wird real ins Modell aufgenommen.)
9. **Andock-Punkt 4 `check_gate(story_id, project_key)`** real (FK-40 §40.5.4): `AreClient.check_gate` -> Soll-Ist-Abgleich; **Gate-Verdikt** `CoverageVerdict` (PASS, wenn alle `must_cover`-Items Evidence haben; sonst FAIL mit Liste der unbelegten Anforderungen). Stale-`are_item_id` (FK-40 §40.5b.5) wird hier sichtbar: meldet `check_gate` ein Item als unbekannt, setzt das Verdikt FAIL mit explizitem Stale-Hinweis in der Liste (trivial im Gate-Pfad enthalten, kein Extra-Mechanismus). Ergebnis-Artefakt `are_gate.json` (`_temp/qa/{story_id}/are_gate.json`) als FK-40-§40.5.4-Audit-Artefakt schreiben (Envelope-Konvention). **Klarstellung:** dieses Artefakt ist der Audit-/QA-Output des Gates, **nicht** der Input des Layer-1-Checks — `check_are_gate` (`are_gate.py:35-41`) konsumiert weiterhin das in-memory aufgeloeste `CoverageVerdict` ueber den bestehenden Provider (`composition_root.py:2395-2401`); dieser Pfad wird **nicht** umgebaut.
10. **`CoverageVerdict`-Contract erweitern** (behebt die fehlenden Felder): `CoverageVerdict` (`contract.py:149-161`) erhaelt zwei typisierte Felder: `uncovered_requirements: tuple[AreRequirement, ...] = ()` (FK-40 §40.5.4) und `reason: str | None = None` (fuer `are_gate_unavailable`). `model_config extra="forbid", frozen=True` bleibt. Der bestehende Layer-1-Konsument (`are_gate.py:65-80`) bleibt kompatibel (er liest `status`/`verdict`); die neuen Felder reichern die Diagnose an. **Contract-/Schema-Tests** ziehen mit. ARE-enabled-ohne-Client wird (statt `AreCapabilityNotImplementedError`) zu `CoverageVerdict(status=FAIL, verdict="FAIL", reason="are_gate_unavailable")` (FK-40 §40.5.4-Doc / `top.py:171-172`).
11. **`StoryAreLink`-Schreibwege exakt nach FK-40 §40.5b.3:** Andock-Punkt 1 schreibt (INSERT), 2 liest, 3 liest + optionale `kind`-UPDATE, 4 liest. `story_id`/`are_item_id` immutable; nur `kind`-Wechsel zulaessig (das Repository erzwingt das bereits, `story_are_link_repository.py:37-69`).
12. **Producer-Registrierung** `qa-are-context-loader` in der Producer-Registry des BC (FK-40 §40.5.2); Artefakt-Envelope nach den bestehenden Konventionen (FK-71).
13. **Produktive `AreClient`-Konstruktion + Injektion aus `ProjectConfig.are`** (behebt die Real-Pfad-Kernluecke; FK-40 §40.2/§40.4 — Adapter-Verdrahtung ist Composition-Root-Arbeit, R-Code, kein neuer Fachbaustein): wenn `features.are: true`, wird genau **ein** `AreClient(rest_base_url, auth_token)` aus `ProjectConfig.are` (`config/models.py:443`, `:59-60`) konstruiert und an **beide** Real-Pfade injiziert:
    - **Layer-1/Implementation:** `_resolve_structural_evidence_ports` (`implementation/phase.py:576-585`) ersetzt das unbedingte `build_structural_are_provider(None, pipeline)` (`:580-582`) durch die Uebergabe des konstruierten Clients (`build_structural_are_provider(are_client, pipeline)`), sodass `RequirementsCoverage(are_client, pipeline_config)` (`composition_root.py:2370-2371`) den realen Client traegt. Bei `features.are: false` bleibt `None` (Provider bleibt korrekt `SKIPPED`).
    - **Setup/`load_are_bundle`:** derselbe Client (bzw. ein aus derselben Konfig konstruierter) wird in den Setup-seitigen `load_are_bundle`-Collaborator (In-Scope 6, ueber `build_setup_phase_handler`, `composition_root.py:1317-1323`) injiziert. Keine zweite Konfig-Wahrheit, keine Doppelparsing-Heuristik — eine Konstruktionsstelle, beide Pfade konsumieren sie.
    - **Fail-closed bei fehlendem `rest_base_url`:** der Config-Validator erzwingt nur die Praesenz der `are`-Sektion, **nicht** `rest_base_url` (`config/models.py:445-454`). `features.are: true` **mit** `are`-Sektion **ohne** `rest_base_url` ist ein fail-closed Fehler: kein verdeckter Disable, kein Leer-Fallback. Layer-1 -> `CoverageVerdict(FAIL, reason="are_gate_unavailable")`; Setup -> Bundle FAILED + Setup-Stopppunkt (analog zur enabled-ohne-Client-Semantik). Es wird **keine** Konfig-Datei oder Validierung umgebaut (Out-of-Scope-Owner: FK-03-Config-Validierung); AG3-077 behandelt den absenten `rest_base_url` nur an der Konstruktionsstelle fail-closed.
14. **Negativpfade/Phasengrenzen** als Tests: ARE enabled + `AreClient`-Fehler -> Bundle FAILED -> Setup-Abbruch (kein Worker-Spawn); `submit_evidence` fuer nicht-verknuepftes Item -> Ablehnung; `submit_evidence` mit `coverage=PARTIAL` -> `kind`-UPDATE; `check_gate` mit unbelegten `must_cover` -> FAIL inkl. Liste; `check_gate` enabled-ohne-Client -> `CoverageVerdict(FAIL, reason="are_gate_unavailable")`; `features.are: true` + `are`-Sektion ohne `rest_base_url` -> Layer-1 `are_gate_unavailable` bzw. Setup-Bundle FAILED (kein stilles Disable); enabled + real konstruierter Client -> Provider traegt den Client (kein `None`); disabled-Pfad bleibt SKIPPED.

### 2.2 Out of Scope (mit Owner)

- **Stage-Registrierung der `are.gate`-Stage in verify-system** — FK-33 / Stage-Registry-Owner (AG3-064); hier wird der bestehende Layer-1-Konsument bedient, nicht die Registry umgebaut.
- **Umbau des in-memory Layer-1-Konsumenten-Pfads** (`are_gate.py` / `AreGateProvider` / `checker.py:448-450`) — der bestehende Provider-Pfad bleibt unveraendert; AG3-077 macht nur `check_gate` real und schreibt zusaetzlich das `are_gate.json`-Audit-Artefakt. Kein Wechsel auf Datei-Lesen im Layer-1-Check.
- **`StoryAreLink`-DELETE bei Story-Reset/Split** — FK-40 §40.5b.4 verweist auf FK-53 (AG3-071) / FK-54 (AG3-072); diese Stories besitzen die Purge-/Verschiebe-Logik. Hier nur INSERT/`kind`-UPDATE/Read.
- **Aufrufer-Verdrahtung von Andock-Punkt 1 in der Story-Erstellung/VektorDB** (FK-21, AG3-068) — die Story-Erstellung als *Aufrufer* von `link_requirements` ist Story-Creation-seitig; hier wird nur die Andock-Methode selbst real gemacht (inkl. Scope-Resolver), nicht der Story-Creation-Pfad umgebaut.

## 3. Akzeptanzkriterien

1. Alle vier Andock-Punkte werfen im ARE-enabled-Pfad **kein** `AreCapabilityNotImplementedError` mehr, sondern liefern echte Resultate (Test pro Methode).
2. `AreClient` hat einen realen HTTP-Body fuer alle fuenf Methoden; keine `NotImplementedError`-Reste; Client-/HTTP-Fehler werden als typisierte Fehler propagiert (Test inkl. Fehlerpfad mit Fake-HTTP-Transport).
3. **`AreClient` wird im Real-Pfad produktiv aus `ProjectConfig.are` konstruiert und injiziert:** bei `features.are: true` traegt der Layer-1-Provider den realen Client (`build_structural_are_provider(are_client, pipeline)` statt `None` in `implementation/phase.py:580-582`; Test belegt `RequirementsCoverage` mit nicht-`None`-Client) **und** der Setup-`load_are_bundle`-Collaborator wird mit demselben aus `ProjectConfig.are` (`rest_base_url`/`auth_token`) konstruierten Client verdrahtet. `features.are: true` + `are`-Sektion **ohne** `rest_base_url` ist fail-closed: Layer-1 -> `are_gate_unavailable`, Setup -> Bundle FAILED + Stopppunkt; **kein** stilles Disable, **kein** Leer-Fallback (Tests fuer beide Pfade + beide Fehlerfaelle). `features.are: false` -> Client `None`, Provider SKIPPED.
4. `link_requirements` verlinkt wiederkehrende (`kind=recurring`) und story-spezifische (`kind=addresses`) Anforderungen und schreibt sie als INSERT in `StoryAreLink`; Eindeutigkeit `(story_id, are_item_id, kind)` wird respektiert (Test inkl. Duplikat-Idempotenz). `scope`/`story_type` werden ueber den BC-internen Resolver aus `StoryContext`/Story-Feldern abgeleitet (Test des Resolvers).
5. `load_are_bundle()` persistiert `are_bundle.json` mit `ArtifactClass.QA`, Producer `qa-are-context-loader` und den FK-40-Pflichtfeldern; das typisierte `AreBundleSignal` (`status`, `requirement_count`) landet als `SetupPayload`-Feld im Phase-State (Test).
6. Statusmodell LOADED/SKIPPED/FAILED ist korrekt: ARE aus -> SKIPPED ohne Artefakt; Client-Fehler -> FAILED + Setup-Stopppunkt (kein Worker-Spawn); Erfolg -> LOADED + `requirement_count` (drei Tests).
7. `submit_evidence` akzeptiert nur Evidence fuer verknuepfte ARE-Items; `coverage=PARTIAL` setzt `kind` von `addresses` auf `partial` via `update_kind`, `coverage=FULL` laesst `kind` unveraendert (Test beider Faelle + Ablehnung fuer nicht-verknuepftes Item).
8. `check_gate` liefert PASS bei vollstaendiger Abdeckung und FAIL mit `uncovered_requirements`-Liste sonst; Stale-Item -> FAIL mit Stale-Hinweis; schreibt das `are_gate.json`-**Audit-Artefakt** (`_temp/qa/{story_id}/are_gate.json`); ARE-enabled-ohne-Client -> `CoverageVerdict(status=FAIL, verdict="FAIL", reason="are_gate_unavailable")` (Tests). Der bestehende Layer-1-Konsument `are_gate.py` bleibt unveraendert kompatibel (konsumiert das in-memory `CoverageVerdict`, **nicht** die Datei) — Regressionstest belegt, dass `check_are_gate` weiterhin gegen das Verdikt-Objekt arbeitet.
9. `CoverageVerdict` traegt `uncovered_requirements` und `reason`, bleibt `extra="forbid"`/`frozen`; Contract-/Schema-Tests sind mitgezogen.
10. Die `StoryAreLink`-Schreibwege entsprechen exakt FK-40 §40.5b.3 (nur Andock-Punkt 1 INSERT, nur 3 optionale `kind`-UPDATE); `story_id`/`are_item_id` sind immutable (Test prueft, dass 2/4 nicht schreiben).
11. `load_are_bundle()` ist als deterministischer Setup-Schritt **nach der Context-Berechnung und vor der Story-Typ-Weiche** in `SetupPhaseHandler.on_enter` eingehaengt, ueber `build_setup_phase_handler` injiziert, und benoetigt keinen LLM (Test/Phasengrenze inkl. Persistenz ueber `HandlerResult.updated_state`).
12. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage >= 85 %.

## 4. Definition of Done

- AK 1-12 erfuellt; giftige Codex-Review PASS; (Implementierung/Commit erst nach Execution-Plan-Freigabe — diese Story wird zunaechst nur autorisiert/reviewt).

## 5. Guardrail-Referenzen

- **FAIL-CLOSED:** Bundle FAILED bricht Setup ab; `check_gate` ohne Client -> `CoverageVerdict(FAIL, reason="are_gate_unavailable")`, nie stilles PASS; `features.are: true` ohne konstruierbaren Client (fehlendes `rest_base_url`) -> derselbe fail-closed Pfad (Layer-1 `are_gate_unavailable` / Setup-Bundle FAILED), **kein** verdecktes Disable durch nicht-injizierten Client; `AreClient`-HTTP-/Decode-Fehler propagieren typisiert, kein verdeckter Leer-Fallback; Evidence fuer nicht-verknuepftes Item wird abgelehnt.
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** ARE bleibt Quelle der Wahrheit fuer Anforderungs-/Evidence-Inhalt; AK3 spiegelt nur die Verknuepfung in `StoryAreLink`. `scope` wird aus vorhandenen autoritativen Story-Feldern abgeleitet (kein neues Persistenzfeld); der `AreClient` wird aus genau **einer** Konfig-Wahrheit (`ProjectConfig.are`) an **einer** Stelle konstruiert und in beide Real-Pfade injiziert (keine zweite Konfig-Quelle, kein Doppelparsing); `CoverageVerdict`/`AreEvidence`/`SetupPayload` werden real erweitert statt durch Schattenfelder/`extra="allow"` umgangen. Kein zweites Anforderungsmodell, keine Schattenkopie des Evidence-Status, keine `are_gate.json`-Parallelwahrheit neben dem in-memory Verdikt.
- **TYPISIERT STATT STRINGS:** Evidence-Typen, `kind`-Werte, `coverage` (FULL/PARTIAL), Bundle-Status (LOADED/SKIPPED/FAILED) und Gate-Status (PASS/FAIL) als Enums.
- **ARCH-55:** alle neuen Felder/Enum-Werte/Producer-Namen/Wire-Keys englisch (`qa-are-context-loader`, `must_cover`, `uncovered_requirements`, `coverage`, `are_bundle`, `requirement_count`, `are_gate_unavailable`).
- **ZERO DEBT:** keine `NotImplementedError`/`AreCapabilityNotImplementedError`-Reste in den vier Andock-Punkten **und im `AreClient`**; alle Schreibwege real verdrahtet; keine Test-Attrappen fuer produktive Kernlogik (HTTP-Transport im Test ueber einen echten, einspeisbaren Transport-Port, nicht ueber Monkeypatch der Kernlogik).

## 6. Hinweise fuer den Sub-Agent

- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- Kritische Anker (real, file:line verifiziert): `requirements_coverage/top.py:104-182` (die vier Stubs + disabled/fail-closed-Pfade), `requirements_coverage/are_client.py:50,65,79,103,117` (die fuenf `NotImplementedError`-Adapter-Stubs), `requirements_coverage/contract.py:89-105` (`AreEvidence`), `:149-161` (`CoverageVerdict`), `requirements_coverage/repository.py:11-35` + `state_backend/store/story_are_link_repository.py:21-87` (Persistenz inkl. Eindeutigkeit), `requirements_coverage/models.py:10-16` (`StoryAreLinkKind`), `story_context_manager/models.py:74-77` (`SetupPayload`), `:279-284` (`PhasePayload`-Union), `:315,348` (`story_type`/`participating_repos`), `governance/setup_preflight_gate/phase.py:174-178` (Einhaengepunkt nach Context-Berechnung, vor der Weiche) + `:188` (Worktree-Weiche), `bootstrap/composition_root.py:1317-1323` (`build_setup_phase_handler`), `implementation/phase.py:576-585` (Layer-1-Resolver mit unbedingtem `build_structural_are_provider(None, pipeline)` `:580-582` — hier den realen Client injizieren), `bootstrap/composition_root.py:2339-2372` (`build_structural_are_provider` -> `RequirementsCoverage(are_client, pipeline_config)`), `config/models.py:443` + `:45-60` (`ProjectConfig.are`/`AreConfig.rest_base_url`/`auth_token`) + `:445-454` (Validator erzwingt nur die `are`-Sektions-Praesenz, **nicht** `rest_base_url`), `requirements_coverage/are_client.py:33` (Konstruktor `AreClient(base_url, auth_token=None)`).
- **`are_gate.json`-Klarstellung (frueherer Story-Fehler, jetzt korrigiert):** Der Layer-1-Check `check_are_gate` (`verify_system/structural/checks/are_gate.py:35-41`) liest **keine** Datei. Er bekommt ein in-memory aufgeloestes `CoverageVerdict` ueber `checker.py:448-450` -> `composition_root.py:2395-2401` (`coverage_verdict` delegiert an `check_gate`). `are_gate.json` ist das FK-40-§40.5.4-Ergebnis-/Audit-Artefakt, das `check_gate` zusaetzlich schreibt — **nicht** der Input des Layer-1-Checks. Den Provider-Pfad **nicht** auf Datei-Lesen umbauen.
- **Kontext-Sinnhaftigkeit (Persistenzpfad):** FK-40 §40.5.2 nutzt `ArtifactManager.persist`, FK-22 §22.4b skizziert einen direkten `Path.write_text`. Autoritativ ist der **ArtifactManager-Pfad** (FK-71, Content-Plane, Producer-Registry) — den Direkt-Schreib-Pseudocode aus FK-22 nicht 1:1 uebernehmen. (Die FK-22-Pseudocode-Diskrepanz ist eine bewusste doc-Schwaeche; **nicht** im AG3-077-Code-Cut korrigieren, sondern bei Bedarf als doc-only-Nachzug an die FK-22-zustaendige Einheit melden — siehe Cross-Story-Voraussetzungen in `remediation-r1.md`.)
- Worker/Orchestrator-Lesbarkeit beachten: `are_bundle.json` ist fuer Worker/QA lesbar, fuer den Orchestrator blockiert (FK-40 §40.5.2) — bestehende Artefakt-Schutz-/Sichtbarkeitsregeln nicht aushebeln.
- **`AreClient`-Konstruktion ist Composition-Root-/Adapter-Arbeit (R-Code), kein neuer Fachbaustein:** den Client aus `ProjectConfig.are` an genau einer Stelle bauen und in den Layer-1-Resolver (`implementation/phase.py:580-582`) **und** den Setup-`load_are_bundle`-Collaborator injizieren. Den Config-Validator (`config/models.py:445-454`) **nicht** umbauen (FK-03-Owner) — den fehlenden `rest_base_url` nur an der Konstruktionsstelle fail-closed behandeln (`are_gate_unavailable` / Bundle FAILED), nicht durch eine neue Validierungsregel.
- Wenn die Aufrufer-Verdrahtung von Andock-Punkt 1 in die Story-Erstellung/VektorDB (AG3-068) eskaliert: **melden**, nicht still ausweiten. Mindestziel dieses Cuts: die vier Methoden real + `AreClient`-HTTP real + **`AreClient` aus Konfig konstruiert/injiziert** + Scope-Resolver + Schreibwege korrekt + Setup-Step eingehaengt.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Test-Namen der Schreibwege-/Statusmodell-/Negativpfad-/`AreClient`-HTTP-Tests.

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

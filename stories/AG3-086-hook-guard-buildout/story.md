# AG3-086: Hook-/Guard-Vollausbau (WebCallBudgetGuard, skill_usage_check, Prompt-Integrity, TTL→ESCALATED)

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `governance` — Guard-/Hook-Enforcement: die blockierenden PreToolUse-/Counter-Guards und die CCAG-Permission-Runtime, die deklarierte-aber-leere Hook-Pfade real durchsetzen.
**Quell-Konzepte (autoritativ):**
- `FK-30 §30.5.1` — Guard-Hook-Tabelle: `skill_usage_check`-Guard (blockt Ad-hoc-Tool-Aufruf, wenn passender Skill existiert und dessen Voraussetzung erfuellt ist; normative Erkennungsregeln F-43-030 in FK-43 §43.6.2) sowie der `budget`-Hook (Event-Emission in `agentkit.backend.telemetry.hooks`; **blockierender Anteil** in `agentkit.backend.governance.guard_system.WebCallBudgetGuard`)
- `FK-30 §30.5.1a` — Klasse `WebCallBudgetGuard` (`agentkit.backend.governance.guard_system.WebCallBudgetGuard`, liest Web-Call-Counter, exit 2 bei Hard-Limit `telemetry.web_call_limit` Default 200, Warnschwelle `telemetry.web_call_warning` Default 180; ausschliesslich Blocking, **kein** Event-Schreiben — Trennung observational vs. blockierend)
- `FK-68 §68.6` / `§68.6.0` — Budget-Tracking-Verantwortung: `web_call`-Event-Emission ist Telemetrie-Aufgabe (`BudgetEventEmitter`), die Blocking-Entscheidung ist Governance-Aufgabe (`WebCallBudgetGuard`)
- `FK-68 §68.2` / `§68.3.1` — `integrity_violation`-Event (`guard`/`detail`, `stage` **nur** bei `prompt_integrity_guard`) bei **jeder** Guard-Blockade (exit 2); die Quellen-Tabelle §68.3.1 listet die Guard-Hooks **inkl. `SkillUsageCheck`** explizit als Emitter; FK-30 §30.7.3 schreibt die Blockade-Details als `integrity_violation` ins `execution_events`-Backend
- `FK-61 §61.12.2` — angereicherte `integrity_violation`-Payload: das Feld `stage` ist explizit „**fuer prompt_integrity_guard**" gefuehrt (Quelle `prompt_integrity_guard.py`), also **nicht** unbedingt-pflichtig fuer jede `integrity_violation`-Emission. Der reale `MANDATORY_PAYLOAD_FIELDS`-Vertrag (`src/agentkit/telemetry/events.py:180` `EventType.INTEGRITY_VIOLATION: ("stage",)`) pinnt `stage` heute jedoch **unbedingt** fuer **jede** `integrity_violation` und der Contract-Test friert das ein (`tests/contract/telemetry/test_event_catalog.py:114`, Vergleich `:150`). Die hier neu gebauten Nicht-prompt-integrity-Emitter (`skill_usage_check`, `WebCallBudgetGuard`) schreiben aber **kein** `stage` — sie sind ohne eine Vertrags-Migration nicht regelkonform baubar (s. Scope 0 + AC0)
- `FK-31 §31.7` — Prompt-Integrity-Guard (PreToolUse auf jeden `Agent`-Tool-Call; permanent aktiv, **modus-scharf**: §31.7.1 Modusgrenze + §31.7.2 3-Stufen-Tabelle; Spawn-Schema-Header `AGENTKIT-SUBAGENT-V1 mode=X role=Y story_id=Z skill_proof=W`)
- `FK-42 §42.2.4` — CCAG: Capability-Huelle als Vorbedingung von `evaluate_ccag()` (fail-closed bei Abwesenheit, `runtime.py` ist heute fail-open)
- `FK-42 §42.3` / `§42.3.1` (F-42-039) / `§42.3.3` — LLM-gestuetzte Regelgeneralisierung als **Vorschlag**; ohne explizite Bestaetigung wird keine Regel gespeichert; Default-Schnitt: erste positive Entscheidung ist nur Einzelfall/Lease, eine Dauerregel entsteht erst durch bewusste Zusatzentscheidung
- `FK-42 §42.4.2 Schritt 5` — Permission-Request-TTL-Ablauf ohne Entscheidung -> Run deterministisch `ESCALATED`
- `FK-93 §93.5a` — Permission-Request-TTL Default **1800s** (`permissions.request_ttl_s`)

---

## 1. Kontext / Ist-Zustand (belegt)

Vier Enforcement-Pfade sind als Hook-Identifier registriert oder konzeptuell vorgesehen, aber **nicht** in dem von FK-30/FK-31/FK-42 geforderten Endzustand:

- **`WebCallBudgetGuard` FEHLT als getrennte Klasse — und der Block-Pfad sitzt am falschen Owner:** Das Konzept (FK-30 §30.5.1a, FK-68 §68.6.0) verlangt eine **strikte Trennung**: Telemetrie schreibt das `web_call`-Event observational, Governance trifft die Blocking-Entscheidung. Real ist diese Trennung **verletzt**: `BudgetEventEmitter` (`src/agentkit/telemetry/hooks/budget_event_emitter.py`) traegt heute eine **Doppelrolle** und **blockiert selbst** — er emittiert `web_call` **und** liefert ein `GuardVerdict.block(...)` bei `over_budget` (`budget_event_emitter.py:51-54` `name = "budget_event_emitter"`, `:112` `over_budget = current_count > self._web_call_limit`, `:132-149` Block-Verdict). Er ist als **PreToolUse-Hook** verdrahtet: der Runner dispatcht `budget_event_emitter` ueber `_run_budget_event_emitter` (`src/agentkit/governance/runner.py:591-594`, Implementierung `:913-987`, Verdict-Rueckgabe `:945`/`:950`/`:963`/`:987`); `base.py:21-23` dokumentiert die Emitter-Doppelrolle ausdruecklich. Im `governance.guard_system`-Namespace existiert **keine** `WebCallBudgetGuard`-Klasse. Der Hook-Identifier `budget` ist in `hook_registration.py:53` (`BUDGET = "budget"`) registriert, aber **ohne** zugehoerige blockierende Guard-Klasse. **Folge:** Es gibt heute einen Block-Pfad am **falschen** Owner (Telemetrie statt Governance) und einen dazu **disjunkten** registrierten, aber leeren `budget`-Identifier — ein offener Duplicate-/Wrong-Owner-Zustand (Gap FK-30 §30.5.1a, FK-68 §68.6.0).
- **`skill_usage_check` UNVOLLSTAENDIG:** Identifier registriert (`hook_registration.py:54` `SKILL_USAGE_CHECK = "skill_usage_check"`, sowie in `runner.py`), aber keine Erkennungs-/Block-Logik (Gap FK-30 §30.5.1 / FK-43 §43.6.2).
- **Prompt-Integrity-Guard FEHLT als Produktions-Guard:** Es gibt **keine** Guard-Klasse und **keinen** `prompt_integrity`-HookIdentifier — Grep `prompt_integrity|PromptIntegrity` ueber `src/agentkit/` -> 0 Treffer (Gap FK-31 §31.7). Der **Spawn-Schema-Header existiert dagegen bereits** als Resource-/Skill-Vertrag und muss vom Guard **konsumiert** (nicht neu erfunden) werden: `src/agentkit/resources/skill_bundles/execute-userstory-core/4.0.0/SKILL.md:57-62` definiert den Header `AGENTKIT-SUBAGENT-V1 mode=<...> role=<...> story_id=<...> skill_proof=<...>`, `:64-69` die Story-Execution-Belegung (`mode=story_execution`, `story_id`, `skill_proof={{AGENT_SPAWN_SKILL_PROOF}}`), `:71` „The governance hook blocks non-conformant spawns", `:151-153` ein konkretes Spawn-Beispiel mit dem Header. Der Header ist also Soll-Vertrag, aber es fehlt der produktive Guard, der ihn durchsetzt.
- **CCAG Capability-Huelle + TTL-Eskalation UNVOLLSTAENDIG/ABWEICHEND:** `CcagPermissionRuntime.evaluate(hook_event)` (`governance/ccag/runtime.py:256`) nimmt nur ein `HookEvent`, hat **keine** Capability-Huelle als Vorbedingung und ist global **fail-open** (Exception -> allow, `runtime.py:275-287`, Kommentar „CCAG is a comfort layer; Guards are the last line of defence"). TTL existiert (`ccag/requests.py:42` `DEFAULT_TTL_SECONDS = 600`, `effective_status()` `:91-99` -> `"expired"`), setzt aber den Run **nicht** auf `ESCALATED`. Der TTL-Default weicht zudem von FK-93 §93.5a ab (600s im Code vs. 1800s im Konzept) und es fehlt der typisierte Config-Key `permissions.request_ttl_s` (heute keine `permissions.*`-Config). LLM-Regelgeneralisierung fehlt (`learned_from`-Feld in `rules.py` vorhanden, aber keine Generalisierungslogik) (Gap FK-42 §42.2.4/§42.3/§42.4.2).
- **`integrity_violation`-Payload-Vertrag zu eng fuer die neuen Guard-Emitter (Vertrag vs. Konzept):** FK-61 §61.12.2 / FK-68 §68.2 fuehren `stage` als `integrity_violation`-Feld **„fuer prompt_integrity_guard"** — also **bedingt**, nur fuer den Prompt-Integrity-Pfad. Der reale Mandatory-Payload-Vertrag pinnt `stage` aber **unbedingt** fuer **jede** `integrity_violation`: `MANDATORY_PAYLOAD_FIELDS[EventType.INTEGRITY_VIOLATION] = ("stage",)` (`src/agentkit/telemetry/events.py:173` Map-Beginn, `:180` der Eintrag), durchgesetzt von `validate_event_payload()` (`events.py:257`) und eingefroren vom Contract-Test (`tests/contract/telemetry/test_event_catalog.py:114` `_EXPECTED_MANDATORY_FIELDS[INTEGRITY_VIOLATION] = ("stage",)`, Assertion `:150` `dict(MANDATORY_PAYLOAD_FIELDS) == _EXPECTED_MANDATORY_FIELDS`). **Folge:** Die in dieser Story neu gebauten Nicht-prompt-integrity-Emitter (`skill_usage_check`, `WebCallBudgetGuard`) sollen `integrity_violation` **ohne** `stage` schreiben (FK-68 §68.2) — das wuerde heute fail-closed an `validate_event_payload` scheitern, und ein `stage`-loser Vertrag wuerde den Contract-Test brechen. Damit ist AC9 (Contract-Tests gruen) mit dem heutigen unbedingten Pinning **nicht** erreichbar. Der Vertrag muss konzeptkonform auf **bedingt** umgestellt werden: `guard`/`detail` pflichtig, `stage` gueltig/pflichtig **nur** bei `guard="prompt_integrity_guard"` (Owner-Hinweis: Mandatory-Payload-Contract-Owner ist AG3-081, s. Scope 0) (Gap FK-61 §61.12.2 / FK-68 §68.2 vs. Code).

Anknuepfungspunkte (existieren, konsumieren statt neu bauen):
- Hook-Dispatch/Registrierung: `governance/runner.py`, `governance/hook_registration.py` (`HookId`-Enum, PRE/POST-Hook-Listen).
- Bestehende Guards als Muster: `governance/guards/branch_guard.py`, `scope_guard.py`, `artifact_guard.py`, `self_protection_guard.py`, `story_creation_guard.py`.
- Web-Call-Counter + `web_call`-Event: `telemetry/hooks/budget_event_emitter.py` (Counter-Projektion via `emitter.query(story_id, EventType.WEB_CALL)`, Config `telemetry.web_call_limit`).
- Skills-Surface: `skills/top.py` (`resolve_binding`, Bundle-Voraussetzungen) zur Erkennung „passender Skill existiert + Voraussetzung erfuellt".
- Spawn-Header-Vertrag: `resources/skill_bundles/execute-userstory-core/4.0.0/SKILL.md` (Header-Schema + Story-Execution-Belegung) — Soll-Format fuer Stufe 2 des Prompt-Integrity-Guards.
- CCAG: `governance/ccag/runtime.py` (`evaluate`), `requests.py` (TTL), `rules.py` (`learned_from`); Capability-Modell `principal_capabilities/*` (`hard_capability_verdict`, Freeze-Overlay).

## 2. Scope

### 2.1 In Scope
0. **`integrity_violation`-Payload-Vertrag konzeptkonform auf bedingtes `stage` migrieren (Voraussetzung fuer AC2b/AC5b/AC9; FIX THE MODEL):**
   - Der Mandatory-Payload-Vertrag fuer `integrity_violation` wird von „unbedingt `stage`" auf den FK-61 §61.12.2/FK-68 §68.2-Sollzustand umgestellt: **`guard` und `detail` sind pflichtig fuer jede `integrity_violation`**; **`stage` ist bedingt** und nur fuer `guard="prompt_integrity_guard"` gueltig/pflichtig (mit `stage ∈ {escape_detection, schema_validation, template_integrity}`). Konkret betrifft das `MANDATORY_PAYLOAD_FIELDS[EventType.INTEGRITY_VIOLATION]` (`src/agentkit/telemetry/events.py:180`), die `validate_event_payload()`-Logik (`events.py:257`, bedingte Pflichtfeld-Pruefung statt fixem Tupel) und den Contract-Test (`tests/contract/telemetry/test_event_catalog.py:114`/`:150`).
   - **Owner-Klarstellung / Cross-Story (keine stille Fremd-Mutation):** Der kanonische **Mandatory-Payload-Contract-Owner** ist **AG3-081** („EventType-Katalog + Mandatory-Payload-Contract", AG3-081 §2.1; es reconciled dort bereits Schwester-Events wie `are_gate_result`). AG3-086 ist der **Producer**-Owner, der diese Vertragsaenderung **ausloest** (es fuehrt die ersten Nicht-prompt-integrity-`integrity_violation`-Emitter ein). Die Vertrags-Map/`validate_event_payload`/`test_event_catalog.py`-Migration wird daher **mit AG3-081 abgestimmt** und AG3-086 nimmt `AG3-081` als `depends_on` auf (s. `status.yaml`). AG3-086 baut die Producer und konsumiert den bedingten Vertrag; die kanonische Map-Definition bleibt am AG3-081-Owner. **Kein** zweites Contract-System, **keine** stille Mutation eines Fremd-Owners ohne abgestimmte Voraussetzung. (Hinweis: AG3-081 §2.1 listet die `integrity_violation`-`stage`-Bedingung heute nicht explizit; AG3-086 fuehrt sie als producer-getriebene Voraussetzung ein und beansprucht **nicht**, dass AG3-081 sie bereits liefert.)
   - **ARCH-55:** Vertragsfelder englisch (`guard`, `detail`, `stage`, `prompt_integrity_guard`).
1. **`WebCallBudgetGuard` als alleiniger Block-Owner — inkl. Migration des bestehenden Block-Pfads (FIX THE MODEL, NO ERROR BYPASSING):**
   - Neue blockierende Klasse `agentkit.backend.governance.guard_system.WebCallBudgetGuard`: liest den bestehenden Web-Call-Counter (Projektion `EventType.WEB_CALL`), blockt fail-closed (exit 2 / Block-Decision) bei `>= telemetry.web_call_limit` (Default 200, typisierte Config, englischer Key); Warnschwelle `telemetry.web_call_warning` (Default 180) -> allow + Warnung; nur Research-Stories (`story_type == "research"`), sonst kein Blocken. **Kein** Event-Schreiben des `web_call`-Counters (FK-30 §30.5.1a Trennung — der Counter bleibt Telemetrie-Aufgabe); der Guard emittiert lediglich das governance-eigene `integrity_violation`-Block-Audit (s. Scope-3-Telemetrie und AC5b).
   - **Fail-closed bei unaufloesbarem Story-Typ wandert mit zum Governance-Owner (kein fail-open-Regress):** Heute liegt diese Block-Entscheidung am **falschen** Owner im Emitter-Dispatch (`runner.py:960-968`: ist der Story-Typ fuer einen Web-Tool-Call nicht aufloesbar — Backend-Fehler oder fehlender Record —, wird fail-closed geblockt und **nicht** stillschweigend auf „nicht-Research"/allow heruntergestuft; Tests `tests/integration/governance/test_budget_event_emitter_dispatch.py:252-268`, `tests/unit/telemetry/hooks/test_budget_event_emitter.py:81-91`). Mit der Migration uebernimmt **`WebCallBudgetGuard`** diese Entscheidung: kann der Story-Typ fuer einen Web-Tool-Call nicht aufgeloest werden, blockt der Guard fail-closed (kein Downgrade auf nicht-Research, kein allow). Verhalten bleibt erhalten, nur der Owner aendert sich (Telemetrie -> Governance). FAIL-CLOSED / NO ERROR BYPASSING.
   - **Migration des heutigen Doppelrollen-Blocks:** Der blockierende Anteil wird aus `BudgetEventEmitter` **entfernt/neutralisiert** — der Emitter bleibt rein observational (schreibt `web_call`, liefert **kein** Block-Verdict mehr, auch nicht fuer den unaufloesbaren Story-Typ). Damit existiert genau **ein** Block-Owner (`WebCallBudgetGuard`), keine Doppelblockade. Die `base.py`-Doppelrollen-Doku und der bisherige `_run_budget_event_emitter`-Dispatch in `runner.py:591-594`/`:913-987` (inkl. des fail-closed-Zweigs `:960-968`) werden entsprechend konsolidiert: Block-Dispatch laeuft fortan unter dem `budget`-HookIdentifier (`hook_registration.py:53`) gegen `WebCallBudgetGuard`; der observationale `web_call`-Emitter-Pfad bleibt erhalten, aber ohne Verdict. **Negativtest gegen Doppelblockade und falschen Owner** sowie **Migrationstest „unaufloesbarer Story-Typ -> Block jetzt vom Governance-Owner"** (s. AK1b).
   - **Hinweis Pre/Post:** FK-30 §30.5.1a / FK-68 §68.6 sehen den observationalen `BudgetEventEmitter` als **PostToolUse**-Emitter und `WebCallBudgetGuard` als blockierenden Pfad. Der Sub-Agent richtet die Trigger-Phasen an FK-30 §30.5.1a aus (Guard blockt vor dem Web-Call); die heutige PreToolUse-Verdrahtung des Emitters wird Teil der Konsolidierung, nicht stillschweigend uebernommen.
   - **Telemetrie:** Jeder Block-Pfad von `WebCallBudgetGuard` (Hard-Limit-Ueberschreitung **und** unaufloesbarer Story-Typ) emittiert ein `integrity_violation`-Event mit `guard="web_call_budget_guard"` und `detail`, **ohne** `stage` (setzt die Vertrags-Migration aus **Scope 0** voraus). FK-68 §68.3.1 fuehrt die Guard-Hooks generell als `integrity_violation`-Emitter bei Blockade (exit 2) (`68_telemetrie_eventing_workflow_metriken.md:447`); FK-30 §30.7.3 schreibt Blockade-Details als `integrity_violation` (`30_hook_adapter_guard_enforcement.md:784`). Das ist **nicht** das `web_call`-Counter-Event (das bleibt observational beim Emitter, FK-30 §30.5.1a) — der Guard schreibt ausschliesslich das Governance-Block-Audit.
2. **`skill_usage_check`-Guard**: Erkennungs-/Block-Logik gem. FK-43 §43.6.2 (F-43-030):
   - erkennt, ob fuer einen Ad-hoc-Tool-Aufruf ein **passender Skill** existiert **und** dessen Voraussetzung erfuellt ist; wenn ja -> Block (Hinweis auf Skill).
   - Erkennungsregeln typisiert; Konsum der Skills-Surface (`skills/top.py`) — kein Skill-Katalog-Neubau.
   - **Telemetrie:** Jede Blockade emittiert ein `integrity_violation`-Event mit `guard="skill_usage_check"` und `detail`, **ohne** `stage` (das Feld ist laut FK-61 §61.12.2/FK-68 §68.2 ausschliesslich prompt_integrity-spezifisch). Dies setzt die Vertrags-Migration aus **Scope 0** voraus (sonst scheitert die Emission an `validate_event_payload`, das heute `stage` unbedingt verlangt — `events.py:180`). FK-68 §68.3.1 listet die Guard-Hooks **inkl. `SkillUsageCheck`** explizit als `integrity_violation`-Emitter bei Blockade (exit 2) (verifiziert `68_telemetrie_eventing_workflow_metriken.md:447`); FK-30 §30.7.3 schreibt die Blockade-Details als `integrity_violation` ins `execution_events`-Backend (verifiziert `30_hook_adapter_guard_enforcement.md:784`).
3. **Prompt-Integrity-Guard** (`FK-31 §31.7`) als PreToolUse-Hook auf **jeden** `Agent`-Tool-Call, **modus-scharf** gemaess §31.7.1 (Modusgrenze) und §31.7.2 (3-Stufen-Tabelle):
   - **Stufe 1 — Governance-Escape-Regex** (Prompt-Injection-/Eskalations-Muster, z. B. „ignore all previous instructions") -> Block. **In beiden Modi aktiv** (permanent).
   - **Stufe 2 — Spawn-Schema-Validierung** des Headers `AGENTKIT-SUBAGENT-V1 mode/role/story_id/skill_proof` (typisierte Felder, Konsum des bestehenden Resource-Vertrags aus SKILL.md):
     - `ai_augmented` (Freestyle): **leichtgewichtiges** Freestyle-Spawn-Schema — `role=general` mit `skill_proof=null` zulaessig; fehlender/struktur-ungueltiger Header -> Block.
     - `story_execution`: **volle** Schema-Pflicht — gueltiges `skill_proof`-Token (aus installiertem Manifest), `mode=story_execution`, `story_id` gesetzt; fehlend/ungueltig -> Block.
   - **Stufe 3 — Template-Integritaetspruefung:** **nur in `story_execution`** (§31.7.2). Whitespace-normalisierter Vergleich des Spawn-Prompts gegen das installierte Template (mit `<STORY-ID>`/`<ROUND>`-Substitution) -> Block bei Abweichung. **QA-Agents sind ausgenommen** (dynamische Prompts, §31.7.2). Im `ai_augmented`-Modus findet Stufe 3 **nicht** statt.
   - Permanent aktiv, aber nicht permanent gleich streng (§31.7.1). **Neuer HookIdentifier** `prompt_integrity` registriert (`hook_registration.py` + `runner.py` PRE-Hook-Dispatch).
   - **Telemetrie:** Jede Blockade emittiert ein `integrity_violation`-Event (FK-68 §68.2 / §68.3.1) mit `guard="prompt_integrity_guard"` und `detail` (beide pflichtig fuer jedes `integrity_violation`, Scope 0) **plus** `stage`, wobei das `stage`-Feld **prompt_integrity-spezifisch** ist (gueltig/pflichtig nur bei `guard="prompt_integrity_guard"`) und `stage ∈ {escape_detection, schema_validation, template_integrity}` je Pruefstufe annimmt (FK-61 §61.12.2 „fuer prompt_integrity_guard"; FK-68 §68.2 Governance-Tabelle, Zeile `integrity_violation`). Opake Block-Meldung nach aussen (§31.7.3: Exit 2 „Operation not permitted.", kein Diff, keine Stufenangabe) — die Stufe steht **nur** im Telemetrie-Event, nicht in der Block-Meldung an den Aufrufer.
4. **CCAG Capability-Huelle-Vorbedingung (§42.2.4):** `evaluate()` darf nur nach vorab berechneter Capability-Huelle (`principal_type`, `path_class`, `operation_class`, `hard_capability_verdict`, `freeze_verdict`) laufen. **Fehlt die Huelle, ist der CCAG-Aufruf unzulaessig und erzeugt einen fail-closed Block** (kein global-allow). Das bestehende fail-open-Verhalten (Exception -> allow, `runtime.py:275-287`) wird auf fail-closed umgestellt (NO ERROR BYPASSING).
5. **TTL-Ablauf -> Run ESCALATED (§42.4.2 Schritt 5):** Laeuft ein Permission-Request ohne Entscheidung ab (`effective_status() == "expired"`), wird der **Run** deterministisch auf `ESCALATED` gesetzt (statt nur stiller request-`expired`). Verdrahtung an die autoritative Run-Status-Quelle (kein Schattenfeld).
   - **Typisierter TTL-Config-Key (FK-93 §93.5a):** Der TTL-Wert wird ueber den typisierten Config-Key `permissions.request_ttl_s` (Default **1800s**) bezogen statt ueber das hartkodierte `DEFAULT_TTL_SECONDS = 600`. `permissions.request_ttl_s` ist ein neuer typisierter Config-Owner (englischer Key, Pydantic). Der bisherige Code-Default 600 wird damit auf den FK-93-Sollwert 1800 gehoben. (Hinweis: Die uebergreifende Defaults-Reconciliation FK-93↔Code ist doc-only-Owner AG3-103; AG3-086 setzt fuer die hier gebaute Permission-TTL den FK-93-konformen Sollwert direkt im typisierten Config-Modell — siehe Cross-Story-Voraussetzungen.)
6. **LLM-gestuetzte Regelgeneralisierung (§42.3) — als Vorschlag, mit Confirm-/Promote-Barriere (§42.3.1 F-42-039, §42.3.3, §42.4.2):**
   - Das LLM erzeugt aus einem Einzelaufruf bzw. einer NL-Absicht **nur einen Regel-Vorschlag/Draft** (Regex + ausgefuellte Felder `tool`/`allow_pattern`/`description`). **Keine Persistenz allein durch den LLM-Aufruf.**
   - `approved.yaml` (mit `learned_from`/`learned_at`) wird **erst nach einer expliziten Promote-/Confirm-Entscheidung** geschrieben. Die erste positive Entscheidung zu einem offenen Permission-Case ist nur Einzelfall/Lease und erzeugt **keine** Dauerregel ohne separate Zusatzentscheidung (§42.3 Default-Schnitt / §42.4.2 letzter Absatz).
   - LLM-Aufruf ueber den bestehenden Evaluator-/LLM-Transport.
7. **Tests:** `integrity_violation`-Vertrag bedingt (skill/budget-Event ohne `stage` validiert gruen, prompt_integrity-Event ohne `stage` fail-closed, Contract-Test gruen mit bedingtem Soll — AC0), Counter-Schwellen-Block (unter/ueber Limit), Migrations-Negativtest „kein Doppelblock, korrekter Owner", Migrationstest „unaufloesbarer Story-Typ -> fail-closed Block jetzt vom Governance-Owner `WebCallBudgetGuard`" (Verhalten aus `test_budget_event_emitter_dispatch.py:252-268` / `test_budget_event_emitter.py:81-91` bleibt erhalten, Owner wechselt), `WebCallBudgetGuard`-Block emittiert `integrity_violation` (Hard-Limit + unaufloesbar), `skill_usage` Block vs. Allow (Skill vorhanden+Voraussetzung vs. nicht) + `integrity_violation`-Emission bei Block, Prompt-Integrity **je Modus**: Stufe-1-Escape (beide Modi), Stufe-2-Schema (Freestyle-leicht-Positiv + Story-Execution-voll, je Negativfall), Stufe-3-Template (nur story_execution; QA-Agent-Ausnahme greift), Telemetrie-`integrity_violation`-Emission je `stage` (Prompt-Integrity), CCAG fehlende Huelle -> fail-closed Block, TTL-Ablauf -> Run ESCALATED, Regelgeneralisierung erzeugt **Vorschlag** und persistiert erst nach Confirm (inkl. Negativtest „keine Persistenz ohne Confirm").

### 2.2 Out of Scope (mit Owner)
- **Worker-Health-Monitor-Hooks (`health_monitor`, FK-30 §30.10)** — **AG3-080** (FK-49). Der Identifier bleibt dort Owner; diese Story fasst `health_monitor` und §30.10 nicht an.
- **Web-Call-Counter-/`web_call`-Event-Erzeugung** selbst — bleibt beim bestehenden `BudgetEventEmitter` (observational); hier nur konsumiert. AG3-086 entfernt aus dem Emitter **nur** den Block-Verdict, nicht die Event-Emission.
- **EventType-Katalog + Mandatory-Payload-Contract als Ganzes (BC14/BC15-EventTypes, `MANDATORY_PAYLOAD_FIELDS_BY_NAME`, `validate_event_payload`-Grundgeruest)** — kanonischer Owner **AG3-081**. AG3-086 fasst **nur** den einen `integrity_violation`-Eintrag an (producer-getriebene `stage`-Konditionalisierung, Scope 0), in Abstimmung mit dem Owner und ueber `depends_on: AG3-081`; es baut **keinen** zweiten Contract-Pfad und beansprucht **nicht**, dass AG3-081 die `integrity_violation`-`stage`-Bedingung bereits liefert (AG3-081 §2.1 listet sie heute nicht).
- **Governance-Beobachtung / Risikoscore / Adjudication** — **AG3-085** (FK-35 §35.3). Die hier gebauten Guards **emittieren** Signale (insb. `integrity_violation`, s. In-Scope 3), die AG3-085 konsumiert; die Akkumulation/Bewertung gehoert nicht hierher.
- **Uebergreifende FK-93-Defaults-Reconciliation** (welcher Wert pro Parameter autoritativ ist, Code vs. FK) — **doc-only AG3-103**. AG3-086 setzt **nur** den hier gebauten Permission-Request-TTL auf den FK-93-§93.5a-Sollwert (`permissions.request_ttl_s` = 1800) im typisierten Config-Modell; die restlichen FK-93-Werte bleiben AG3-103.
- **Concept-Validation-/Concept-Build-Git-Hooks** (`tools/hooks/`) — ausserhalb `src/agentkit/`, nicht Teil dieser Story.
- **CCAG-Namespace-Umzug** `ccag/` -> `ccag_permission_runtime/` (kosmetisch) — nicht in dieser Story (Konzept-Nachzug, doc-only AG3-102 fuehrt den `ccag`-Namespace an die Code-Realitaet).

## 3. Akzeptanzkriterien
0. **`integrity_violation`-Vertrag ist bedingt:** Der Mandatory-Payload-Vertrag (`MANDATORY_PAYLOAD_FIELDS[EventType.INTEGRITY_VIOLATION]`, `events.py:180`; `validate_event_payload`, `events.py:257`; Contract-Test `test_event_catalog.py:114`/`:150`) verlangt fuer **jede** `integrity_violation` `guard` und `detail`; `stage` ist **nur** fuer `guard="prompt_integrity_guard"` gueltig/pflichtig. Migration mit AG3-081 (Contract-Owner) abgestimmt, `depends_on: AG3-081`. Tests: ein `integrity_violation` mit `guard="skill_usage_check"`/`detail` **ohne** `stage` validiert gruen; ein `prompt_integrity_guard`-Event **ohne** `stage` schlaegt fail-closed fehl; Contract-Test gruen (`dict(MANDATORY_PAYLOAD_FIELDS) == _EXPECTED_MANDATORY_FIELDS` mit dem neuen bedingten Soll).
1. `WebCallBudgetGuard` existiert als eigene blockierende Klasse im `governance.guard_system`-Namespace (getrennt vom Emitter), schreibt **kein** `web_call`-Counter-Event (das bleibt observational beim Emitter) und blockt fail-closed bei `>= telemetry.web_call_limit` (Default 200); Warnschwelle `telemetry.web_call_warning` (Default 180) -> allow+Warnung; nicht-Research -> kein Block (Tests: unter Limit -> allow, Warnschwelle -> allow+Warn, >= Limit -> block, non-research -> allow).
1b. **Migration belegt:** `BudgetEventEmitter` liefert **kein** Block-Verdict mehr (bleibt observational, emittiert weiter `web_call`); der `budget`-HookIdentifier dispatcht gegen `WebCallBudgetGuard`. Negativtest: ein einzelner ueberbudgetierter Web-Call erzeugt **genau einen** Block (kein Doppelblock) und der Block stammt vom **Governance-Owner** `WebCallBudgetGuard`, nicht vom Emitter.
1c. **Fail-closed bei unaufloesbarem Story-Typ bleibt erhalten, neuer Owner:** Ein Web-Tool-Call, dessen Story-Typ nicht aufgeloest werden kann (Backend-Fehler oder fehlender Record), blockt fail-closed unter `WebCallBudgetGuard` (kein Downgrade auf nicht-Research, kein allow). Migrationstest: das heute am Emitter belegte Verhalten (`test_budget_event_emitter_dispatch.py:252-268`, `test_budget_event_emitter.py:81-91`) wird auf den Governance-Owner umgezogen — der Block bleibt, der Owner wechselt zu `WebCallBudgetGuard`, und der Emitter erzeugt fuer diesen Fall **kein** Verdict mehr.
2. `skill_usage_check`-Guard blockt einen Ad-hoc-Tool-Aufruf genau dann, wenn ein passender Skill existiert **und** dessen Voraussetzung erfuellt ist; sonst allow (Tests beide Richtungen).
2b. Jede `skill_usage_check`-Blockade emittiert ein `integrity_violation`-Event mit `guard="skill_usage_check"` und `detail`, **ohne** `stage` (FK-61 §61.12.2/FK-68 §68.2: `stage` ist prompt_integrity-spezifisch; FK-68 §68.3.1 listet `SkillUsageCheck` explizit als `integrity_violation`-Emitter, FK-30 §30.7.3). Setzt AC0 (bedingter Vertrag) voraus (Test: Block -> genau ein `integrity_violation` mit korrektem `guard`, validiert gruen ohne `stage`; Allow -> kein Event).
3. Prompt-Integrity-Guard ist als HookIdentifier `prompt_integrity` registriert und PreToolUse auf jeden `Agent`-Tool-Call gebunden; **permanent aktiv, modus-scharf** (§31.7.1/§31.7.2):
   - Stufe 1 (Escape-Regex) blockt in **beiden** Modi (Test je Modus).
   - Stufe 2 (Schema): Freestyle akzeptiert `role=general`/`skill_proof=null` und blockt fehlenden/struktur-ungueltigen Header; `story_execution` verlangt gueltiges `skill_proof` und blockt fehlend/ungueltig (Tests je Modus, Positiv + Negativ).
   - Stufe 3 (Template) blockt eine whitespace-normalisierte Abweichung **nur in `story_execution`**; findet im `ai_augmented`-Modus **nicht** statt; **QA-Agents sind ausgenommen** (Tests: story_execution-Block, ai_augmented-kein-Stufe-3, QA-Agent-Ausnahme).
4. Spawn-Header `AGENTKIT-SUBAGENT-V1 mode/role/story_id/skill_proof` wird gegen den bestehenden Resource-Vertrag (SKILL.md) typisiert validiert; fehlender/ungueltiger Header -> Block (Test).
5. Jede Prompt-Integrity-Blockade emittiert ein `integrity_violation`-Event mit `guard="prompt_integrity_guard"` und `detail` **plus** `stage` (`stage ∈ {escape_detection, schema_validation, template_integrity}`, prompt_integrity-spezifisch, AC0); die Block-Meldung nach aussen bleibt opak (§31.7.3) (Test je `stage`).
5b. Jeder `WebCallBudgetGuard`-Block (Hard-Limit **und** unaufloesbarer Story-Typ) emittiert ein `integrity_violation`-Event mit `guard="web_call_budget_guard"` und `detail`, **ohne** `stage` (FK-61 §61.12.2/FK-68 §68.2, FK-68 §68.3.1, FK-30 §30.7.3; setzt AC0 voraus); dies ist **nicht** das observationale `web_call`-Counter-Event (das bleibt beim Emitter) (Tests: Hard-Limit-Block -> `integrity_violation` validiert gruen ohne `stage`; unaufloesbar -> `integrity_violation`).
6. `CcagPermissionRuntime.evaluate()` ist nur mit Capability-Huelle zulaessig; fehlende Huelle -> fail-closed Block (kein global-allow); das bisherige Exception->allow-Verhalten ist beseitigt (Tests: ohne Huelle -> Block; Exception-Pfad -> Block statt allow).
7. Permission-Request-TTL-Ablauf setzt den Run deterministisch auf `ESCALATED`; der TTL-Wert kommt aus dem typisierten Config-Key `permissions.request_ttl_s` (Default 1800s, FK-93 §93.5a), nicht aus dem hartkodierten 600 (Tests: abgelaufener Request -> Run ESCALATED; Default-Wert == 1800).
8. LLM-Regelgeneralisierung erzeugt aus einem Einzelaufruf/NL-Intent **nur einen Vorschlag**; `approved.yaml` (`learned_from`/`learned_at`) wird **erst nach explizitem Confirm/Promote** geschrieben (Tests mit Fake-LLM an der LLM-Grenze: Vorschlag erzeugt; Negativtest „keine Persistenz ohne Confirm"; Positivtest „Persistenz nach Confirm mit `learned_from`").
9. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage >= 85 %.

## 4. Definition of Done
- AK 0–9 (inkl. 1b/1c, 2b, 5b) erfuellt; giftige Codex-Review PASS; (Implementierung/Commit erst nach Execution-Plan-Freigabe — diese Story wird zunaechst nur autorisiert/reviewt).

## 5. Guardrail-Referenzen
- **FAIL-CLOSED:** Budget-Ueberschreitung, **unaufloesbarer Story-Typ auf einem Web-Tool-Call**, fehlende CCAG-Huelle, ungueltiger Spawn-Header/Template, TTL-Ablauf, Escape-Muster — alle blockieren/eskalieren, keine fail-open-Toleranz. Der bestehende fail-closed-Block fuer den unaufloesbaren Story-Typ (`runner.py:960-968`) wird **nicht** verloren, sondern zum Governance-Owner `WebCallBudgetGuard` migriert (kein Downgrade auf allow). Das bestehende CCAG-fail-open (`runtime.py:275-287`) wird beseitigt (NO ERROR BYPASSING).
- **FIX-THE-MODEL / SINGLE SOURCE OF TRUTH:** Genau **ein** Block-Owner fuer das Web-Budget (`WebCallBudgetGuard`); der heutige Doppelrollen-Block im `BudgetEventEmitter` wird aufgeloest. Guard liest den bestehenden Web-Call-Counter und die Skills-Surface; keine zweite Counter-/Skill-Wahrheit. Der Spawn-Header-Vertrag wird aus der bestehenden Resource (SKILL.md) **konsumiert**, nicht gedoppelt. Der `integrity_violation`-Payload-Vertrag bleibt **eine** Wahrheit (`MANDATORY_PAYLOAD_FIELDS`, Owner AG3-081): AG3-086 stellt `stage` konzeptkonform auf **bedingt** um (nur `prompt_integrity_guard`), statt einen zweiten Validator/Map-Pfad fuer die budget/skill-Events zu bauen — Vertrag fixen, nicht umgehen.
- **TYPISIERT STATT STRINGS:** Spawn-Header-Felder, Erkennungsregeln, Massnahmen, Config-Keys (`telemetry.web_call_limit`/`telemetry.web_call_warning`/`permissions.request_ttl_s`) typisiert; keine ad-hoc String-Parsing-Kaskaden.
- **ARCH-55:** Header-Token (`AGENTKIT-SUBAGENT-V1`, `mode/role/story_id/skill_proof`), `stage`-Werte, Config-Keys, HookIdentifier englisch.
- **SEVERITY-SEMANTIK:** Warnschwelle (`telemetry.web_call_warning`) -> WARNING (allow + Spiegelung); Hard-Limit/Escape/Huelle/TTL -> ERROR/Block bzw. ESCALATED.
- **ZERO DEBT:** kein deklarierter-aber-leerer Hook bleibt zurueck; der `budget`-Identifier bekommt seinen realen Block-Owner, `prompt_integrity` wird neu registriert und durchgesetzt, `skill_usage_check` erhaelt reale Logik.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- **WebCallBudget ist eine Migration, kein Greenfield-Bau:** Der Block sitzt heute **falsch** im `BudgetEventEmitter` (`budget_event_emitter.py:112`/`:132-149`, PreToolUse-Dispatch `runner.py:591-594`/`:913-987`). Erst den Block aus dem Emitter entfernen (observational lassen), dann `WebCallBudgetGuard` als alleinigen Block-Owner einfuehren und den `budget`-Identifier (`hook_registration.py:53`) dagegen dispatchen. Doppelblockade und falschen Owner mit Tests ausschliessen. **Den fail-closed-Zweig fuer den unaufloesbaren Story-Typ (`runner.py:960-968`) NICHT loeschen** — er wandert mit zum Guard (sonst fail-open-Regress; die bestehenden Tests `test_budget_event_emitter_dispatch.py:252-268` / `test_budget_event_emitter.py:81-91` werden auf den neuen Owner umgezogen).
- **`integrity_violation` ist Pflicht fuer ALLE neuen Guard-Block-Pfade, nicht nur Prompt-Integrity:** FK-68 §68.3.1 (`:447`) listet die Guard-Hooks **inkl. `SkillUsageCheck`** als `integrity_violation`-Emitter bei Blockade; FK-30 §30.7.3 (`:784`) schreibt Blockade-Details als `integrity_violation`. Also: `skill_usage_check`-Block -> `integrity_violation` (`guard="skill_usage_check"`), `WebCallBudgetGuard`-Block (Hard-Limit + unaufloesbar) -> `integrity_violation` (`guard="web_call_budget_guard"`). Das `stage`-Feld ist **nur** prompt_integrity-spezifisch (FK-61 §61.12.2 „fuer prompt_integrity_guard"; FK-68 §68.2) und gehoert **nicht** in die budget/skill-Events. Das `web_call`-Counter-Event bleibt observational beim Emitter (FK-30 §30.5.1a) — nicht mit dem `integrity_violation`-Audit verwechseln.
- **VERTRAG ZUERST MIGRIEREN, sonst sind die budget/skill-Events nicht baubar:** Heute pinnt `MANDATORY_PAYLOAD_FIELDS[EventType.INTEGRITY_VIOLATION] = ("stage",)` (`events.py:180`) `stage` **unbedingt**; `validate_event_payload` (`events.py:257`) wuerde jedes `stage`-lose `integrity_violation` fail-closed ablehnen und der Contract-Test (`test_event_catalog.py:114`/`:150`) friert das ein. Erst den Vertrag auf **bedingt** umstellen (`guard`/`detail` immer pflichtig; `stage` nur bei `guard="prompt_integrity_guard"`), `validate_event_payload` entsprechend bedingt machen und `_EXPECTED_MANDATORY_FIELDS` mitziehen — **dann** die budget/skill-Emitter bauen. Der **kanonische Owner** dieser Map ist **AG3-081** (EventType-Katalog + Mandatory-Payload-Contract); die Aenderung ist mit AG3-081 abzustimmen (depends_on gesetzt), **kein** zweiter Validator-/Map-Pfad. AG3-081 §2.1 listet die `integrity_violation`-`stage`-Bedingung heute nicht — nicht so tun, als liefere AG3-081 sie bereits; AG3-086 fuehrt sie producer-getrieben ein.
- **CCAG fail-open ist ein Sicherheits-Defekt** (`runtime.py:275-287`): Umstellung auf fail-closed nur mit Capability-Huelle ist Kern dieser Story — nicht „nebenbei" weiche Defaults lassen.
- **Prompt-Integrity ist modus-scharf** (FK-31 §31.7.1/§31.7.2): Stufe 1 immer, Stufe 2 leicht im Freestyle / voll in story_execution, Stufe 3 nur story_execution und **QA-Agents ausgenommen**. Den Spawn-Header **nicht neu erfinden** — der Vertrag steht in `resources/skill_bundles/execute-userstory-core/4.0.0/SKILL.md:57-62`/`:64-69`/`:151-153`; der Guard konsumiert dieses Format. `prompt_integrity` ist ein **neuer** HookIdentifier (Registrierung in `hook_registration.py` + Dispatch in `runner.py` nicht vergessen, sonst ist der Guard tot).
- Jede Prompt-Integrity-Blockade emittiert `integrity_violation` mit `stage` (FK-68 §68.2-Tabelle) — die Stufe steht im Telemetrie-Event, die Aussen-Meldung bleibt opak (§31.7.3).
- **TTL-Default:** Code hat `DEFAULT_TTL_SECONDS = 600` (`requests.py:42`), FK-93 §93.5a fordert 1800s ueber `permissions.request_ttl_s`. Den typisierten Config-Key mit Default 1800 einfuehren und konsumieren. Die breitere FK-93-Defaults-Reconciliation ist AG3-103 (doc-only) — hier nur diesen einen Wert FK-konform setzen.
- TTL->ESCALATED beruehrt den Run-Status-Pfad (PhaseState/Run-Lifecycle) — die Verdrahtung sauber an die autoritative Run-Status-Quelle, kein Schattenfeld.
- **LLM-Generalisierung erzeugt nur Vorschlaege:** `approved.yaml` erst nach explizitem Confirm/Promote schreiben (FK-42 §42.3.1 F-42-039 „ohne explizite Bestaetigung werden keine Regeln gespeichert", §42.4.2 „keine Dauerregel ohne separate Promote-Entscheidung"). Negativtest „keine Persistenz ohne Confirm" ist Pflicht. Fakes nur an der LLM-Grenze im Unit-Test (MOCKS-Ausnahme).
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Test-Namen (bedingter `integrity_violation`-Vertrag [skill/budget ohne `stage` gruen, prompt_integrity ohne `stage` fail-closed, Contract-Test gruen], Budget-Block + Migrations-Negativtest + „unaufloesbarer Story-Typ -> Block vom Governance-Owner", skill_usage Block/Allow + `integrity_violation`, Prompt-Integrity je Modus/Stufe, `integrity_violation`-Emission je Guard [prompt_integrity je `stage`, skill_usage, web_call_budget], CCAG-Huelle, TTL->ESCALATED, Regelgeneralisierung-Vorschlag + „keine Persistenz ohne Confirm").

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

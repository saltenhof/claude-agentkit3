# AG3-091: API/Event-Katalog-Endpunkte (Frontend-Read-Models + Execution-Limits-Read)

> **Scope-Label (read-layer only):** Diese Story baut ausschliesslich Frontend-Read-Models + das reine Caps-Read-Model `execution-input/limits`. Sie baut **nicht** die lebende Execution-Input-Doppel-Surface `snapshot|next` (Owner: AG3-100). Wo Metadaten/Index die alte Prosa „Execution-Input-Surface (`snapshot|next|limits`)" tragen, ist das durch diesen Schnitt ueberholt; autoritativ sind die Story-Files (AG3-091 §2.2/§6 + AG3-100 §2.1.3-5). Die Index-Zeile `_STORY_INDEX.md:116` ist Stale-Prosa (so von AG3-100 §1 `:25` festgehalten) und wird nicht von AG3-091 editiert (kein Fremd-Datei-Eingriff).

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `frontend`/BFF-Read-Layer (cross-cutting, FK-91/FK-72). Liefert die konkreten Lese-Payloads, die die Frontend-Sichten (AG3-093/094) und der Orchestrator-Skill konsumieren. Die **lebende Execution-Input-Doppel-Surface (snapshot/next) und ihr deterministischer Selektor sind NICHT Teil dieser Story** — sie gehoeren AG3-100 (`execution-planning`); AG3-091 konsumiert sie nur (siehe §2.2 / §6).
**Quell-Konzepte (autoritativ):**
- `FK-91 §91.1a` — Frontend-Read-Models als offizieller API-Katalog (projekt-skopiert `/v1/projects/{project_key}/...`); Bindung jeder Web-Schnittstelle an `formal.frontend-contracts.*` (FK-72 §72.14.3).
- `FK-70 §70.6.2` — `GET .../execution-input/limits` liefert die aktiven Caps als Read-Model (FK-91 §91.1a Katalogzeile `91_api_event_katalog.md:134`, gebunden an `frontend-contracts.entity.execution_limits`). Die Caps-Mutation (`PUT`) und die snapshot/next-Triage sind nicht hier.
- `FK-24 §24.3.3` — Mode-Lock-Ableitung; der Frontend-Read-Model-Wert `project_mode_lock` ist **story-derived** (`formal.frontend-contracts.invariant.mode_lock_derived`).
- `FK-73 §73.5` — `project_key` autoritativ aus `Project.key`; durchzieht alle BC-Tabellen.

---

## 1. Kontext / Ist-Zustand (belegt)

Die Topologie (AG3-090) traegt die Routing-Huelle. Ein Teil der Read-Model-Aggregate existiert backend-seitig bereits, aber **nur** im gebuendelten `project_detail`-Aggregat und **ohne** die projekt-skopierten Standalone-Endpunkte des Katalogs. Der Prototyp synthetisiert die Sichten heute clientseitig (Concept-as-Code, normativ).

- **Bestehendes Backend-Aggregat (story-derived, MUSS wiederverwendet werden, keine zweite Logik):** `GET /v1/projects/{project_key}` liefert bereits `mode_lock` und `story_counters` als Teil des `project_detail`-Read-Models — Route `src/agentkit/project_management/http/routes.py:162-174`, Aggregation `src/agentkit/project_management/service.py:109-117`, Wire-Modelle `src/agentkit/project_management/views.py:44-95`. Die Ableitung ist story-derived: `derive_mode_lock` (`service.py:124-153`) und `compute_story_counters` (`service.py:156-217`). Diese Logik ist die **Single Source of Truth**; die Standalone-Endpunkte `.../mode-lock` und `.../stories/counters` muessen daraus lesen/sie wiederverwenden, **nicht** eine zweite Berechnung aufbauen.
- **Mode-Lock-Wire-Shape (autoritativ, KEIN `holder_count`):** Das Frontend-Read-Model `frontend-contracts.entity.project_mode_lock` traegt **genau** `{project_key, mode}` mit `mode ∈ {standard, fast, idle}` (`concept/formal-spec/frontend-contracts/entities.md:93-110`) und ist per `frontend-contracts.invariant.mode_lock_derived` (`concept/formal-spec/frontend-contracts/invariants.md:108-122`) aus dem Story-Korpus abgeleitet. Das bestehende Wire-Modell `ProjectModeLock` (`views.py:44-56`) bildet das exakt ab. **Abgrenzung:** Das kanonische Persistenz-Objekt `project_mode_lock` der Control Plane (`src/agentkit/state_backend/store/mode_lock_repository.py:61-77`, `:199-213`) traegt `active_mode`/`holder_count` und ist der Fast/Standard-**Mutex** (Acquire/Release auf Story-Start/-Close) — ein **anderer fachlicher Belang** (FK-24 §24.3.3 Lock-Enforcement) und **nicht** die Read-Model-Wire-Shape. AG3-091 baut **kein** `holder_count` in das Read-Model ein und migriert **keine** Persistenz.
- **Mode-Lock im Prototyp ist story-derived:** `frontend/prototype/src/store/storySelectors.ts:280-286` `selectActiveProjectMode(stories)` -> `ProjectModeLock = Mode | null` (kein `holder_count`). Das deckt sich mit dem story-derived Read-Model; der Prototyp ist hier **UI-Zustandsbild**, fachliche Quelle ist die kanonische Ableitung (FK-24 §24.3.3 / Invariant `mode_lock_derived`).
- **Story-Counters im Prototyp:** `storySelectors.ts:43-63` `selectStoryCounters` -> `{ total, finished, running, ready, queue, blocked }`; `ready`/`blocked`-Klassifikation in `isReady`/`isBlocked`. Backend-Aequivalent liegt in `compute_story_counters` (`service.py:156-217`); Standalone-Endpunkt fehlt.
- **Story-Flow-Snapshot:** `storySelectors.ts:413` `selectStoryFlow(story)` baut Phasen+Substeps+Loop-Iterationen rein clientseitig aus Fixtures. FK-72 §72.6 verlangt, dass der `Ablauf`-Tab die kanonische `phase-state-projection` (FK-39) projiziert — das Read-Model muss daraus entstehen. Kein Backend-Payload vorhanden.
- **Coverage-Read-Models (formale Entitaet fehlt heute — AG3-091-eigene Spec-Ergaenzung):** FK-91 §91.1a listet `.../coverage/stories/{story_id}/acceptance` (Soll) und `.../coverage/stories/{story_id}/are-evidence` (Ist) (`concept/technical-design/91_api_event_katalog.md:127-128`). FK-40 §40.5b.6 (`40_are_integration_anforderungsvollstaendigkeit.md:448-452`) definiert die Lese-API-Quelle autoritativ: beide Endpunkte konsumieren **ausschliesslich `StoryAreLink` plus ARE-Live-Status** und mutieren die Tabelle nicht. **Wichtig:** `formal.frontend-contracts.entities` enthaelt heute **keine** Entitaet, die diese Coverage-Payload modelliert — `story_specification` (`entities.md:341-367`) traegt nur das Spezifikations-Tab-`acceptance` (Liste von Strings aus der Story-Spec), `story_evidence` (`entities.md:369-394`) traegt das QA-Zyklus-Evidence-Bundle; **keines** bildet `StoryAreLink` + ARE-Coverage-Status/Evidenz-Pfade ab. Da AG3-091 **selbst** diese beiden Coverage-Read-Endpunkte einfuehrt, ist die zugehoerige formale Coverage-Entitaet nach der FK-72-§72.14.3-Aenderungsregel (Endpoint + Entity entstehen im selben Cut) **Eigentum dieser Story** (nicht der `next`-Fall, der zur AG3-100-Surface gehoert). Kein Backend-Payload vorhanden.
- **Execution-Limits-Read-Model:** FK-91 §91.1a katalogisiert `GET /v1/projects/{project_key}/execution-input/limits` (`91_api_event_katalog.md:134`) gebunden an `frontend-contracts.entity.execution_limits` (`entities.md:728-753`). Kein Backend-Read-Payload vorhanden. Der korrespondierende `PUT` (Mutation) ist **nicht** Teil dieser Story (siehe Out-of-Scope).
- **Execution-Input-Doppel-Surface (snapshot/next) gehoert NICHT hierher:** FK-91 §91.1a listet `GET .../execution-input/snapshot` und `GET .../execution-input/next` (`91_api_event_katalog.md:114-115`, gebunden an `frontend-contracts.entity.execution_input_snapshot`/`execution_input_stack`, `entities.md:669-726`). FK-70 §70.8a.3 (`70_...:701-718`) verlangt **genau einen** deterministischen Selektor, der beide Auspraegungen speist; Doppel-Implementierung ist explizit unzulaessig. Owner dieser lebenden Doppel-Surface **und** des einen Selektors ist **AG3-100** (`_STORY_INDEX.md:136`, `stories/AG3-100-evaluate-scheduling-execution-input-surface/story.md` §2.1.3-5). AG3-091 baut diese Endpunkte und den Selektor **nicht** und liefert auch **keine** zweite Triage; es konsumiert die Surface als Read-Layer-Verbraucher (Abgrenzung §70.8a.5). Der Prototyp-Selektor `selectExecutionInput` (`storySelectors.ts:138-145`, `:190`) ist die normative funktionale Soll-Quelle **fuer AG3-100**, nicht fuer AG3-091.
- **Bestehende Planning-Routes (realer Ist-Zustand):** `src/agentkit/execution_planning/http/routes.py` matcht **projekt-skopiert** `/v1/projects/{project_key}/planning/dependency-graph`, `.../planning/dependencies`, `.../planning/next-ready`, `.../planning/config` (`routes.py:39-52`, `:125-157`). Es gibt **keine** Routen `ready-set`, `execution-plan`, `execution-input/snapshot` oder `execution-input/next` im Code — letztere beiden sind Owner-Surface von AG3-100; `ready-set`/`execution-plan` stammen aus der FK-70-§70.8a.5-Prosa (`70_...:733`) als Planungs-**Detailsichten** und existieren als Endpunkte heute **nicht**.
- **Project-Config-Lesepfad:** FK-73 §73.3 (`73_project_management.md:67-80`) katalogisiert **keinen** separaten `GET .../configuration`-Endpunkt; Project-Config wird ueber `GET /v1/projects/{project_key}` (project_detail) gelesen und ueber `PATCH /v1/projects/{project_key}` mutiert. Der `project_key` ist autoritativ aus `Project.key` (FK-73 §73.5, `73_project_management.md:93-99`). AG3-091 baut **keinen** neuen Config-Read-Endpunkt; relevant ist allein die `project_key`-Autoritaet fuer alle hier neuen projekt-skopierten Pfade.

## 2. Scope

### 2.1 In Scope
1. **Execution-Limits-Read-Model** (FK-70 §70.6.2; FK-91 §91.1a `91_api_event_katalog.md:134`):
   - `GET /v1/projects/{project_key}/execution-input/limits` -> `ExecutionLimits` (`frontend-contracts.entity.execution_limits`, `entities.md:728-753`) mit `project_key`, `repo_parallel_cap`, `merge_risk_cap`, `max_parallel_agent_cap`, `llm_pool_cap`, `ci_capacity_cap` (alle non-negative Integer; 0 = "Cap blockiert"). Read-only; die Mutation (`PUT`) ist Out-of-Scope (Command `update_execution_limits`). Dies ist ein **reines Read-Model**; es enthaelt **keine** Triage und speist den AG3-100-Selektor nicht (es liest nur die aktiven Caps).
2. **Frontend-Read-Models** (FK-91 §91.1a, projekt-skopiert), jeweils gebunden an die `formal.frontend-contracts.entities`-Entitaet. **`mode-lock` und `stories/counters` lesen aus dem bestehenden story-derived Aggregat** (`service.py:124-153`/`:156-217`); keine zweite Berechnung:
   - `GET /v1/projects/{project_key}/mode-lock` -> `ProjectModeLock` (`frontend-contracts.entity.project_mode_lock`, `entities.md:93-110`): `{project_key, mode}` mit `mode ∈ {standard, fast, idle}`, **story-derived** (`invariant.mode_lock_derived`, `invariants.md:108-122`) ueber `derive_mode_lock` (`service.py:124-153`). **Kein** `holder_count`, **keine** Persistenz-Migration (Abgrenzung siehe §1).
   - `GET /v1/projects/{project_key}/stories/counters` -> `StoryCounters` (`frontend-contracts.entity.story_counters`) mit der FK-/Prototyp-konformen Klassifikation (`ready`=Approved ohne Blocker/offene Dependency; `blocked`=Backlog oder Approved mit Blocker/offener Dependency) ueber `compute_story_counters` (`service.py:156-217`).
   - `GET /v1/projects/{project_key}/stories/{story_id}/flow` -> `StoryFlowSnapshot` (`frontend-contracts.entity.story_flow_snapshot`, `entities.md:554-651`; Phasen + Substeps + Loop-Iterationen), projiziert aus der kanonischen `phase-state-projection` (FK-39); Fast-Mode haelt Exploration als `skipped` ohne Substeps (FK-24 §24.3.3).
   - `GET /v1/projects/{project_key}/coverage/stories/{story_id}/acceptance` (Soll) und `.../coverage/stories/{story_id}/are-evidence` (Ist), read-only, Payload-Quelle autoritativ `StoryAreLink` + ARE-Live-Status (FK-40 §40.5b.6 `40_...:448-452`; Katalog FK-91 §91.1a `91_api_event_katalog.md:127-128`). **Hierfuer fehlt heute eine formale Entitaet** in `formal.frontend-contracts.entities` (`story_specification`/`story_evidence` modellieren diese Coverage-Payload nicht); AG3-091 **fuehrt die fehlende formale Coverage-Entitaet (Soll: adressierte Akzeptanzkriterien + verlinkte ARE-Anforderungen; Ist: verlinkte Anforderungen + Coverage-Status + Evidenz-Pfade) in `concept/formal-spec/frontend-contracts/entities.md` neu ein** und bindet beide Endpunkte per Contract-Test daran (FK-72 §72.14.3). Sie liest ausschliesslich aus dem von **AG3-077** (`depends_on`) geschriebenen `StoryAreLink` + ARE-Live-Status — keine zweite Coverage-Wahrheit, keine Tabellen-Mutation.
3. **Planning-Endpunkt-Abgrenzung** (FK-70 §70.8a.5): Die im Code vorhandene projekt-skopierte Planning-Detail-Surface (`/v1/projects/{project_key}/planning/next-ready|dependency-graph|dependencies|config`, `routes.py:39-52`) bleibt der Owner der Detail-Sichten und ist unberuehrt. AG3-091 legt hier **keine** neue Planning-Route an und dupliziert keine Triage. Die in der FK-70-§70.8a.5-Prosa genannten Detail-Namen `ready-set`/`execution-plan` werden **nicht** als neue Endpunkte angelegt; eine etwaige FK-Prosa-Angleichung an die realen Pfadnamen ist ein doc-only-Nachzug (siehe §6 Cross-Story-Voraussetzungen).
4. **Project-Config-Lesepfad-Abgleich** (FK-73 §73.5): `project_key` ist in allen neuen projekt-skopierten Pfaden autoritativ aus `Project.key`. Ein separater Project-Config-Read-Endpunkt wird **nicht** gebaut (FK-73 §73.3 katalogisiert keinen; Config-Read = `GET /v1/projects/{project_key}` project_detail). 404 fuer unbekannten `project_key` ist Pflicht (fail-closed).
5. **Vertragsbindung** (FK-72 §72.14.3): jeder neue Endpunkt dieser Story entsteht zusammen mit seinem Eintrag im API-Katalog (FK-91 §91.1a) und der zugehoerigen `formal.frontend-contracts.entities`-Entitaet (Contract-Test). Bestandsbindung (vorhandene Entitaeten, kein Spec-Eingriff noetig): `limits` -> `execution_limits` (`entities.md:728-753`), `mode-lock` -> `project_mode_lock` (`entities.md:93-110`), `stories/counters` -> `story_counters` (`entities.md:116`), `stories/{id}/flow` -> `story_flow_snapshot` (`entities.md:554-651`). **Eine** echte Spec-Luecke ist in diesem Cut zu schliessen: die beiden Coverage-Endpunkte haben heute **keine** formale Entitaet — AG3-091 fuehrt diese (Soll/Ist-Coverage aus `StoryAreLink` + ARE-Live-Status) selbst neu in `formal.frontend-contracts.entities` ein, weil es die Endpunkte einfuehrt (FK-72 §72.14.3, Endpoint + Entity im selben Cut). Es bleibt **kein** Endpunkt ohne formale Entitaet und **keine** still gefuellte Luecke. (Die fehlende `next`-Reason-Entitaet betrifft dagegen die AG3-100-Surface, nicht AG3-091; siehe §2.2 / §6.)

### 2.2 Out of Scope (mit Owner)
- **Execution-Input-Doppel-Surface (snapshot/next) + der eine deterministische Selektor + die `next`-Triage-Begruendung (Repo-Bucket, Critical-Path-Flag, Slots pro Cap, Tiebreaker) + die formale Reason-Entitaet** — **AG3-100** (`_STORY_INDEX.md:136`). AG3-100 ist Owner der lebenden snapshot/next-Surface und des einzigen Selektors (FK-70 §70.8a.3, Doppel-Implementierung unzulaessig). Die fehlende formale `next`-Reason-Entitaet ist in AG3-100 **explizit und buildbar** als dessen Eigentum gefuehrt: AG3-100 §1 (`stories/AG3-100-evaluate-scheduling-execution-input-surface/story.md:19`, „Formale `next`-Reason-Entitaet fehlt (FEHLT, §70.8a.2)"), §2.1.5 (`:34`, „AG3-100 fuehrt die zugehoerige **formale Reason-/`next`-Entitaet** in `concept/formal-spec/frontend-contracts/entities.md` neu ein") und AC5 (`:51`, Contract-Test gegen die von AG3-100 neu gefuehrte Entitaet, FK-72 §72.14.3). AG3-091 baut diese Endpunkte/den Selektor **nicht**, liefert keine `next`-Reason-Felder und greift nicht in die Formal-Spec fuer `next` ein. AG3-091 konsumiert die Surface als Read-Layer (Frontend/Skill), sobald AG3-100 sie liefert.
- **Routing-Huelle/Module/Middleware** (`control_plane_http`, BC-`http/`-Module) — **AG3-090** (`depends_on`). Hier werden die Module mit Inhalt gefuellt.
- **Mutationen** (`create_story`/`update_story_fields`/`approve_story`/`reject_story`/`cancel_story`/`update_execution_limits`, inkl. `PUT .../execution-input/limits`) — gehoeren zu den jeweiligen BC-Story-Stories; diese Story liefert die **Read-Models**. Vorhandene Mutationspfade nicht duplizieren.
- **`evaluate_scheduling`-Pflichtaufruf vor Story-Start + Planning-Domaenenmodell** (`BlockingCondition`/`PlanDerivation`/`ExecutionWave`) und die typisierten Cap-Werte — **AG3-098/099/100** (`depends_on: AG3-098`). AG3-091 liest nur die aktiven Caps (`limits`), nicht das Planning-Domaenenmodell und nicht den Start-Vertrag.
- **SSE-Events** (`planning`-Topic, `execution_input_changed`/`limits_changed`) — **AG3-094** (FK-91 §91.8; `formal.frontend-contracts.invariant.execution_input_changed_after_limits`).
- **KPI-Rollup-Payloads** (`/api/kpi/*`) — **AG3-084**.
- **`StoryAreLink`-Schreibwege + ARE-Andock-Punkte (`link_requirements`/`submit_evidence`/`check_gate`) + `are_bundle.json`** — **AG3-077** (`depends_on`). AG3-091 **liest** den von AG3-077 geschriebenen `StoryAreLink` + ARE-Live-Status fuer die Coverage-Read-Endpunkte (FK-40 §40.5b.6) und mutiert nichts; bei noch nicht gemergtem AG3-077 gegen dessen Schnitt arbeiten und die Abhaengigkeit melden.

## 3. Akzeptanzkriterien
1. `GET .../execution-input/limits` liefert `ExecutionLimits` mit `project_key` plus den fuenf Caps `repo_parallel_cap`/`merge_risk_cap`/`max_parallel_agent_cap`/`llm_pool_cap`/`ci_capacity_cap` als non-negative Integer (`execution_limits`-Wire-Shape, `entities.md:728-753`); 404 fuer unbekannten `project_key`; read-only (Mutation -> Methode nicht erlaubt) (Test).
2. `GET .../mode-lock` liest aus der story-derived Ableitung (`derive_mode_lock`, `invariant.mode_lock_derived`); Wire-Shape `{project_key, mode}` mit `mode ∈ {standard, fast, idle}`, **kein** `holder_count`; `idle`=keine In-Progress-Story (Test inkl. idle, standard, fast). Ein Test belegt: keine zweite Berechnung neben `service.derive_mode_lock`.
3. `GET .../stories/counters` klassifiziert `ready`/`blocked` exakt nach Regel (Approved-ohne-Blocker-ohne-offene-Dependency = ready; Backlog/blockiert = blocked) ueber `compute_story_counters` — Test mit gemischtem Korpus.
4. `GET .../stories/{story_id}/flow` projiziert aus `phase-state-projection` (FK-39); Fast-Mode -> Exploration `skipped` ohne Substeps (Test Standard + Fast).
5. `GET .../coverage/stories/{story_id}/acceptance` und `.../are-evidence` sind read-only, lesen ausschliesslich `StoryAreLink` + ARE-Live-Status (FK-40 §40.5b.6) und mutieren die Tabelle nicht (Test; Mutation -> Methode nicht erlaubt). AG3-091 fuehrt die fehlende formale Coverage-Entitaet in `formal.frontend-contracts.entities` neu ein und bindet beide Endpunkte per Contract-Test daran (FK-72 §72.14.3); ein Test belegt, dass keine zweite Coverage-Berechnung neben dem AG3-077-`StoryAreLink`-Pfad entsteht.
6. **Planning-/Execution-Input-Abgrenzung:** ein Test belegt, dass AG3-091 keine projekt-skopierte Planning-Route (`.../planning/next-ready` etc.) und **keine** Execution-Input-Surface (`.../execution-input/snapshot|next`) anlegt oder dupliziert; die Planning-Detail-Routen bleiben unveraendert erreichbar, und es entsteht **kein** zweiter Triage-Selektor (die snapshot/next-Surface bleibt Owner-Sache von AG3-100).
7. **Project-Config/`project_key`-Autoritaet:** alle neuen projekt-skopierten Endpunkte ziehen `project_key` aus `Project.key`; unbekannter `project_key` -> 404 fail-closed (Test ueber mindestens zwei der neuen Endpunkte).
8. Jeder neue Endpunkt dieser Story hat einen Contract-Test gegen `formal.frontend-contracts.entities` und einen Katalog-Eintrag-Bezug (FK-91 §91.1a); kein Endpunkt ohne formale Entitaet/Bindung (Negativ: fehlende Bindung schlaegt fehl). `limits`/`mode-lock`/`stories/counters`/`stories/{id}/flow` binden an **vorhandene** Entitaeten (`execution_limits`/`project_mode_lock`/`story_counters`/`story_flow_snapshot`); fuer die beiden Coverage-Endpunkte fuehrt AG3-091 die heute fehlende formale Coverage-Entitaet **explizit und owner-korrekt** neu ein (FK-72 §72.14.3, Endpoint + Entity im selben Cut) — es wird **keine** Spec-Luecke still gefuellt und **keine** fremde Owner-Surface beruehrt (die `next`-Reason-Entitaet bleibt AG3-100).
9. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage >= 85 %.

## 4. Definition of Done
- AK 1–9 erfuellt; giftige Codex-Review PASS; (Implementierung/Commit erst nach Execution-Plan-Freigabe — diese Story wird zunaechst nur autorisiert/reviewt).

## 5. Guardrail-Referenzen
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** `mode-lock`/`counters` lesen aus dem bestehenden story-derived Aggregat (`service.derive_mode_lock`/`compute_story_counters`), keine zweite Berechnung; Mode-Lock-Read-Model traegt die formale Wire-Shape (kein `holder_count`, keine Vermischung mit dem Control-Plane-Mutex `mode_lock_repository`). Die snapshot/next-Triage hat **genau einen** Owner-Selektor — bei AG3-100, nicht hier; AG3-091 erzeugt keinen zweiten. Read-Models entstehen aus den kanonischen Projektionen (FK-39/FK-40), nicht aus Schattenrechnungen.
- **FAIL CLOSED:** leere Surface = 200-Leerantwort (definiert), nicht 404; unbekannter `project_key` = 404; fehlende Quelle = strukturierter Fehler, kein erfundener Wert.
- **TYPISIERT STATT STRINGS:** alle Read-Models als Pydantic-Modelle gebunden an `formal.frontend-contracts.entities` (snake_case Wire-Feldnamen).
- **CONTRACT-/GOLDEN-DISZIPLIN:** jede Schnittstellenaenderung zieht FK-91 §91.1a + Formal-Spec + Contract-Test mit (FK-72 §72.14.3). AG3-091 fuehrt **genau eine** Formal-Spec-Ergaenzung durch — die heute fehlende Coverage-Entitaet fuer die beiden von AG3-091 selbst eingefuehrten Coverage-Endpunkte (Owner-korrekt, da AG3-091 die Endpunkte einfuehrt). Die `next`-Reason-Entitaet ist dagegen **AG3-100**-Owner-Sache; AG3-091 greift dort nicht in die Formal-Spec ein.
- **ARCH-55:** Endpunkt-Pfade, Wire-Feldnamen (`global_slots_left`, `eligible_ready`, `total_ready`, `mode`, `repo_parallel_cap`, `merge_risk_cap`, `max_parallel_agent_cap`, `llm_pool_cap`, `ci_capacity_cap` …), `error_code` ausschliesslich englisch. Prototyp-CamelCase ist UI-seitig und wird **nicht** als Wire-Shape uebernommen; die Backend-Wire-Feldnamen binden an die formalen snake_case-Felder (`entities.md:685`/`:694`/`:699`/`:734-751`).

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- **Klarer Schnitt gegen AG3-100:** Die lebende Execution-Input-Doppel-Surface (`execution-input/snapshot|next`) und ihr **einziger** deterministischer Selektor (FK-70 §70.8a.3) gehoeren AG3-100 (`stories/AG3-100-evaluate-scheduling-execution-input-surface/story.md` §2.1.3-5). AG3-091 baut sie **nicht** und liefert **keine** zweite Triage. Falls beim Implementieren der Eindruck entsteht, AG3-091 muesse snapshot/next bauen: stoppen und melden — das ist ein Schnittfehler, kein Implementierungsdetail.
- Normative funktionale Soll-Quelle (Concept-as-Code) **fuer AG3-091**: `frontend/prototype/src/store/storySelectors.ts` — `selectActiveProjectMode` (`:280`), `selectStoryCounters` (`:43`), `selectStoryFlow` (`:413`). Die Counter-/Flow-/Mode-Semantik ist 1:1 zu uebernehmen, aber backend-seitig im Read-Layer — nicht den TS-Code portieren, sondern die Semantik typisiert in Python neu bauen. Der Triage-Selektor `selectExecutionInput` (`:190`) ist Soll-Quelle **fuer AG3-100**, nicht fuer AG3-091. **Mode-Lock:** der Prototyp-Selektor (`:280-286`, `Mode | null`) ist nur das **UI-Zustandsbild**; fachliche Quelle ist die kanonische story-derived Ableitung (`service.derive_mode_lock`, FK-24 §24.3.3 / `invariant.mode_lock_derived`) — **kein** `holder_count`, **keine** Bindung an das Control-Plane-Mutex-Repository (`mode_lock_repository.py`).
- Wiederverwendung (kein Doppel-Code): `src/agentkit/project_management/service.py:124-153` (`derive_mode_lock`) und `:156-217` (`compute_story_counters`) sind die autoritative Berechnung hinter `GET /v1/projects/{project_key}`. Die Standalone-Endpunkte `.../mode-lock` und `.../stories/counters` rufen dieselbe Logik auf (ggf. Service-Methode extrahieren/wiederverwenden), nicht neu implementieren. Wire-Modelle: `src/agentkit/project_management/views.py:44-95`.
- Execution-Limits-Heimat: Das `limits`-Read-Model liest die aktiven Caps. Es liegt im Read-Layer und darf **keine** Triage/Selektor-Logik enthalten — es ist eine reine Caps-Projektion (`execution_limits`-Entity). Die Caps-Quelle (Config/Budget-Modell) liefert AG3-098/099; bei noch nicht gemergten Vorgaengern gegen deren Schnitt arbeiten und Abhaengigkeit melden.
- **Coverage-Read-Models (eigene Formal-Spec-Ergaenzung):** Die beiden Coverage-Endpunkte haben heute **keine** formale Entitaet. AG3-091 fuehrt sie als Owner ihrer Endpunkte selbst neu in `concept/formal-spec/frontend-contracts/entities.md` ein (Soll: adressierte Akzeptanzkriterien + verlinkte ARE-Anforderungen; Ist: verlinkte Anforderungen + Coverage-Status + Evidenz-Pfade) und bindet beide per Contract-Test (FK-72 §72.14.3). Datenquelle ist **ausschliesslich** der von **AG3-077** geschriebene `StoryAreLink` + ARE-Live-Status (FK-40 §40.5b.6 `40_...:448-452`) — read-only, keine Tabellen-Mutation, keine zweite Coverage-Wahrheit. Bei noch nicht gemergtem AG3-077 gegen dessen `StoryAreLink`-Schnitt arbeiten und die Abhaengigkeit melden.
- Bestehende Planning-Routes (`src/agentkit/execution_planning/http/routes.py:39-52`, `:125-157`) bleiben **unveraendert** — AG3-091 fasst sie nicht an.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Formal-Spec nur fuer die eigene Coverage-Entitaet** ergaenzen (FK-72 §72.14.3); die `next`-Reason-Entitaet bleibt AG3-100-Owner-Sache und wird **nicht** angefasst. FK-Technical-Design-/Domain-Concept-Prosa NICHT veraendern. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff, gruene Pflichtbefehle, Endpoint-Tests inkl. Empty-State-, 404- und Read-only-Tests sowie der Abgrenzungstest (AK6), dass weder snapshot/next noch ein zweiter Selektor entsteht.

### Cross-Story-Voraussetzungen (echte Abhaengigkeiten an andere Owner)
1. **Execution-Input-Doppel-Surface (snapshot/next) + Selektor + formale Reason-Entitaet** — **AG3-100** (Owner, `_STORY_INDEX.md:136`; FK-70 §70.8a.3 verlangt einen einzigen Selektor; FK-72 §72.14.3 verlangt fuer den `next`-Endpoint eine Entity/Command). Die formale Reason-Entitaet (Repo-Bucket, Critical-Path-Flag, Slots-pro-Cap, Tiebreaker) fehlt heute in `formal.frontend-contracts.entities` (nur `execution_input_snapshot`/`execution_input_stack`/`execution_limits`, `entities.md:669-753`) und ist von AG3-100 zu fuehren — **buildbar belegt**: AG3-100 fuehrt sie explizit als eigenen Scope (`stories/AG3-100-evaluate-scheduling-execution-input-surface/story.md:19` Ist-Zustand-Gap, `:34` §2.1.5 In-Scope „neu ein", `:51` AC5 Contract-Test). **AG3-091 darf die Formal-Spec nicht selbst aendern** und baut die Surface nicht. (Status `depends_on: AG3-100`.)
2. **`StoryAreLink`-Schreibwege + ARE-Live-Status (Coverage-Datenquelle)** — **AG3-077** (Owner, `_STORY_INDEX.md:78`; vier ARE-Andock-Punkte + `StoryAreLink`-INSERT/UPDATE). AG3-091 liest diese Quelle fuer die Coverage-Read-Endpunkte (FK-40 §40.5b.6) und mutiert sie nicht. Die zugehoerige **formale Coverage-Entitaet** fuehrt **AG3-091 selbst** ein (es besitzt die Coverage-Read-Endpunkte; FK-72 §72.14.3) — dies ist **kein** AG3-077-Owner-Fall, sondern der eine bewusste Formal-Spec-Zuwachs dieser Story. (Status `depends_on: AG3-077`.)
3. **Doc-only-Nachzug FK-70 §70.8a.5** — die Prosa nennt `/v1/planning/ready-set` und `/v1/planning/execution-plan` (`70_story_planung_abhaengigkeiten_ausfuehrungsplanung.md:733`), waehrend der reale Code projekt-skopierte `.../planning/next-ready` etc. fuehrt. Falls die FK-Prosa an die realen Pfadnamen anzugleichen ist, ist das ein konzeptioneller doc-only-Nachzug (Welle-10-Klasse) ausserhalb des AG3-091-Code-Cuts.

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

# AG3-066: Review-Paar-Divergenz + Quorum/Tiebreaker (`normalize_verdict`)

**Typ:** Implementation
**Groesse:** M
**Bounded Context:** `verify-system` / Telemetrie & LLM-Bewertungen (BC3) — der Quorum-/Tiebreaker-Mechanismus, der das abgeloeste qualitative LOW/MEDIUM/HIGH-Scoring durch ein verdict-basiertes Mehrheitsverfahren ersetzt.
**Quell-Konzepte (autoritativ):**
- `FK-34 §34.8` (FK-34-130..132) — Modul `agentkit/telemetry/divergence.py` mit `normalize_verdict()` (`VERDICT_NORMALIZATION`: PASS/CONCERN/FAIL), `check_divergence()`, `@dataclass(frozen=True) ReviewPairDivergence` (`reviewer_a`/`reviewer_b`/`verdict_a`/`verdict_b`/`divergent`/`quorum_triggered`/`final_verdict`); bei Divergenz dritter Reviewer (Tiebreaker), Mehrheit 2-gegen-1; das Quorum **ersetzt** das fruehere qualitative Scoring (LOW/MEDIUM/HIGH).
  - Verdikt-Normalisierung: `concept/technical-design/34_llm_bewertungen_adversarial_testing_runtime.md:496-514` (`VERDICT_NORMALIZATION`, `normalize_verdict`).
  - Dataclass-Felder inkl. `divergent`: `concept/.../34_llm...md:531-540`.
  - `check_divergence` (zwei normalisierte Verdikte != → divergent): `concept/.../34_llm...md:543-552`.
  - Quorum-Ablauf (QA-Agent steuert; Pipeline delegiert): `concept/.../34_llm...md:555-568`.
  - Event-Felder (`story_id`/`reviewer_a`/`reviewer_b`/`divergent`/`quorum_triggered`/`final_verdict`): `concept/.../34_llm...md:575-582`.
  - Nicht-Divergenz-Fall emittiert ebenfalls ein Event (`quorum_triggered=false`): `concept/.../34_llm...md:589`.
- **FK-68** (Telemetrie/Eventing/Telemetry-Hooks-Owner, `authority_over: eventing/telemetry-hooks`, `concept/technical-design/68_telemetrie_eventing_workflow_metriken.md:9-13`) — `review_divergence` ist ein Telemetrie-Event und faellt damit unter die Schema-Hoheit von FK-68. Die **Payload-Feldtabelle** unter der H4-Ueberschrift „Review-Divergenz" (in `### 68.2.2 Event-Katalog`) deklariert das Event aktuell noch mit den **abgeloesten** Feldern `reviewer_a`, `reviewer_b`, `score` (LOW/MEDIUM/HIGH), `routing` (`concept/.../68_telemetrie...md:358-362`). FK-34 §34.8.4 ist jedoch die **autoritative Feldsatz-Quelle** fuer dieses Event (`concept/.../34_llm...md:570-582`) und verweist fuer das Schreiben des Events explizit auf „Kap. 68" (`:573`), schreibt aber den neuen Feldsatz vor → **FK-uebergreifender Widerspruch** (siehe §1 Konfliktcheck). **Aufloesung (FK-34 autoritativ):** Diese Code-Story zieht das **Code-Schema** FK-34-konform nach (in Scope, §2.1.4). Die FK-68-Payload-**Prosa**-Zeile ist eine `concept/`-Aenderung und damit **out-of-scope** dieser Code-Story; sie wird als **Owner-Scope-Erweiterung an AG3-103 geroutet** (FK-68 §68.2-Owner, doc-only, `var/concept-gap-analysis/_STORY_INDEX.md:144`) — siehe §2.2. **Wichtig:** AG3-103 fuehrt die §68.2.2-Payload-Feldzeile inzwischen als Owner-Scope (`AG3-103/story.md:13`, `:36`, `:52`, `:63`); damit ist das AG3-066-Routing der FK-68-§68.2.2-Payload-Zeile gueltig und auf einen real getragenen Owner-Scope gerichtet. Ein **harter `depends_on`-Zwang** auf AG3-103 wurde **entfernt** (war eine fabrizierte Ordnungs-Garantie; FIX THE MODEL): die Code-Migration ist FK-34-autoritativ und nicht durch die FK-68-Prosa-Reihenfolge blockiert.

---

## 1. Kontext / Ist-Zustand (belegt)

Der Code haelt das ausdruecklich **ersetzte** LOW/HIGH-Scoring und hat keinen Quorum-Pfad. Belegt (gap-fk-26-35.md §FK-34, `var/concept-gap-analysis/gap-fk-26-35.md:404-429`):

- Implementiert ist nur `src/agentkit/telemetry/hooks/divergence_hook.py` (`DivergenceHook`), das ein `review_divergence`-Event mit den **abgeloesten** Score-Buckets `LOW`/`HIGH` emittiert: `_SCORE_LOW`/`_SCORE_HIGH` in `divergence_hook.py:30-31`, Payload `score=_SCORE_HIGH`, `routing="third_reviewer"` in `divergence_hook.py:93-94`.
- Der Docstring deklariert den dritten Reviewer explizit als „THEME-009 (verify-system.A9) and out of scope" (`divergence_hook.py:8-11`).
- Es gibt **kein** Modul `src/agentkit/telemetry/divergence.py`, keine `normalize_verdict()`/`check_divergence()`-Funktion, keine `ReviewPairDivergence`-Dataclass, keinen Quorum-/Tiebreaker-/`final_verdict`-Pfad (Grep `normalize_verdict|check_divergence|ReviewPairDivergence|quorum` → keine Implementierung).
- Der Hook nutzt `_PASS_VERDICTS = frozenset({"PASS","pass","APPROVE","approve"})` (`divergence_hook.py:34`) und `_find_diverging_pair` (Pass-vs-nicht-Pass, `divergence_hook.py:147-166`) — eine **binaere** Divergenz, nicht die FK-34-§34.8-Dreiwert-Normalisierung (PASS/CONCERN/FAIL).
- Der `EventType.REVIEW_DIVERGENCE` ist in `src/agentkit/telemetry/events.py:64` (`= "review_divergence"`) definiert; das Payload-Schema wird heute vom Hook gesetzt. `REVIEW_DIVERGENCE` ist **nicht** in `MANDATORY_PAYLOAD_FIELDS` (`events.py:173-221`) und auch nicht in dessen Contract-Pin (`tests/contract/telemetry/test_event_catalog.py:112-141`) gelistet — das Feldschema ist heute also nicht fail-closed erzwungen.
- **Realer Telemetrie-Consumer** `EventNormalizer` (`src/agentkit/telemetry/risk_window/normalizer.py`) kategorisiert `REVIEW_DIVERGENCE` als `INTEGRITY` (`:40`) und kopiert die **alten** Payload-Schluessel in den Audit-Excerpt: `_EXCERPT_KEYS` enthaelt `"score"`, `"reviewer_a"`, `"reviewer_b"` (`:56-67`), aber **kein** `divergent`/`quorum_triggered`/`final_verdict`. Der Excerpt traegt also nach der Schema-Abloesung den toten `score`-Key und verliert die neuen FK-34-Felder, wenn er nicht mitgezogen wird.

**Konfliktcheck (Kontext-Sinnhaftigkeit + FK-34/FK-68-Widerspruch):** `DivergenceHook` ist der vorhandene Emitter und liest `review_response`-Events (`reviewer_role`/`verdict`/`review_round`, `divergence_hook.py:107-140`). Diese Story baut das **fehlende** `telemetry/divergence.py`-Kernmodul (`normalize_verdict`/`check_divergence`/`ReviewPairDivergence`) als **reine, seiteneffektfreie Logik** und verdrahtet den Hook darauf um, sodass das LOW/HIGH-Scoring real abgeloest wird (nicht parallel weitergefuehrt).

Der `review_divergence`-Event ist ein **Telemetrie-Event** und faellt damit unter die Schema-Hoheit von **FK-68** (`authority_over: eventing/telemetry-hooks`, `concept/.../68_telemetrie...md:9-13`). Es liegt ein **FK-uebergreifender Widerspruch** vor: FK-34 §34.8.4 ist die **autoritative Feldsatz-Quelle** und schreibt `story_id`/`reviewer_a`/`reviewer_b`/`divergent`/`quorum_triggered`/`final_verdict` vor (`:570-582`); FK-34 §34.8.4 schreibt das Event und referenziert dabei „Kap. 68" (`:573`), waehrend die FK-68-§68.2.2-Payload-Feldtabelle unter `#### Review-Divergenz` (in `### 68.2.2 Event-Katalog`) dasselbe Event **noch** mit `reviewer_a`/`reviewer_b`/`score (LOW/MEDIUM/HIGH)`/`routing` deklariert (`:358-362`) — sie ist die stale Telemetrie-Owner-Zeile. Aufloesung **entlang der Hoheiten** (FIX THE MODEL): FK-34 §34.8.4 verweist fuer das Event auf „Kap. 68" (`:573`) und definiert den autoritativen Feldsatz (`:575-582`); die FK-68-§68.2.2-Payload-Zeile ist die **stale Telemetrie-Owner-Zeile**, die noch `score`/`routing` traegt (`:358-362`) und per Owner-Scope-Erweiterung anzugleichen ist (AG3-103). Damit ist die Code-Migration FK-34-autoritativ und nicht durch die FK-68-Prosa blockiert; die stale FK-68-Prosa-Zeile ist eine `concept/`-Aenderung **out-of-scope** dieser Code-Story und wird **sichtbar an ihren Owner geroutet** (nicht als „Stale-Eintrag" liegengelassen, aber auch nicht ueber eine fabrizierte Abhaengigkeit kaschiert).
- **FK-68-Prosa-Angleichung (out-of-scope, an Owner geroutet):** Die FK-68-Payload-Zeile fuer `review_divergence` (`:358-362`, noch `score`/`routing`) ist eine `concept/`-Aenderung und wird **nicht** in dieser Code-Story angefasst. Sie gehoert strukturell der doc-only-Nachzug-Story **AG3-103**, die FK-68 **§68.2** als Owner-Scope fuehrt (`var/concept-gap-analysis/_STORY_INDEX.md:144`); die `review_divergence`-Payload-Zeile sitzt in **§68.2.2 (Event-Katalog)** ⊂ **§68.2** und damit innerhalb dieses Owner-Cuts. **Owner-Scope getragen:** AG3-103 fuehrt die §68.2.2-Payload-Feldzeile `review_divergence` inzwischen explizit in seinen Quell-Konzepten/In-Scope/ACs (`AG3-103/story.md:13`, `:36`, `:52`, `:63`) — neben der §68.2-Glossar-`event-type-id`-value-Liste (`:12`, `:51`); value-Liste (welche Event-Typen existieren) und Payload-Feldtabelle (welche Felder ein Event traegt) sind getrennte Dimensionen derselben §68.2-Hoheit, und AG3-103 traegt beide. AG3-066 aendert AG3-103s Scope **nicht** aus dieser Story heraus (Rollentrennung / fremde Story-Datei); das Routing der §68.2.2-Payload-Feldzeile `review_divergence` (`score`/`routing` → FK-34-Feldsatz `:570-582`) zeigt damit auf einen real getragenen Owner-Scope und ist gueltig. Ein **harter `depends_on`-Zwang** auf AG3-103 wurde **entfernt**: er garantierte eine Ordnung, machte den Konzept-Stand aber nicht konsistent — er versteckte das Routing nur hinter einem Pfeil. Die Konsistenz ist stattdessen durch die FK-34-Hoheit + sichtbares Routing auf den AG3-103-Owner-Scope gewahrt.
- **Code-Migration (in Scope dieser Story):** AG3-066 besitzt laut Index-Cut die Telemetrie-Schema-Abloesung. Daher wird das **eine kanonische** `review_divergence`-Payload **end-to-end** auf den FK-34-Feldsatz umgestellt: Hook-Payload, `MANDATORY_PAYLOAD_FIELDS` (`events.py`), der Contract-Pin (`test_event_catalog.py`) und die `_EXCERPT_KEYS` des `EventNormalizer` (`normalizer.py`). Kein zweites/paralleles Divergenz-Format.

Das ist eine bewusste Telemetrie-Schema-Aenderung (Contract-/Golden-Tests mitziehen, CLAUDE.md „Telemetrie-Formate nur mit Contract-Tests aendern").

**Owner-Abgrenzung des Tiebreaker-LLM-Calls (FK-34 §34.8.3, `:555-568`):** Laut FK steuert der **QA-Agent (Worker)** das Quorum eigenstaendig und ruft Reviewer C; die Pipeline (Orchestrator) delegiert die Quorum-Auslosung vollstaendig an den QA-Agenten. Der eigentliche Reviewer-C-LLM-Call gehoert damit **nicht** in dieses Story-Cut (Index `var/concept-gap-analysis/_STORY_INDEX.md:57`: Cut = `telemetry/divergence.py` + `normalize_verdict`/`check_divergence`/`ReviewPairDivergence` + Scoring-Abloesung; Index-`depends_on: AG3-037, AG3-043`). Diese Story haelt sich an genau diesen Index-`depends_on`; die FK-68-Prosa-Angleichung wird **nicht** ueber eine zusaetzliche harte Abhaengigkeit erzwungen, sondern als Owner-Scope-Erweiterung an AG3-103 geroutet (§1/§2.2). Diese Story liefert deshalb:
- die **reine Quorum-Logik** (`normalize_verdict`/`check_divergence`/`apply_quorum`), die aus drei vorliegenden Verdikten deterministisch das Mehrheits-`final_verdict` ableitet, und
- die **Fakten-Emission** des `DivergenceHook` (kein LLM-Call im Hook).

Der QA-/Layer-2-Aufruf von Reviewer C (LLM) ist out-of-scope und an seinen Owner geroutet (siehe §2.2).

## 2. Scope

### 2.1 In Scope
1. **Neues Modul `agentkit/telemetry/divergence.py`** (FK-34 §34.8.2/§34.8.3), reine Logik ohne I/O/LLM:
   - `VERDICT_NORMALIZATION: dict[str, str]` (`PASS`→`PASS`, `PASS_WITH_CONCERNS`→`CONCERN`, `REWORK`→`FAIL`, `FAIL`→`FAIL`; exakt FK-34 §34.8.2, `:505-510`) + `normalize_verdict(raw_verdict: str) -> str`. **Unbekannter Wert → Passthrough** (`VERDICT_NORMALIZATION.get(raw_verdict, raw_verdict)`), exakt nach FK-34 §34.8.2 (`:512-514`). Englische Keys/Werte (ARCH-55).
   - `check_divergence(verdict_a: str, verdict_b: str) -> bool` — `True` gdw. zwei **normalisierte** Verdikte ungleich sind (FK-34 §34.8.3, `:543-552`). Vergleich auf den Dreiwerten, nicht binaer Pass/nicht-Pass.
   - `@dataclass(frozen=True) ReviewPairDivergence` mit **exakt** den FK-Feldern (`:531-540`): `reviewer_a: str`, `reviewer_b: str`, `verdict_a: str`, `verdict_b: str`, `divergent: bool`, `quorum_triggered: bool`, `final_verdict: str | None`.
   - `apply_quorum(verdict_a: str, verdict_b: str, verdict_c: str) -> str` — leitet aus drei normalisierten Verdikten das Mehrheits-`final_verdict` ab (2-gegen-1). **No-majority-Regel (siehe AC4):** drei paarweise verschiedene Verdikte (`PASS`/`CONCERN`/`FAIL`) → fail-closed strengstes Verdikt nach Ordnung `PASS < CONCERN < FAIL`.
2. **Quorum/Tiebreaker-Logik** (FK-34 §34.8.3): bei Divergenz wird ein dritter Verdikt (Tiebreaker) erwartet; `apply_quorum` bestimmt `final_verdict`, `ReviewPairDivergence.quorum_triggered=True`. Die **Beschaffung** des dritten Verdikts (Reviewer-C-LLM-Call) ist out-of-scope (§2.2); dieses Modul nimmt die drei Verdikte entgegen und entscheidet deterministisch.
3. **LOW/MEDIUM/HIGH-Scoring abloesen**: `DivergenceHook` (`telemetry/hooks/divergence_hook.py`) wird auf `normalize_verdict`/`check_divergence` umgestellt; `_SCORE_LOW`/`_SCORE_HIGH` (`:30-31`), die binaere `_PASS_VERDICTS`-Logik (`:34`, `:143-144`) und `_find_diverging_pair` (`:147-166`) entfallen bzw. werden durch die Dreiwert-Logik ersetzt. Das `review_divergence`-Event traegt `divergent`/`quorum_triggered`/`final_verdict` statt `score`/`routing`. Der Hook macht **keinen** LLM-Call (Fakten-Emission only).
4. **Telemetrie-Schema-Mitzug end-to-end (FK-68-Eventing-Hoheit)**: das `review_divergence`-Event-Payload wird auf die FK-34-§34.8.4-Felder (`:575-582`) umgestellt (`story_id`, `reviewer_a`, `reviewer_b`, `divergent`, `quorum_triggered`, `final_verdict`); `EventType.REVIEW_DIVERGENCE` (`events.py:64`) bleibt der eine Typ. Damit das **eine kanonische** Format ueberall greift, werden **alle realen Consumer/Owner** des Schemas mitgezogen:
   - **Pflicht-Feldsatz fail-closed pinnen:** Eintrag `EventType.REVIEW_DIVERGENCE: ("story_id", "reviewer_a", "reviewer_b", "divergent", "quorum_triggered", "final_verdict")` in `MANDATORY_PAYLOAD_FIELDS` (`src/agentkit/telemetry/events.py:173-221`) — heute fehlt das Event dort, das Schema ist also nicht fail-closed erzwungen.
   - **Contract-Pin nachziehen:** denselben Eintrag in `_EXPECTED_MANDATORY_FIELDS` (`tests/contract/telemetry/test_event_catalog.py:112-141`) ergaenzen, sonst schlaegt `test_mandatory_payload_fields_match_contract` (`:149-150`) an.
   - **Risk-Window-Excerpt nachziehen:** `_EXCERPT_KEYS` des `EventNormalizer` (`src/agentkit/telemetry/risk_window/normalizer.py:56-67`) — den toten `"score"`-Key durch die neuen `divergent`/`quorum_triggered`/`final_verdict` ersetzen, damit der INTEGRITY-Audit-Excerpt das FK-34-Format traegt statt des abgeloesten `score`. Der zugehoerige Normalizer-Test (`tests/unit/telemetry/risk_window/test_normalizer.py`) wird konsistent mitgezogen.
   - Kein zweites Divergenz-Format (FIX THE MODEL / SINGLE SOURCE OF TRUTH).
5. **Nicht-Divergenz-Event (FK-34 §34.8.4, `:589`)**: bei `divergent=False` emittiert der Hook **dennoch** ein `review_divergence`-Event mit `divergent=False`, `quorum_triggered=False`, `final_verdict=null` (FK-Mermaid: Zweig `divergent=False` → `review_divergence Event (quorum_triggered=false)`). Das frueher „kein Event"-Verhalten wird an das zitierte FK ausgerichtet.
6. **Negativpfade** als Tests: uebereinstimmende Verdikte → kein Quorum, Event mit `quorum_triggered=False`; divergierendes Paar + dritter Verdikt → `final_verdict` per Mehrheit; drei verschiedene Verdikte → fail-closed strengstes Verdikt; unbekanntes Rohverdikt → Passthrough-Normalisierung.

### 2.2 Out of Scope (mit Owner)
- **Reviewer-C-LLM-Call / QA-Quorum-Orchestrierung** (Beschaffung des dritten Verdikts) — gehoert laut FK-34 §34.8.3 (`:566-568`) dem **QA-Agenten/Layer-2-Pfad** (Layer-2-Maschinerie AG3-037/AG3-043; der konkrete Verify-LLM-Transport ist **AG3-065**, FK-11). Diese Story konsumiert **keinen** LLM-Transport und fuegt **keine** AG3-065-Abhaengigkeit hinzu; sie liefert nur die reine Quorum-Logik, die der QA-Pfad mit drei Verdikten aufruft. (Daher wird **keine** AG3-065-Abhaengigkeit aufgenommen. `depends_on` bleibt der Index-Cut `AG3-037, AG3-043` (`_STORY_INDEX.md:57`); es wird **keine** zusaetzliche harte Abhaengigkeit aufgenommen — die FK-68-Prosa-Angleichung ist als Owner-Scope-Erweiterung an AG3-103 geroutet, nicht als Ordering-Zwang, siehe §1/§2.2.)
- **Parallel-Reviewer-Runtime / `StructuredEvaluator`-Aenderungen** — bestehende Layer-2-Maschinerie (AG3-037/AG3-043); hier nur die Divergenz-/Quorum-Schicht darueber.
- **Governance-Risk-Window/Adjudication** — AG3-085 (FK-35); `risk_window`-Telemetrie ist davon getrennt.
- **FK-68-Prosa-Angleichung (`review_divergence`-Payload-Feldtabelle, §68.2.2) → ROUTING an AG3-103 (Owner-Scope-Erweiterung):** Die FK-68-Payload-Feldzeile unter `#### Review-Divergenz` deklariert das Event noch mit `score (LOW/MEDIUM/HIGH)`/`routing` (`concept/.../68_telemetrie...md:358-362`), FK-34 §34.8.4 ist die autoritative Feldsatz-Quelle (`:570-582`). Diese Code-Story zieht das **Code-Schema** FK-34-konform nach (§2.1.4), faesst aber **keine** `concept/`-Datei an. Die Angleichung der FK-68-Prosa-Zeile ist doc-only und gehoert strukturell **AG3-103** (`var/concept-gap-analysis/_STORY_INDEX.md:144` — fuehrt FK-68 §68.2 explizit als Owner-Scope; die §68.2.2-Payload-Zeile sitzt in §68.2.2 ⊂ §68.2). **Routing gueltig (Owner-Scope getragen):** AG3-103 fuehrt die **§68.2.2-Payload-Feldzeile** `review_divergence` inzwischen explizit in Quell-Konzepten/In-Scope/AC (`AG3-103/story.md:13`, `:36`, `:52`, `:63`) — neben der §68.2-Glossar-`event-type-id`-value-Liste (`:12`, `:51`); value-Liste (welche Event-Typen existieren) und Payload-Feldtabelle (welche Felder ein Event traegt) sind getrennte Dimensionen derselben §68.2-Hoheit, und AG3-103 traegt beide. AG3-103 gleicht die §68.2.2-Payload-Feldzeile `review_divergence` (`:358-362`) von `score`/`routing` auf den FK-34-§34.8.4-Feldsatz (`story_id`/`reviewer_a`/`reviewer_b`/`divergent`/`quorum_triggered`/`final_verdict`, `:570-582`) an. AG3-066s Routing zeigt damit auf einen real getragenen Owner-Scope und ist gueltig. **Keine harte `depends_on`-Erzwingung:** AG3-066 aendert AG3-103s Scope nicht aus dieser fremden Story-Datei heraus; eine Ordering-Dependency wuerde nur eine Reihenfolge vortaeuschen und das sichtbare Routing hinter einem Pfeil verstecken. Die Konzept-Konsistenz ist durch die FK-34-Hoheit (FK-34 §34.8.4 schreibt das Event, referenziert „Kap. 68", `:573`, und definiert den Feldsatz, `:575-582`) + dieses sichtbare Routing auf den AG3-103-Owner-Scope gewahrt.
- **No-majority-Regel & Passthrough-Haerte (kein FK-Prosa-Routing noetig):** FK-34 §34.8.2 gibt den Passthrough fuer unbekannte Rohverdikte **explizit** vor (`get(raw, raw)`, `:512-514`) und ist damit die autoritative Quelle — diese Story implementiert ihn FK-treu, ohne eine FK-Prosa-Aenderung anzustossen. Die No-majority-Regel (drei paarweise verschiedene Dreiwerte → fail-closed strengstes Verdikt nach `PASS < CONCERN < FAIL`) ist eine bewusste, hier dokumentierte **Code-Entscheidung**, die durch die Code-AC **AC4** abgedeckt und getestet wird. FK-34 §34.8.4 ist bereits die autoritative Quelle; eine FK-Prosa-Ergaenzung ist nicht erforderlich, daher **kein** Routing an einen fremden Owner (AG3-103 owned FK-68/Schema-Katalog-Scope, **nicht** FK-34-§34.8-Prosa). Kein stilles Abweichen vom FK in dieser Code-Story.

## 3. Akzeptanzkriterien
1. `agentkit/telemetry/divergence.py` existiert mit `VERDICT_NORMALIZATION`, `normalize_verdict()`, `check_divergence()`, `apply_quorum()` und `ReviewPairDivergence`; `ReviewPairDivergence` ist `@dataclass(frozen=True)` mit **exakt** den Feldern `reviewer_a: str`, `reviewer_b: str`, `verdict_a: str`, `verdict_b: str`, `divergent: bool`, `quorum_triggered: bool`, `final_verdict: str | None` (Test: Felder, Typen und `frozen=True` werden assertiert — nicht nur Symbol-Existenz).
2. `normalize_verdict` mappt `PASS`/`PASS_WITH_CONCERNS`/`REWORK`/`FAIL` deterministisch auf `PASS`/`CONCERN`/`FAIL`; **unbekanntes Verdikt → Passthrough** (`get(raw, raw)`, FK-34 §34.8.2) (Tests inkl. unbekannter Wert → unveraendert zurueck).
3. `check_divergence` erkennt Divergenz auf den **normalisierten** Dreiwerten (nicht binaer Pass/nicht-Pass); `True` gdw. ungleich (Test: `CONCERN` vs `FAIL` → divergent; `PASS` vs `PASS` → nicht divergent).
4. **Quorum/2-gegen-1 inkl. No-majority:** bei drei normalisierten Verdikten liefert `apply_quorum` das Mehrheitsverdikt (zwei gleiche bestimmen das Ergebnis), `ReviewPairDivergence.quorum_triggered=True`, `final_verdict` gesetzt. **No-majority** (drei paarweise verschiedene `PASS`/`CONCERN`/`FAIL`): deterministisch fail-closed strengstes Verdikt nach `PASS < CONCERN < FAIL` (Tests: alle Mehrheits-Konstellationen + der No-majority-Fall).
5. Das LOW/MEDIUM/HIGH-Scoring ist abgeloest: `_SCORE_LOW`/`_SCORE_HIGH` und die binaere `_PASS_VERDICTS`/`_find_diverging_pair`-Logik entfallen; `DivergenceHook` nutzt das neue Modul; das `review_divergence`-Event-Payload traegt **exakt** `story_id`, `reviewer_a`, `reviewer_b`, `divergent`, `quorum_triggered`, `final_verdict` (FK-34 §34.8.4) und **kein** `score`/`routing` (Test + Schema-Assertion auf den Payload-Feldsatz).
5a. **Fail-closed-Pin + Consumer-Mitzug (FK-68-Eventing-Hoheit):**
   - `MANDATORY_PAYLOAD_FIELDS` (`events.py`) enthaelt `EventType.REVIEW_DIVERGENCE` mit **exakt** `("story_id", "reviewer_a", "reviewer_b", "divergent", "quorum_triggered", "final_verdict")`; ein Payload ohne eines dieser Felder wird durch `validate_event_payload` fail-closed abgelehnt (Test).
   - Der Contract-Pin `_EXPECTED_MANDATORY_FIELDS` (`tests/contract/telemetry/test_event_catalog.py`) traegt denselben Eintrag; `test_mandatory_payload_fields_match_contract` ist gruen (kein Drift).
   - `_EXCERPT_KEYS` des `EventNormalizer` (`normalizer.py`) traegt **kein** `score` mehr, sondern `divergent`/`quorum_triggered`/`final_verdict` (neben `reviewer_a`/`reviewer_b`); der INTEGRITY-Excerpt eines `review_divergence`-Events enthaelt das FK-34-Format (Test in `tests/unit/telemetry/risk_window/test_normalizer.py` mitgezogen).
6. **Nicht-Divergenz-Event (FK-34 §34.8.4, `:589`):** uebereinstimmende Verdikte → der Hook emittiert ein `review_divergence`-Event mit `divergent=False`, `quorum_triggered=False`, `final_verdict=null`, kein Tiebreaker (Test). (Bewusste Ausrichtung an das zitierte FK statt „kein Event".)
7. Divergierendes Paar + dritter Verdikt → der Hook emittiert ein `review_divergence`-Event mit `divergent=True`, `quorum_triggered=True`, `final_verdict=<Mehrheit>` (Test).
8. Contract-/Golden-Tests des `review_divergence`-Events sind konsistent angepasst und gruen (kein paralleles Divergenz-Format) — insbesondere `tests/contract/telemetry/test_event_catalog.py` (Mandatory-Field-Pin) und die Hook-/Normalizer-Unit-Tests.
9. **Pflichtbefehle gruen** (alle ueber das Projekt-venv):
   - `.venv\Scripts\python -m pytest` fuer `tests/unit`, `tests/integration`, `tests/contract` (in Chunks, `-n0`).
   - `.venv\Scripts\python -m mypy src` (default) **und** `.venv\Scripts\python -m mypy src --platform linux`.
   - `.venv\Scripts\python -m ruff check src tests`.
   - Vier Konzept-Gates: `scripts/ci/check_concept_frontmatter.py`, `scripts/ci/compile_formal_specs.py`, `scripts/ci/check_concept_code_contracts.py`, `scripts/ci/check_architecture_conformance.py`.
   - Remote-Gates (Jenkins/Sonar) via `scripts/ci/check_remote_gates.ps1` nach Commit in der CI.
   - Coverage ≥ 85 %.

## 4. Definition of Done
- AK 1–9 (inkl. 5a) erfuellt; giftige Codex-Review PASS; Implementierung/Commit erst nach Execution-Plan-Freigabe.

## 5. Guardrail-Referenzen
- **FAIL CLOSED:** No-majority (drei verschiedene Dreiwerte) → strengstes Verdikt, nie stilles PASS; abgedeckt durch die Code-AC AC4. (Unbekannte Rohverdikte folgen FK-treu dem von FK-34 §34.8.2 vorgegebenen Passthrough, `:512-514`; keine FK-Prosa-Aenderung und kein fremdes Owner-Routing noetig.)
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** das abgeloeste Scoring wird real entfernt, nicht parallel gefuehrt; ein Divergenz-Modul, ein Event-Format.
- **TYPISIERT STATT STRINGS:** `ReviewPairDivergence`-`@dataclass(frozen=True)` + normalisierte Dreiwerte statt String-Score-Buckets.
- **ARCH-55:** alle Verdict-/Feld-/Wire-Keys englisch.
- **State/Telemetrie-Disziplin:** Event-Schema-Aenderung nur mit mitgezogenen Contract-/Golden-Tests. `review_divergence` faellt unter FK-68-Eventing-Hoheit (`68_telemetrie...md:9-13`); das Code-Schema wird end-to-end (Hook, `MANDATORY_PAYLOAD_FIELDS`, Contract-Pin, Risk-Window-Excerpt) auf den FK-34-Feldsatz gezogen — ein kanonisches Format.
- **KONZEPTTREUE / FK-34↔FK-68-Konflikt:** Nicht-Divergenz-Event-Verhalten an FK-34 §34.8.4 ausgerichtet. Der FK-uebergreifende Widerspruch (FK-68 §68.2.2-Payload-Zeile `score`/`routing` vs. FK-34 §34.8.4) wird **entlang der Hoheiten** aufgeloest: FK-34 §34.8.4 (`:570-582`) referenziert „Kap. 68" (`:573`) und definiert den neuen Feldsatz (`:575-582`); die FK-68-§68.2.2-Zeile ist die **stale Telemetrie-Owner-Zeile** (`:358-362`), die per Owner-Scope-Erweiterung (AG3-103) anzugleichen ist — daher migriert diese Story das Code-Schema FK-34-konform, ohne durch die stale FK-68-Prosa blockiert zu sein. Die FK-68-Prosa-Angleichung ist `concept/`-doc-only und **sichtbar an ihren Owner geroutet** (AG3-103, FK-68 §68.2), der die §68.2.2-Payload-Feldzeile inzwischen als Owner-Scope fuehrt (`AG3-103/story.md:13`, `:36`, `:52`, `:63`); das AG3-066-Routing zeigt damit auf einen real getragenen Owner-Scope und ist gueltig. Eine fruehere **harte `depends_on`-Erzwingung wurde entfernt**, weil sie nur eine Ordnung vortaeuschte und das sichtbare Routing hinter einem Pfeil verstecken wuerde (FIX THE MODEL statt Symptom hinter einem Pfeil verstecken). **No-majority und Passthrough-Haerte sind keine FK-34-§34.8-Prosa-Luecken:** FK-34 §34.8.2 gibt den Passthrough explizit vor (`get(raw, raw)`, `:512-514`) und ist damit autoritativ; das fail-closed No-majority-Verhalten ist eine bewusste Code-Entscheidung, die durch die Code-AC (AC4) abgedeckt ist — keine FK-Prosa-Aenderung noetig, kein Routing an einen fremden Owner.
- **ZERO DEBT:** Quorum-Logik real (kein „dritter Reviewer spaeter" wie im aktuellen Hook-Docstring); die Reviewer-C-Beschaffung ist sauber an den QA-/Layer-2-Owner abgegrenzt, nicht stillschweigend weggelassen.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- Der aktuelle `DivergenceHook` (`telemetry/hooks/divergence_hook.py`) ist binaer (Pass/nicht-Pass, `:143-166`) und nutzt LOW/HIGH (`:30-31`, `:93`) — beide Aspekte werden durch das Dreiwert-Quorum **ersetzt**, nicht ergaenzt.
- Das `review_divergence`-Payload aendert sich (`score`/`routing` → `divergent`/`quorum_triggered`/`final_verdict`, FK-34 §34.8.4 `:575-582`) — **alle realen Consumer end-to-end mitziehen:** Hook-Payload, `MANDATORY_PAYLOAD_FIELDS` (`events.py:173-221`), Contract-Pin `_EXPECTED_MANDATORY_FIELDS` (`tests/contract/telemetry/test_event_catalog.py:112-141`) und `_EXCERPT_KEYS` des `EventNormalizer` (`risk_window/normalizer.py:56-67`, toten `score`-Key ersetzen). `EventType.REVIEW_DIVERGENCE` (`events.py:64`) bleibt.
- `review_divergence` ist FK-68-Eventing-Schema (`68_telemetrie...md:9-13`). Die FK-68 §68.2.2-Payload-Feldzeile unter `#### Review-Divergenz` (`:358-362`, noch `score`/`routing`) ist doc-only und gehoert **AG3-103** (FK-68 §68.2-Owner) — hier **keine** `concept/`-Datei anfassen und AG3-103s Story-Datei nicht aus dieser Story heraus aendern. Das Code-Schema folgt der autoritativen FK-34-§34.8.4-Quelle (`:570-582`; FK-34 §34.8.4 referenziert „Kap. 68", `:573`, und definiert den Feldsatz), unabhaengig vom Stand der stale FK-68-§68.2.2-Owner-Zeile (`:358-362`). Die FK-68-Prosa-Angleichung ist als Owner-Scope-Erweiterung an AG3-103 geroutet (§2.2), **nicht** ueber eine harte Abhaengigkeit erzwungen.
- **`divergence.py` bleibt reine Logik** (kein LLM-Call). Der Reviewer-C-LLM-Call gehoert laut FK-34 §34.8.3 (`:566-568`) dem QA-Agenten/Layer-2-Pfad (AG3-037/AG3-043; Transport AG3-065) — hier **nicht** bauen und **nicht** als Dependency aufnehmen; nur die drei Verdikte als Funktionsargumente entgegennehmen.
- Nicht-Divergenz emittiert dennoch ein Event (FK `:589`); nicht das alte „kein Event"-Verhalten reproduzieren.
- No-majority-Regel ist eine bewusste, hier dokumentierte Code-Entscheidung (fail-closed strengstes Verdikt), abgedeckt durch AC4; der Passthrough fuer unbekannte Rohverdikte folgt der autoritativen FK-34-§34.8.2-Vorgabe (`get(raw, raw)`, `:512-514`). Keine FK-Prosa-Ergaenzung und **kein** Routing an AG3-103 noetig (AG3-103 owned FK-68/Schema-Katalog-Scope, nicht FK-34-§34.8-Prosa) — keine eigene FK-Aenderung in dieser Story.
- `risk_window`-Telemetrie (`telemetry/risk_window/*`) ist Governance (AG3-085) — nicht vermischen.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Testnamen (normalize/check_divergence/apply_quorum-No-majority/Schema-Abloesung/Nicht-Divergenz-Event/Negativpfade).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

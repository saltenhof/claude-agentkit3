Summary
Reviewed HEAD 9269626b against base 864c4844 for sub-step C only. Normal AC5 flow is edge-staged and fail-closed: `on_enter` commissions probe/provision stages and returns PAUSED on pending edge work (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:256`, `src/agentkit/backend/governance/setup_preflight_gate/phase.py:642`, `src/agentkit/backend/governance/setup_preflight_gate/phase.py:676`), `on_resume` re-enters the same path (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:497`), the pause is `AWAITING_EDGE_PROVISIONING` (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:709`), command IDs are deterministic (`src/agentkit/backend/control_plane/edge_commands.py:134`), and existing commands are loaded before insert (`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:213`). `worktree_map` is populated from reported `worktree_report.worktree_root` only (`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:164`, `src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:172`, `src/agentkit/backend/governance/setup_preflight_gate/phase.py:687`).

AC6 branch decision is sound on the consulted path: missing/unreadable probe maps to `edge_probe_missing` FAIL (`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:255`, `src/agentkit/backend/control_plane/edge_commands.py:293`), legitimate branch takeover requires own active ownership plus a transfer base (`src/agentkit/backend/control_plane/edge_commands.py:264`) and then local/remote alignment to that base (`src/agentkit/backend/control_plane/edge_commands.py:346`, `src/agentkit/backend/control_plane/edge_commands.py:360`). Remote story-branch state is read via `GitLsRemoteReader.read_head_sha` (`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:302`, `src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:330`). Grep found no surviving `utils.git`, `branch_exists`, `show-ref`, `create_worktree`, `write_story_marker`, or setup/preflight `git -C <worktree>` subprocess in the setup/preflight flow; the only production `git -C` hit is a composition-root change-evidence port outside this setup/preflight path (`src/agentkit/backend/bootstrap/composition_root.py:1457`).

Findings

CRITICAL: A worktree story can complete setup without any edge `preflight_probe` or `worktree_report` if the incoming run context misclassifies the story as non-worktree.

Evidence: `on_enter` resolves the worktree plan before building the enriched context from the authoritative StoryService record (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:248`, enrichment only later at `src/agentkit/backend/governance/setup_preflight_gate/phase.py:277`). `_resolve_worktree_plan` decides `uses_worktree` from `ctx.story_type` (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:585`) and returns `(False, (), branch)` when that profile is non-worktree or `create_worktree` is false (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:587`). The handler explicitly documents this input as the initial, possibly sparse context that is enriched from StoryService (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:232`). The authoritative builder later reads the real story type and participating repos from StoryService (`src/agentkit/backend/governance/setup_preflight_gate/context_builder.py:67`, `src/agentkit/backend/governance/setup_preflight_gate/context_builder.py:119`, `src/agentkit/backend/governance/setup_preflight_gate/context_builder.py:134`), but the already-computed non-worktree plan is still used for provisioning (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:327`).

Failure scenario: the dispatch enters setup for `AG3-900` with a stale/sparse `StoryContext` carrying `story_type=CONCEPT`, while StoryService's authoritative record for `AG3-900` is `IMPLEMENTATION` with participating repos. `_resolve_worktree_plan` returns non-worktree, `_edge_probe_stage` returns `(None, None)` without commissioning a probe (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:629`), Check 7 and Check 8 optimistically PASS because `edge_probe_reports is None` means "no worktree edge consultation" (`src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_story_branch.py:52`, `src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_stale_worktree.py:46`), `_edge_provision_stage` returns the now-enriched implementation context unchanged (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:662`), and setup can return COMPLETED (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:364`) with no edge report and no `worktree_map`. This is a fail-open AC5/AC6 bypass.

C to D teardown gap
I do not count the removed backend setup-failure `remove_worktree` cleanup as a separate C finding. The deleted backend worktree module was local-backend provisioning/cleanup (`src/agentkit/backend/governance/setup_preflight_gate/worktree.py` removed; old `remove_worktree` import and persistence-failure cleanup removed from `phase.py` in the diff), and it could not clean an edge-provisioned dev-local worktree in the target topology. The replacement edge `teardown_worktree` commissioning path is explicitly sub-step D. There is still residual leak risk after an edge-provisioned worktree if later setup steps fail, but within the stated internal cut I treat that as the accepted C to D gap, not the rejection reason.

Internal-cut integrity
The old backend provisioning truth is removed: `worktree.py` is deleted, its tests are deleted, and current setup/preflight modules no longer import `agentkit.backend.utils.git` or call `setup_worktrees`/`write_story_marker`/`branch_exists`. The productive setup handler is wired with `build_setup_edge_provisioning_coordinator` in the composition root (`src/agentkit/backend/bootstrap/composition_root.py:2072`). The AC5 integration test drives the real `SetupPhaseHandler` through real edge-command queue/executor dispatch, while stubbing unrelated preflight/context-build concerns (`tests/integration/governance/test_setup_edge_provisioning.py:3`, `tests/integration/governance/test_setup_edge_provisioning.py:171`, `tests/integration/governance/test_setup_edge_provisioning.py:251`).

D/E untouched
The diff name list does not include `state_backend/store/worktree_repository.py`, `utils/git.py`, `governance/runner.py`, `control_plane/workspace_locator.py`, or `bootstrap/story_reset_adapters.py`.

K5 / ARCH-55
The command/ownership paths are wired through the Postgres-only control-plane repositories: `RunOwnershipRepository` documents non-Postgres fail-closed behavior (`src/agentkit/backend/control_plane/repository.py:229`, `src/agentkit/backend/control_plane/repository.py:237`), `TakeoverTransferRepository` is Postgres-only (`src/agentkit/backend/control_plane/repository.py:294`, `src/agentkit/backend/control_plane/repository.py:298`), and `EdgeCommandRepository` is Postgres-only with fail-closed methods off Postgres (`src/agentkit/backend/control_plane/repository.py:333`, `src/agentkit/backend/control_plane/repository.py:336`). Command kinds, identifiers, finding codes, comments, and docstrings reviewed in this delta are English; wire finding codes such as `stale_foreign_branch` and `local_stale_or_dirty_takeover_target` are English (`src/agentkit/backend/control_plane/edge_commands.py:170`, `src/agentkit/backend/control_plane/edge_commands.py:184`).

Static checks run: `git --no-pager diff --check 864c4844..9269626b`; targeted grep/read-only inspection only. Per instruction, no Postgres integration tests or DB-touching pytest were run.

VERDICT: REJECT

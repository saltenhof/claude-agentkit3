Summary
Reviewed remediation HEAD 5ebc1564 against base 9269626b for the single Codex C-r1 CRITICAL only. The fail-open is closed: `on_enter` still computes the worktree plan before enrichment, but it now calls `_resolve_worktree_plan(cfg, story_display_id, story_service)` with no incoming `ctx` parameter (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:248`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:252`). `_resolve_worktree_plan` derives the branch from the story id, short-circuits only on the authoritative `cfg.create_worktree` flag, then reads `story_service.get_story(story_display_id)` and derives both `uses_worktree` and repos from the authoritative record (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:600`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:609`). The sparse incoming `StoryContext.story_type` is no longer referenced by the classifier.

The plan gates both edge stages and Checks 7/8. The probe stage uses `plan` and returns `(None, None)` only when `uses_worktree` is false; otherwise it commissions `ensure_preflight_probes` and PAUSES while pending (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:647`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:662`). `_run_preflight_check` receives `edge_probe_reports` plus `participating_repos=plan[1]` (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:267`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:277`), and `run_preflight` carries those into `PreflightContext` for Checks 7/8 (`src/agentkit/backend/governance/setup_preflight_gate/preflight.py:226`-`src/agentkit/backend/governance/setup_preflight_gate/preflight.py:241`). The provision stage also gates on the same plan and PAUSES until reported worktree data exists (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:680`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:695`).

The sparse-CONCEPT/auth-IMPLEMENTATION bypass is covered by the new regression. The test constructs a stale incoming context with `story_type=CONCEPT` and no repos (`tests/integration/governance/test_setup_edge_provisioning.py:247`-`tests/integration/governance/test_setup_edge_provisioning.py:263`), while the authoritative service record is `WireStoryType.IMPLEMENTATION` with `_REPO` (`tests/integration/governance/test_setup_edge_provisioning.py:126`-`tests/integration/governance/test_setup_edge_provisioning.py:148`, `tests/integration/governance/test_setup_edge_provisioning.py:371`-`tests/integration/governance/test_setup_edge_provisioning.py:375`). First setup entry asserts `PAUSED`, `AWAITING_EDGE_PROVISIONING`, and an actual `preflight_probe` command, with no `begin_progress` and no provision command yet (`tests/integration/governance/test_setup_edge_provisioning.py:396`-`tests/integration/governance/test_setup_edge_provisioning.py:410`). It then drives the real edge executor path and proves eventual provisioning/completion from the same sparse incoming context (`tests/integration/governance/test_setup_edge_provisioning.py:412`-`tests/integration/governance/test_setup_edge_provisioning.py:429`).

No fix-introduced defect found in the requested cases. Resume re-enters `on_enter` (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:475`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:500`), so it re-derives the same plan from the same authoritative record and config; command duplication is prevented by deterministic `edge_command_id(run_id, command_kind, repo_id)` (`src/agentkit/backend/control_plane/edge_commands.py:134`-`src/agentkit/backend/control_plane/edge_commands.py:152`) plus load-before-insert in `_commission_and_load` (`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:209`-`src/agentkit/backend/bootstrap/edge_provisioning_adapter.py:236`). Legitimate non-worktree behavior is preserved because authoritative `CONCEPT`/`RESEARCH` profiles have `uses_worktree=False` (`src/agentkit/backend/story_context_manager/types.py:77`-`src/agentkit/backend/story_context_manager/types.py:98`), `_resolve_worktree_plan` returns non-worktree for that profile (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:606`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:608`), and Checks 7/8 intentionally PASS when the edge was not consulted for a non-worktree story (`src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_story_branch.py:52`-`src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_story_branch.py:57`, `src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_stale_worktree.py:46`-`src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/no_stale_worktree.py:51`). `cfg.create_worktree=False` also remains a direct no-edge path (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:600`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:602`). If `story_service.get_story` returns `None`, `_resolve_worktree_plan` returns no repos/no edge (`src/agentkit/backend/governance/setup_preflight_gate/phase.py:603`-`src/agentkit/backend/governance/setup_preflight_gate/phase.py:605`), and preflight Check 1 fails the missing story (`src/agentkit/backend/governance/setup_preflight_gate/preflight.py:226`-`src/agentkit/backend/governance/setup_preflight_gate/preflight.py:241`, `src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/story_exists.py:30`-`src/agentkit/backend/governance/setup_preflight_gate/preflight_checks/story_exists.py:39`). The enum conversion is type-correct for valid authoritative records: `Story.story_type` is `WireStoryType` (`src/agentkit/backend/story_context_manager/story_model.py:57`-`src/agentkit/backend/story_context_manager/story_model.py:64`, `src/agentkit/backend/story_context_manager/story_model.py:190`-`src/agentkit/backend/story_context_manager/story_model.py:200`), and its values exactly match runtime `StoryType` values accepted by `get_profile` (`src/agentkit/backend/story_context_manager/types.py:17`-`src/agentkit/backend/story_context_manager/types.py:22`, `src/agentkit/backend/story_context_manager/types.py:42`-`src/agentkit/backend/story_context_manager/types.py:109`).

Static/read-only checks only per instruction: `git --no-pager diff --check 9269626b..5ebc1564` passed. I did not run Postgres integration tests or any pytest touching the control-plane DB.

Findings
None.

VERDICT: APPROVE

# AG3-097: ai_augmented-Abgrenzung + Multi-LLM-Feindesign-Vollausfuehrung

**Typ:** Implementation
**Groesse:** M
**Bounded Context:** `governance` (Integrity-Gate-Mode-Abgrenzung) + `exploration` (Feindesign / Change-Frame) + `multi_llm_hub`-Konsum — drei zusammengehoerige Restpunkte rund um den Betriebsmodus `ai_augmented` und die Multi-LLM-Feindesign-Diskussion.
**Quell-Konzepte (autoritativ):**
- `FK-56 §56.7a` / `§56.10` — im `ai_augmented`-Modus kein Integrity-Gate (kein `integrity_gate_started`/`integrity_gate_result`, keine Closure-FAIL-Codes); bei `binding_invalid` fail-closed Block mutierender Aktionen; explizite Mode-Ausschlussabgrenzung
- `FK-25 §25.5.2` — Rollen: ChatGPT **Pflicht-Berater**, zweiter Berater **Pflicht** (Qwen bevorzugt, ersatzweise Gemini/Grok); steht kein zweites LLM zur Verfuegung, greift die Nicht-Erreichbarkeits-Regel
- `FK-25 §25.5.4` — AgentKit-Ueberwachung: Hook-basierte 10-Send-Zaehlung (blockiert den **11.** Send), `llm_session_stats`-Post-hoc-Verifikation (jedes akquirierte LLM >=1 Antwort, sonst Abbruch), Session-Release-Pruefung (Telemetrie-WARNING bei Verletzung), Nicht-Erreichbarkeit -> `status: PAUSED` / `escalation_class: "infra_unavailable"` / `escalation_reason: "Multi-LLM-Quorum nicht erreichbar"` (FK-25 §25.5.4 Z. 646-650). **Carrier-Hinweis (belegt):** Die FK-25-`escalation_class`-Taxonomie (`domain_gap` / `normative_conflict` / `scope_explosion` / `impact_exceeded` / `infra_unavailable`, §25.5.4 Z. 230-231/301/336/647) ist **FK-25-eigen**; FK-25 §25.5.4 (`defers_to_edges`, Z. 23-25) delegiert die **Eskalations-Mechanik** (`PAUSED`/`ESCALATED`, CLI, Resume) explizit an **FK-35**, **nicht** an FK-39. Sie ist damit **kein** FK-39-Feld: FK-39 §39.2.1 (Z. 243) kennt ausschliesslich `escalation_reason: String|null` (gesetzt nur bei `status: ESCALATED`) mit einem **geschlossenen** Wertesatz `{worker_blocked, max_rounds_exceeded, preflight_fail, integrity_fail, merge_fail, doc_fidelity_fail, impact_violation, design_review_rejected, governance_violation}` — **ohne** `infra_unavailable`; ein `escalation_class`-Feld existiert in FK-39 gar nicht. **Hook-Hinweis:** FK-25 §25.5.4 (Z. 615) nennt „der **bestehende** Hook-Mechanismus", der `llm_send`-Aufrufe pro Session zaehlt und den 11. Send blockiert — diese Send-Count-Sensorik (`*_send`-PostToolUse-Pfad) ist Hook-/Guard-Infrastruktur (FK-30-Territorium), existiert im AK3-Code aber **nicht** (siehe §1) und hat **keinen** aktuellen Story-Owner (siehe §2.2).
- `FK-25 §25.5.5` / `§25.10` — 8. Change-Frame-Bestandteil (FK-Konzeptname `feindesign_entscheidungen`); optionaler achter Bestandteil des Entwurfsartefakts

---

## 1. Kontext / Ist-Zustand (belegt)

- **ai_augmented-Integrity-Gate-Abgrenzung UNVOLLSTAENDIG (schwach):** der `binding_invalid`-fail-closed-Block fuer Mutationen ist real (`governance/guard_evaluation.py:96-105`). Die negative Abgrenzung „kein Integrity-Gate im ai_augmented-Modus" ist nur **implizit** (das Integrity-Gate `IntegrityGate.evaluate` nimmt `story_dir`/`story_type`, aber **keinen** `operating_mode` — `governance/integrity_gate/__init__.py:151`). Es fehlt eine **explizite** Mode-Pruefung, die einen versehentlichen Aufruf im `ai_augmented`-Modus hart und typisiert ausschliesst (Gap FK-56 §56.7a/§56.10).
- **`operating_mode_resolver`-Namespace ABWEICHEND:** PROJECT_STRUCTURE sieht `story_context_manager/operating_mode_resolver/` vor (`PROJECT_STRUCTURE.md:120`); die Mode-Resolution lebt in `governance/guard_evaluation.py` (`evaluate_pre_tool_use`, Zeile 85) + `projectedge/runtime.py` (`OperatingMode` Literal, Zeile 29) — funktional korrekt, aber nicht am vorgesehenen Ort (Gap §56-PROJECT_STRUCTURE-Mapping).
- **Multi-LLM-Feindesign-Wiring UNVOLLSTAENDIG:** `exploration/mandate/fine_design.py` implementiert den bounded Loop (`DEFAULT_MAX_ROUNDS=10` Zeile 38; `FineDesignSubprocess` Zeile 143), arbeitet aber gegen eine **abstrakte** `FineDesignEvaluator`-Protokollschnittstelle (`fine_design.py:102`). Die konkrete Hub-Verdrahtung (ChatGPT-Pflicht, zweiter Pflicht-Berater Qwen/Fallback, `llm_session_stats`-Post-hoc, Hook-Send-Count, `infra_unavailable`-Eskalation) ist nur in Docstrings (`fine_design.py:5-25`, `FineDesignEvaluatorUnavailableError` Zeile 41) beschrieben — **keine** adaptierende Implementierung gegen `multi_llm_hub` (Gap FK-25 §25.5.2/§25.5.4).
- **`llm_session_stats`-Transport FEHLT in AK3-Code:** der AK3-`HubClient` kennt `health/pool_status/list_sessions/acquire/send/release/resume` (`multi_llm_hub/client.py:92-109`), aber **kein** `session_stats`. Auch die BFF-Routen kennen status/sessions/events/acquire/send/release, aber **kein** stats (`multi_llm_hub/http/routes.py:96-102` GET, `:112-122` POST). Der externe Hub bietet ein `llm_session_stats`-MCP-Tool an (FK-25 §25.5.4 „Anforderung an den Hub"); die AK3-seitige **Konsum-Surface** dafuer existiert noch nicht.
- **Eskalations-Carrier `escalation_class`/`infra_unavailable` FEHLT als typisiertes Phase-State-Feld (belegt) — kein aktueller Story-Owner:** der reale `PhaseState` (`story_context_manager/models.py:436-479`) hat `status`, `paused_reason: PauseReason | None`, `errors`, aber **kein** `escalation_class` und **kein** `escalation_reason`. `PauseReason` ist ein **geschlossenes 3-Wert-Enum** `{AWAITING_DESIGN_REVIEW, AWAITING_DESIGN_CHALLENGE, GOVERNANCE_INCIDENT}` (`core_types/pause_reason.py:46-60`) — **kein** `infra_unavailable`. Der heutige Exploration-Eskalationspfad baut ein `HandlerResult(status=ESCALATED, yield_status=PauseReason....value, errors=..., suggested_reaction=...)` (`exploration/phase.py:701-723`, `:746-759`); es gibt **keinen** Pfad, der `status: PAUSED` mit einem typisierten `escalation_class`-Feld setzt. Die FK-25-`escalation_class`-Taxonomie (inkl. `infra_unavailable`) ist **FK-25-eigen** und delegiert die Mechanik an **FK-35** (§25.5.4 `defers_to_edges` Z. 23-25) — sie ist **kein** FK-39-Feld (FK-39 §39.2.1 Z. 243 kennt nur `escalation_reason` mit geschlossenem Wertesatz **ohne** `infra_unavailable`, **kein** `escalation_class`). **Belegte Konsequenz:** Im `_STORY_INDEX.md` baut **keine** Story den typisierten `escalation_class`-Phase-State-Carrier — AG3-047 (MandateClassification) eskaliert die **Geschwister**-Mandatsklassen (Klasse 1/3/4: `scope_explosion`/`impact_exceeded`/`domain_gap`) heute ueber denselben `HandlerResult.ESCALATED`+`suggested_reaction`-Pfad (`AG3-047/story.md:157-163`), **nicht** als typisiertes Feld. AG3-085 (FK-35 §35.3, `_STORY_INDEX.md:96`) ist der GovernanceObserver-Risikoscore, **nicht** der `escalation_class`-Carrier. Der typisierte FK-35-Carrier ist damit ein offener, **noch nicht geschnittener** Gap (siehe §2.2 + Cross-Story-Hinweise) — AG3-097 baut ihn **nicht** und behauptet **nicht**, eine andere Story liefere ihn.
- **Hook-Send-Count-Sensorik FEHLT im Produktivpfad (belegt) — kein aktueller Story-Owner:** `HookEvent.operation` ist ein **geschlossenes** Literal `{bash_command, file_write, file_edit, file_read, unknown_tool}` (`governance/guard_evaluation.py:28-34`) — **kein** `llm_send`. Es existiert **kein** produktiver Send-Count-Guard; `llm_send` taucht im Repo nur in Skill-Bundle-Ressourcen auf (`resources/skill_bundles/llm-discussion-core/4.0.0/SKILL.md:5/58`, `create-userstory-core/.../SKILL.md`), nicht als Runtime-Guard. Der reale Python-Pfad eines Sends ist der direkte `HubClient.send(...)` (`multi_llm_hub/client.py:168`), der einen Harness-Hook **umgeht**, solange kein `*_send`-PostToolUse-Guard gebaut ist. Die Hook-basierte 11.-Send-Blockade (FK-25 §25.5.4 Z. 615-618) ist damit **nicht buildbar** im AG3-097-Cut. **Belegte Konsequenz:** **Keine** `_STORY_INDEX.md`-Story scoped `llm_send`/`*_send`-PostToolUse-Send-Count-Enforcement — AG3-086 (FK-30 §30.5.1/§30.10, `_STORY_INDEX.md:97`) baut `WebCallBudgetGuard`/`skill_usage_check`/Prompt-Integrity/CCAG-TTL, aber **keinen** `*_send`-Send-Count-Guard (`AG3-086/story.md:32-47`), und AG3-095 (`_STORY_INDEX.md:126`) ownt nur die **Katalog-Praesenz** des `llm-discussion`-Bundles und nennt das Hub-Wiring explizit als AG3-097 (`AG3-095/story.md:73`). Die Echtzeit-Hook-Blockade ist damit ein offener, **noch nicht geschnittener** Gap (siehe §2.2) — AG3-097 enforced die 10-Runden-Grenze nur im Feindesign-Adapter und behauptet **nicht**, eine andere Story liefere die Hook-Sensorik.
- **8. Change-Frame-Feld FEHLT:** `exploration/change_frame.py` (`ChangeFrame`, Zeile 270) definiert die 7 Pflicht-Bestandteile (`goal_and_scope` … `open_points`, Zeilen 298-305) plus Identitaet/Lifecycle (`frozen`/`frozen_at`, Zeilen 308/316). Das optionale **achte** Feld fehlt. `FineDesignDecision` existiert bereits eigenstaendig mit **englischen** Keys (`decision_id`/`question`/`decision`/`rationale`/`normative_basis`/`llm_responses`, `fine_design.py:56-83`), wird aber nicht als achte Komponente in das Change-Frame-Artefakt persistiert (Gap §25.5.5/§25.10).

Anknuepfungspunkte (existieren, konsumieren):
- `multi_llm_hub` R-Adapter (`multi_llm_hub/client.py`, BFF-Routen) als Hub-Transport; die `session_stats`-Konsum-Surface wird hier ergaenzt.
- `FineDesignEvaluator`-Protokoll + `FineDesignSubprocess` (`fine_design.py`) als Loop-Geruest, das eine konkrete Hub-gestuetzte Implementierung erhaelt. `FineDesignEvaluatorUnavailableError` (`fine_design.py:41`) ist der bereits vorgesehene Nicht-Erreichbarkeits-Signalpfad; AG3-097 reichert ihn mit der typisierten FK-25-Triple als Payload an und eskaliert ihn am Aufruferrand ueber den **bestehenden** `_escalate`-ESCALATED-Pfad (`exploration/phase.py:701-723`) — denselben Pfad, ueber den AG3-047 die Geschwister-Mandatsklassen (Klasse 1/3/4) eskaliert. Der typisierte `escalation_class`-Phase-State-Carrier (FK-35-Mechanik) existiert nicht und hat keinen Story-Owner; AG3-097 fuehrt ihn **nicht** ein.
- `ChangeFrame` (frozen Pydantic) als Erweiterungsziel fuer das achte, optionale Feld.
- Integrity-Gate (`governance/integrity_gate/__init__.py`, `dimensions.py`) + Mode-Resolution (`guard_evaluation.py`, `projectedge/runtime.py`).

## 2. Scope

### 2.1 In Scope
1. **Explizite Integrity-Gate-Mode-Ausschlussabgrenzung (§56.7a/§56.10):** eine harte, **typisierte** Mode-Pruefung im integrity_gate-Pfad. Wird das Integrity-Gate im `ai_augmented`-Modus aufgerufen, wird **vor** jeglicher Integritaetsarbeit (vor `integrity_gate_started`) eine typisierte Exception `IntegrityGateNotApplicableError` geworfen; es werden **keine** Integrity-Gate-Events emittiert und **keine** Closure-FAIL-Codes produziert. Damit ist die negative Abgrenzung explizit statt implizit.
2. **`operating_mode_resolver`-Namespace (§56-Mapping, MUSS):** die bestehende, korrekte Mode-Resolution unter einem benannten `operating_mode_resolver`-Namespace bereitstellen (gem. PROJECT_STRUCTURE), **ohne** Verhaltensaenderung — Konsolidierung der heute verstreuten Resolution-Logik auf einen benannten Owner, von dem `guard_evaluation.py` und der Integrity-Gate-Mode-Guard (Punkt 1) den Modus beziehen. Kein „melden statt liefern": die benannte Resolver-Surface ist verbindlicher Bestandteil dieser Story. (Hinweis: die doc-only-Konzept-Nachzug der FK-56-Namespace-Prosa liegt bei **AG3-102**; hier nur der Code-Owner.)
3. **Multi-LLM-Hub-Wiring des Feindesigns (§25.5.2/§25.5.4):** eine konkrete `FineDesignEvaluator`-Implementierung gegen `multi_llm_hub`:
   - **ChatGPT Pflicht UND zweiter Berater Pflicht** (Qwen bevorzugt, ersatzweise Gemini/Grok) — beide ueber den Hub akquirieren/senden/freigeben; fehlt der zweite Berater bzw. ist kein zweites LLM verfuegbar, greift die Nicht-Erreichbarkeits-Regel (letzter Unterpunkt dieses Abschnitts: keine Klasse-2-Entscheidung ohne Multi-Perspektiven-Quorum).
   - **`llm_session_stats`-Post-hoc-Verifikation:** read-only `session_stats`-Konsum-Surface ergaenzen (HubClient-Methode + BFF-GET-Route + typisiertes Antwortmodell); jedes akquirierte LLM hat >=1 Antwort geliefert, sonst Abbruch (fail-closed, kein Treffen der Klasse-2-Entscheidung).
   - **10-Runden-Grenze im Feindesign-Adapter (buildbar in diesem Cut):** die `DEFAULT_MAX_ROUNDS=10`-Grenze (`fine_design.py:38`) wird vom bounded Loop (`FineDesignSubprocess`, `fine_design.py:143-198`) und der konkreten Hub-Implementierung **deterministisch im Adapter** durchgesetzt — nach 10 Sends pro LLM sendet die Implementierung nicht weiter und terminiert als `max_rounds_exceeded`. Das ist die in diesem Cut real lieferbare Rundenschranke; sie braucht keinen Harness-Hook. **Out-of-Scope (offener Gap, kein vorhandener Owner):** die zusaetzliche **Echtzeit-Hook-Blockade des 11. Sends** (`*_send`/`llm_send`-PostToolUse-Send-Count-Guard, FK-25 §25.5.4 „bestehender Hook-Mechanismus", Z. 615-618) ist eine Hook-/Guard-Infrastruktur und im AK3-Code **heute nicht vorhanden** (`HookEvent.operation` kennt kein `llm_send`, `guard_evaluation.py:28-34`). **Keine** `_STORY_INDEX.md`-Story scoped dieses `*_send`-Send-Count-Enforcement: AG3-086 baut `WebCallBudgetGuard`/`skill_usage_check`/Prompt-Integrity/CCAG-TTL (`AG3-086/story.md:32-47`), **nicht** den `*_send`-Guard; AG3-095 ownt nur die Katalog-Praesenz des `llm-discussion`-Bundles und verweist das Hub-Wiring auf AG3-097 (`AG3-095/story.md:73`). Die Send-Count-Hook-Sensorik ist damit ein offener Gap und in §2.2 als FK-30-Infrastruktur **ohne aktuellen Owner** gemeldet. AG3-097 baut diese Hook-Sensorik **nicht** und gibt nicht vor, sie zu liefern; es enforced die 10-Runden-Grenze ausschliesslich im Feindesign-Adapter.
   - **Session-Release-Pruefung:** die `session_stats`-Post-hoc-Auskunft zeigt korrektes Release; ist die Session nicht korrekt released, wird ein **WARNING** in die Telemetrie geschrieben (FK-25 §25.5.4, SEVERITY-Semantik: Handlungsauftrag mit aufschiebender Wirkung, nicht stilles Liegenlassen).
   - **Nicht-Erreichbarkeit (§25.5.4) — typisiertes Signal + Eskalation ueber den bestehenden ESCALATED-Pfad:** antwortet ein akquiriertes LLM nicht / steht kein zweites LLM zur Verfuegung, signalisiert die Implementierung fail-closed ueber `FineDesignEvaluatorUnavailableError` (`fine_design.py:41`). Dieser Error traegt die FK-25-Triple als **typisierte Payload** (`escalation_class="infra_unavailable"`, `escalation_reason="Multi-LLM-Quorum nicht erreichbar"`, gewuenschter Phase-State-`status: PAUSED`) — ein eigener Eskalationscode, **kein** Rueckfall auf `domain_gap`/`normative_conflict`. **Carrier-Abgrenzung (belegt, kein vorhandener Owner):** der reale `PhaseState` hat **kein** `escalation_class`/`escalation_reason`-Feld und `PauseReason` kennt **kein** `infra_unavailable` (`models.py:436-479`, `pause_reason.py:46-60`). Die FK-25-`escalation_class`-Taxonomie (inkl. `infra_unavailable`) ist **FK-25-eigen** mit FK-35-Mechanik-Delegation (§25.5.4 Z. 23-25) — **kein** FK-39-`escalation_reason` (FK-39 §39.2.1 Z. 243 kennt `infra_unavailable` **nicht**, und **kein** `escalation_class`-Feld). **Keine** `_STORY_INDEX.md`-Story baut den typisierten `escalation_class`-Carrier; AG3-047 eskaliert die Geschwister-Mandatsklassen (Klasse 1/3/4) heute ueber denselben `HandlerResult.ESCALATED`+`suggested_reaction`-Pfad (`AG3-047/story.md:157-163`). AG3-097 fuegt sich **konsistent** ein: der Exploration-Phase-Handler (`exploration/phase.py:701-723`, `_escalate`) eskaliert die `infra_unavailable`-Nicht-Erreichbarkeit ueber denselben bestehenden ESCALATED-Pfad und traegt die Triple als typisierte Payload in `errors`/`suggested_reaction`. AG3-097 fuehrt **keine** neuen Phase-State-Felder oder `PauseReason`-Werte ein (FIX-THE-MODEL: kein Schattenfeld neben dem fehlenden FK-35-Owner) und behauptet **nicht**, eine andere Story liefere einen `infra_unavailable`-Phase-State-Carrier. Der typisierte FK-35-`escalation_class`-Carrier (`status: PAUSED` + Resume) ist ein offener, noch nicht geschnittener Gap (siehe §2.2).
4. **8. Change-Frame-Bestandteil (§25.5.5/§25.10):** optionales achtes Feld am `ChangeFrame` mit dem **englischen Wire-Key `fine_design_decisions`** (Default: leere Liste / `None`; FK-Konzeptname ist `feindesign_entscheidungen`). Es traegt die `FineDesignDecision`-Liste (englische Unterfelder `decision_id`/`question`/`decision`/`rationale`/`normative_basis`/`llm_responses`, bereits in `fine_design.py:56-83` typisiert) als Entscheidungsprotokoll in das Change-Frame-Artefakt. ARCH-55: Wire-Key und Unterfelder ausschliesslich englisch; verlangt das FK deutsche Wire-Keys, ist das ein **doc-only-Nachzug** (AG3-102), kein deutscher Code. Das **bestehende Freeze-Verhalten** von `ChangeFrame` (FK-23 §23.4.3, owned by AG3-047) wird **nicht** geaendert; das neue Feld ist Teil des immutablen Modells wie die uebrigen Bestandteile.
5. **Tests:** siehe Akzeptanzkriterien — alle Negativ-/Aequivalenz-/Fake-Hub-Pfade.

### 2.2 Out of Scope (mit Owner)
- **Verify-LLM-Transport / Pool-Adapter im Verify-Pfad** — **AG3-065** (FK-11). Diese Story nutzt den Hub fuer Feindesign und ergaenzt nur die schmale read-only `session_stats`-Konsum-Surface; sie baut **nicht** den Verify-Dialogue-Transport/`DialogueRunner`.
- **Free-mode inhaltliche Arbeitsweise des Menschen / reduzierte Prompt-Integrity fuer freie Sub-Agents** (FK-56 §56.1/§56.11) — Harness-/Skill-Territorium, FK zieht hier explizit die Grenze; **kein** Python-Scope.
- **escalation_class-Varianten domain_gap/normative_conflict** (FK-25 §25.3, Klasse-1-LLM-Pfad) — angrenzend, aber nicht Teil dieses Schnitts; nur mitnehmen, wenn trivial, sonst als Folge melden.
- **Multi-LLM-Diskussions-Skill (`llm-discussion`-Bundle)** als Harness-Seite — Katalog-Praesenz bei **AG3-095** (`AG3-095/story.md:73`). AG3-097 verdrahtet den AK3-seitigen Hub-Transport/Feindesign gegen das von AG3-095 katalogisierte Bundle (siehe `depends_on: AG3-095`); AG3-097 baut **nicht** das Bundle, AG3-095 baut **nicht** das Hub-Wiring.
- **Multi-LLM-Hub-Adapter-Kern selbst** (`multi_llm_hub/client.py` Transport/Entities) — bestehend; hier nur konsumiert + um `session_stats` erweitert.
- **Typisierter `escalation_class`-Phase-State-Carrier inkl. `infra_unavailable` (FK-25-Taxonomie / FK-35-Mechanik: `status: PAUSED` + Resume)** — **kein aktueller Story-Owner.** Die FK-25-`escalation_class`-Taxonomie ist FK-25-eigen mit FK-35-Mechanik-Delegation (§25.5.4 Z. 23-25); sie ist **nicht** FK-39-`escalation_reason` (FK-39 §39.2.1 Z. 243 kennt `infra_unavailable` nicht und hat kein `escalation_class`-Feld), also **nicht** Teil von AG3-059. Im `_STORY_INDEX.md` baut **keine** Story den typisierten Carrier (AG3-047 traegt die Geschwister-Klassen als `suggested_reaction`; AG3-085 ist Risikoscore, nicht Carrier). **Gemeldeter offener Gap** an die FK-35-Eskalations-Mechanik-Owner: ein typisierter `escalation_class`-Carrier mit `infra_unavailable`/`scope_explosion`/`impact_exceeded`/`domain_gap`/`normative_conflict` + PAUSED/Resume gehoert in eine eigene FK-35-Story (noch nicht geschnitten). AG3-097 eskaliert bis dahin **konsistent zu AG3-047** ueber den bestehenden ESCALATED-Pfad und baut den Carrier **nicht**.
- **Hook-Send-Count-Sensorik / `*_send`/`llm_send`-PostToolUse-Guard** (Echtzeit-Blockade des 11. Sends) — **kein aktueller Story-Owner.** FK-30-Hook-/Guard-Infrastruktur, aber **keine** `_STORY_INDEX.md`-Story scoped das `*_send`-Send-Count-Enforcement: AG3-086 baut `WebCallBudgetGuard`/`skill_usage_check`/Prompt-Integrity/CCAG-TTL (`AG3-086/story.md:32-47`), **nicht** den `*_send`-Guard; AG3-095 ownt nur die Katalog-Praesenz des Bundles. **Gemeldeter offener Gap** an die FK-30-Hook-/Guard-Owner. AG3-097 enforced die 10-Runden-Grenze nur im Feindesign-Adapter und behauptet **nicht**, eine andere Story liefere die Echtzeit-Hook-Blockade.
- **FK-56-Namespace-/FK-Prosa-Nachzug (doc-only)** — **AG3-102** (PROJECT_STRUCTURE/BC-Registry-Konzept-Nachzug); hier nur der Code-Namespace.

## 3. Akzeptanzkriterien
1. Wird das Integrity-Gate im `ai_augmented`-Modus aufgerufen, wirft es **vor** `integrity_gate_started` eine typisierte `IntegrityGateNotApplicableError`; es werden **keine** `integrity_gate_started`/`integrity_gate_result`-Events emittiert und **keine** Closure-FAIL-Codes produziert (Negativtest: ai_augmented -> Exception, keine Events, kein FAIL-Code).
2. Die Mode-Resolution ist unter einem benannten `operating_mode_resolver`-Namespace verfuegbar und liefert **verhaltensgleich** zur bisherigen Resolution (Aequivalenz-Test fuer `ai_augmented`/`story_execution`/`binding_invalid`); `guard_evaluation` und der Integrity-Gate-Mode-Guard beziehen den Modus von diesem Resolver.
3. Die konkrete `FineDesignEvaluator`-Implementierung gegen `multi_llm_hub` erzwingt **ChatGPT-Pflicht UND einen zweiten Pflicht-Berater** (Qwen bevorzugt, Gemini/Grok als Fallback). Test mit Fake-Hub: fehlt ChatGPT **oder** fehlt jeder zweite Berater -> deterministischer Abbruch (kein Treffen der Klasse-2-Entscheidung).
4. Steht kein zweites LLM zur Verfuegung bzw. ist das Multi-LLM-Quorum nicht erreichbar, signalisiert die Implementierung fail-closed ueber `FineDesignEvaluatorUnavailableError` mit der **typisierten Payload** `escalation_class="infra_unavailable"`, `escalation_reason="Multi-LLM-Quorum nicht erreichbar"`, gewuenschter Phase-State-`status: PAUSED` — eigener Eskalationscode, kein Rueckfall auf `domain_gap`/`normative_conflict` (Test mit Fake-Hub: Error mit exakter Triple-Payload). Die **Eskalation** am Aufruferrand laeuft ueber den **bestehenden** `_escalate`-ESCALATED-Pfad (`HandlerResult.status=ESCALATED` mit `errors`/`suggested_reaction`, `exploration/phase.py:701-723`) — **denselben** Pfad, ueber den AG3-047 die Geschwister-Mandatsklassen eskaliert; die Triple wird dort als typisierte Payload getragen (Test: kein erfundenes Phase-State-Feld, kein neuer `PauseReason`-Wert). AG3-097 fuehrt **keinen** typisierten `escalation_class`-Phase-State-Carrier ein und behauptet **nicht**, eine andere Story (insb. **nicht** AG3-059/FK-39) liefere ein `escalation_class`/`infra_unavailable`-Feld; der typisierte FK-35-Carrier (`status: PAUSED` + Resume) ist ein gemeldeter offener Gap (siehe §2.2).
5. `llm_session_stats`-Post-hoc-Verifikation: die read-only `session_stats`-Konsum-Surface (HubClient-Methode + BFF-GET-Route + typisiertes Modell) existiert; ein akquiriertes LLM mit **0** Antworten fuehrt zu deterministischem Abbruch (fail-closed) (Test).
6. **10-Runden-Grenze im Feindesign-Adapter (buildbar in diesem Cut):** die konkrete Hub-Implementierung sendet **nicht mehr als 10x** pro LLM; der bounded Loop terminiert deterministisch bei Runde 10 (Test mit Fake-Hub: maximal 10 Sends pro LLM, kein 11. Send aus der Implementierung). Die zusaetzliche **Echtzeit-Hook-Blockade des 11. Sends** (`*_send`/`llm_send`-PostToolUse-Guard) ist **NICHT** Teil dieses Cuts und hat **keinen aktuellen Story-Owner** (gemeldeter offener FK-30-Gap, siehe §2.2: weder AG3-086 noch AG3-095 scopen `*_send`-Send-Count-Enforcement); `HookEvent.operation` kennt heute kein `llm_send` (`guard_evaluation.py:28-34`). AG3-097 gibt diese Hook-Sensorik **nicht** vor zu liefern.
7. **Subprozess-Terminierung:** erreicht der bounded Loop Runde 10 ohne Konvergenz, terminiert er und dokumentiert den erreichten Stand als `max_rounds_exceeded` (Test, bestehendes `FineDesignSubprocess`-Verhalten, `fine_design.py:194-198`).
8. **Session-Release-Pruefung:** zeigt die `session_stats`-Auskunft eine nicht korrekt released Session, wird ein Telemetrie-**WARNING** geschrieben (Test); ein korrektes Release erzeugt kein Warning.
9. `ChangeFrame` traegt das optionale achte Feld `fine_design_decisions` (englischer Wire-Key, englische Unterfelder); die `FineDesignDecision`-Liste wird in das Change-Frame-Artefakt persistiert. Das bestehende Freeze-Verhalten (`frozen`/`frozen_at`, AG3-047) bleibt **unveraendert** (Test: Feld vorhanden + serialisierbar + Freeze-Verhalten unveraendert).
10. ARCH-55: das achte Feld, seine Unterfelder und alle neuen Identifier/Enum-Werte sind englisch; `feindesign_entscheidungen` erscheint nur als FK-Konzeptname in Doku/Kommentar, nicht als Code-Key (Test/Review).
11. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage >= 85 %.

## 4. Definition of Done
- AK 1–11 erfuellt; giftige Codex-Review PASS; (Implementierung/Commit erst nach Execution-Plan-Freigabe — diese Story wird zunaechst nur autorisiert/reviewt).

## 5. Guardrail-Referenzen
- **FAIL-CLOSED:** ai_augmented-Integrity-Gate-Ausschluss hart (typisierte Exception vor jedem Event); LLM-ohne-Antwort -> Abbruch; ChatGPT + zweiter Berater verbindlich; Nicht-Erreichbarkeit -> typisiertes `infra_unavailable`-Signal (`FineDesignEvaluatorUnavailableError`-Payload), eskaliert ueber den bestehenden ESCALATED-Pfad (`_escalate`, konsistent zu AG3-047) — **kein** neues Phase-State-Feld, **kein** Fremd-Story-Carrier-Versprechen; der typisierte `escalation_class`-PAUSED-Carrier (FK-35) ist gemeldeter offener Gap. 10-Runden-Grenze im Feindesign-Adapter (Echtzeit-11.-Send-Hook-Blockade = gemeldeter offener FK-30-Gap, kein aktueller Owner).
- **FIX-THE-MODEL / SINGLE SOURCE OF TRUTH:** Mode-Resolution auf einen benannten Resolver konsolidieren statt verstreuter Logik; `fine_design_decisions` ins **eine** Change-Frame-Artefakt, kein paralleles Decision-File.
- **TYPISIERT STATT STRINGS:** `OperatingMode`/`FineDesignDecision`/`IntegrityGateNotApplicableError`/`session_stats`-Modell typisiert; Hub-Verifikation als deterministische Regel, nicht als Docstring-Versprechen.
- **ARCH-55:** der deutsche FK-Konzeptname wird zum englischen Code-Key `fine_design_decisions`; deutsche Keys ggf. doc-only (AG3-102) nachziehen, nicht im Code.
- **SEVERITY-SEMANTIK:** Session-Release-Verletzung ist WARNING (aufschiebend, aber nicht ignorierbar); Quorum-/Antwort-Verletzung ist ERROR (sofort, fail-closed).
- **ZERO DEBT:** keine Docstring-only-Verdrahtung mehr; der Hub-Pfad ist real (Fakes nur an der Hub-/LLM-Grenze im Unit-Test).

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- Drei thematisch verwandte Restpunkte — nicht splitten, aber sauber getrennt halten (Integrity-Mode-Abgrenzung / Resolver-Namespace / Hub-Feindesign + achtes Feld + `session_stats`-Adapter).
- Der `FineDesignEvaluator` ist bereits ein Protokoll (`fine_design.py:102`) — die Hub-Implementierung **adaptiert** es; den bounded Loop (`FineDesignSubprocess`, `fine_design.py:143-198`) **nicht** neu schreiben. Nicht-Erreichbarkeit ueber den bereits existierenden `FineDesignEvaluatorUnavailableError` (`fine_design.py:41`) signalisieren und die FK-25-Triple als typisierte Payload anhaengen. **WICHTIG (Carrier-Abgrenzung):** der reale `PhaseState` hat **kein** `escalation_class`/`escalation_reason` und `PauseReason` kein `infra_unavailable` (`models.py:436-479`, `pause_reason.py:46-60`). Die FK-25-`escalation_class`-Taxonomie (inkl. `infra_unavailable`) ist FK-25-eigen mit FK-35-Mechanik-Delegation — **nicht** FK-39-`escalation_reason` und damit **nicht** Teil von AG3-059; **keine** `_STORY_INDEX.md`-Story baut den typisierten Carrier. Am Aufruferrand (Exploration-Phase-Handler, `_escalate`, `exploration/phase.py:701-723`) ueber den **bestehenden** ESCALATED-Pfad (`HandlerResult.errors`/`suggested_reaction`) mit Triple-Payload eskalieren — **genau wie AG3-047** die Geschwister-Mandatsklassen (`AG3-047/story.md:157-163`). **Keine** eigenen Phase-State-Felder oder `PauseReason`-Werte einfuehren und **nicht** vorgeben, eine andere Story liefere einen `infra_unavailable`-Carrier; der typisierte FK-35-Carrier ist als offener Gap zu melden.
- **Send-Count-Grenze:** im Feindesign-Adapter durchsetzen (max 10 Sends pro LLM, dann `max_rounds_exceeded`). Die **Echtzeit-Hook-Blockade des 11. Sends** ist **NICHT** Teil dieser Story — `HookEvent.operation` kennt heute kein `llm_send` (`guard_evaluation.py:28-34`), es gibt keinen produktiven Send-Count-Guard. **Kein** `_STORY_INDEX.md`-Owner scoped das `*_send`-Send-Count-Enforcement (AG3-086 baut es nicht, `AG3-086/story.md:32-47`; AG3-095 ownt nur Katalog-Praesenz, `AG3-095/story.md:73`) — als offener FK-30-Gap melden. Nicht selbst bauen, nicht vorgeben zu liefern.
- `ChangeFrame` ist `frozen=True` (`change_frame.py:283`) — das achte Feld als optionales Feld ergaenzen; das bestehende Freeze-Verhalten **nicht** aendern (es existiert bewusst **keine** `frozen`/`frozen_at`-Konsistenz-Invariante, `change_frame.py:277-280` — diese Invariante also auch **nicht** neu einfuehren).
- `FineDesignDecision` (`fine_design.py:56`) hat bereits englische Keys — als Subfeld-Typ des achten Felds wiederverwenden, nicht duplizieren. Wire-Key des Container-Felds: `fine_design_decisions` (englisch). FK-25 nennt das Feld deutsch (`feindesign_entscheidungen`); der Code-Key ist englisch — verlangt das FK deutsche Wire-Keys, ist das ein doc-only-Nachzug (AG3-102), kein deutscher Code.
- `session_stats`: read-only Konsum-Surface ergaenzen — HubClient-Methode (`multi_llm_hub/client.py`, neben `resume`), passende BFF-GET-Route (`multi_llm_hub/http/routes.py`) und ein typisiertes Pydantic-Antwortmodell (pro LLM: Message-Count, ob geantwortet, Session-/Release-Status). Read-only, keine Enforcement-Logik im Adapter. Fakes nur an der Hub-Grenze im Unit-Test (MOCKS-Ausnahme).
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- **Cross-Story-Lage (`depends_on` vs. gemeldete offene Gaps):** Echte Voraussetzung ist **AG3-095** (Katalog-Praesenz des `llm-discussion`-Bundles, das der AG3-097-Hub-Transport konsumiert; `AG3-095/story.md:73`) — in `depends_on`. **NICHT** als Voraussetzung gefuehrt: ein FK-39/AG3-059-Carrier fuer `infra_unavailable` (existiert dort nicht; FK-39 §39.2.1 hat `infra_unavailable` nicht und kein `escalation_class`-Feld) und ein AG3-086-`*_send`-Hook-Guard (AG3-086 scoped ihn nicht). Beide nicht-buildbaren Surfaces — der typisierte FK-35-`escalation_class`-PAUSED-Carrier und der FK-30-`*_send`-Send-Count-Hook — haben **keinen aktuellen Story-Owner** und sind als **offene Gaps** an die jeweiligen FK-35-/FK-30-Owner zu melden. AG3-097 konsumiert sie **nicht** (es gibt nichts zu konsumieren) und gibt **nicht** vor, dass eine andere Story sie liefert; es eskaliert die Nicht-Erreichbarkeit konsistent zu AG3-047 ueber den bestehenden ESCALATED-Pfad und enforced die Rundengrenze adapterseitig.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Test-Namen (ai_augmented-Integrity-Ausschluss, ChatGPT+zweiter-Berater-mandatory, `infra_unavailable`-Signal-Payload, session_stats-Abbruch, 10-Runden-Adapter-Grenze, achtes-Feld-Persistenz).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

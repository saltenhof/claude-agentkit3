story_id: AG3-114
title: "§91.1a Agent-Surface fuer Story-Create: ProjectEdgeClient.create_story + Target-Project-Tool-Entry gegen POST /v1/stories (FK-91 §91.1a, FK-21 ReconciliationEvidence) — Ersatz fuer gh issue create"
type: implementation
status: completed
phase: closure
completed: "2026-06-12"
# COMPLETED 2026-06-12. R1 (typed evidence, correlation header, stable error contract, op_id) +
# resume (wired the real AG3-115 CreateTimeConflictAdjudicator over the FK-65 HubLlmClient, role->pool
# via pipeline.llm_roles.story_creation_review) + R2/R3 remediation: create_story drives reconcile_only
# INSIDE the boundary from a single CreateStoryInputs (no forgery seam); posts to the tenant-scoped
# /v1/projects/{project_key}/stories (the route ControlPlaneApplication actually exposes); X-Correlation-Id
# case-insensitive on the server; stable tool error contract (vectordb_unavailable / transport_error /
# conflict_adjudication_unavailable); StructuredEvaluatorError fails closed. E2E NO-STUB routes through the
# REAL ControlPlaneApplication router. Codex (sole gate): APPROVE-WITH-NITS. Gates green (1016 passed,
# mypy 695, ruff, GAC-1). Residual (foreign owner): persisting the §21.4.2 counters as Story columns is
# story_context_manager scope (follow-up). .mcp.json kept out of the commit.
created: "2026-06-12"
size: L
# Herkunft: PO-Entscheidung 2026-06-12. FK-91 §91.1a Regel #9: Stories werden ausschliesslich
# ueber die Control-Plane-API angelegt; GitHub ist nie Story-Wahrheit. Regel #3: Agents nutzen
# ausschliesslich den ProjectEdgeClient gegen die REST-API (nicht CLI, nicht curl). Ist-Zustand:
# POST /v1/stories existiert + erzwingt ReconciliationEvidence (AG3-068), der ProjectEdgeClient
# ist gebaut + als Target-Project-Tool deployt (tools/agentkit/projectedge.py) — aber der Client
# hat KEIN create_story und das Tool keinen create-story-Entry. Diese Story schliesst die Luecke,
# damit der create-userstory-Skill Stories nativ ueber den Client anlegt statt via gh issue create.
# Entblockt AG3-113 (Bundle-Re-Cut), das wiederum AG3-111 entblockt.
depends_on:
  - AG3-014
  - AG3-068
  - AG3-090
  - AG3-115
unblocks:
  - AG3-113

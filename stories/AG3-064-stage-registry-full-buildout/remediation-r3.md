# AG3-064 — Remediation R3 (response to round-3 re-review `review-r3.md`)

**Outcome:** the single remaining round-3 must-fix ERROR is resolved in `story.md` via the reviewer's **Option A** (preferred — no new story). The Bugfix-Red-Green Layer-1 checks are now genuinely in AG3-064's scope as **complete checks** (registry entry **and** deterministic check-body wiring + ACs + tests), not just registry naming. Only `story.md` and this report were written; `status.yaml` was re-checked and needed no change (its `title` already carries "Bugfix-Red-Green" and the change does not alter `type`/`size`/`status`/`phase`/deps — the cut grows within L, no field is genuinely wrong). No production code, tests, or `concept/` files were touched.

The R3 anchors were re-verified against the real source before rewriting (see "Code anchors re-verified" below). The decisive finding: **no bugfix check body exists anywhere in production code**, and the structural dispatch invariant makes registration-only non-viable — so Option A is not just preferred, it is the only technically sound cut.

---

## Round-3 finding resolution

| # | Round-3 finding (review-r3.md) | Severity | Resolution in story.md |
|---|---|---|---|
| 1 | **Bugfix-Red-Green body wiring still excluded** while `_STORY_INDEX.md:159` assigns Bugfix-Red-Green to AG3-064. The story still excluded the real body (`story.md` old §2.1-6 / Out-of-Scope / AC5 / Hints), AC5 only checked registration, and the "fehlende Bodies melden" wording did not satisfy the R2 must-fix. FK-33:390-398 lists the five checks as BLOCKING; the gap analysis describes Reproducer/Red/Green/Suite/Structural substance, not just naming. | ERROR | **Resolved via Option A — full check brought into scope.** §2.1-6 rewritten from "register only, body excluded, report if missing" to a concrete **end-to-end** Layer-1 check spec: (a) five `bugfix.*` Stage-Definitions; (b) new `BugfixEvidencePort` + `BugfixEvidence` + `ABSENT_BUGFIX_EVIDENCE_PORT` in a new `structural/checks/bugfix_checks.py`, **exactly in the existing `build_test_checks.py:43-93` port pattern** (port-based, no subprocess in the aggregation, fail-closed); (c) five `check_bugfix_*` functions with the uniform `(ctx, story_dir, *, severity, port)` signature carrying the FK-26 §26.9 / FK-33 §33.3.2 body semantics (Reproducer manifest fields, Red `exit!=0`, Green `exit==0`, Suite `exit==0`, consistency = same command / different commits); (d) dispatch entries for all five `bugfix.*` ids in `StructuralChecker._dispatch` + a new `bugfix_port` init param, satisfying the `_run_stage` dispatch invariant. **AC5 rewritten** into 5a-5e: registration, body pass-path, body negative-paths (each BLOCKING), fail-closed-without-port, and the end-to-end dispatch invariant (`evaluate()` for a BUGFIX story raises no `KeyError`, yields the five BLOCKING findings on violated evidence; IMPLEMENTATION does not run them). **Out-of-Scope** carve-out narrowed to only the productive subprocess adapter behind the port (analogous to the productive `BuildTestEvidencePort` adapter), explicitly keeping the check logic In Scope. Quell-Konzepte gains the FK-26 §26.9 anchor; Ist-Zustand documents that no bugfix body exists today and that the dispatch invariant (`checker.py:350-357`, `KeyError` on undispatched stage) makes registration-only crash `evaluate()` for BUGFIX stories — i.e. registration-only is not acceptance-fit. ZERO DEBT guardrail + Hints + "done"-evidence line updated to the full check (no more "Body spaeter / als Folge-Einheit melden"). The `_STORY_INDEX.md:159` quote is reframed: "mitgezogen" = the **whole** Layer-1 check, and there is no separate Bugfix-Red-Green story, so the body has no other owner. |

---

## Why Option A (not a new story / index change)

- The reviewer named Option A as preferred (avoids a new story).
- `_STORY_INDEX.md:159` already assigns the Red-Green suite to AG3-064 ("wird dort mitgezogen") and **no separate Bugfix-Red-Green story exists** in the index — routing the body elsewhere would require inventing a new story, which Option A explicitly avoids.
- The structural dispatch invariant (`structural/checker.py:350-357`) raises `KeyError` for any registered Layer-1 stage without a dispatch entry ("every Layer-1 stage MUST be dispatched — ZERO DEBT, no dead stage"). Registering the five `bugfix.*` stages **without** bodies would hard-crash `StructuralChecker.evaluate` for every BUGFIX story. Registration-only is therefore not a viable cut, independent of the index wording.
- The body fits the established pattern with no new architecture: `BuildTestEvidencePort` (`build_test_checks.py`) is the exact template — port + dataclass + fail-closed absent port + pure check functions, with the productive subprocess adapter living at the composition root. The bugfix port mirrors it 1:1, so only the productive subprocess adapter remains out of scope (same boundary as build/test).

## Code anchors re-verified for this round

- `concept/technical-design/33_...md:390-398` — FK-33 §33.3.2 lists the five BLOCKING bugfix checks (`reproducer_manifest`/`red_evidence`/`green_evidence`/`suite_evidence`/`red_green_consistency`). → finding #1 body semantics.
- `concept/technical-design/26_implementation_runtime_worker_loop.md:737-767` — FK-26 §26.9 Red-Green-Suite + §26.9.2 reproducer JSON (`bug_description`/`stack`/`test_locator.nodeid`/`expected_failure`) + the structural-check validation note (Red `exit!=0`, Green `exit==0`, Suite `exit==0`, same command / different commits). → finding #1 body semantics; new Quell-Konzepte anchor.
- `var/concept-gap-analysis/_STORY_INDEX.md:159` — "Bugfix-Red-Green-Suite ist Layer-1-Check und wird dort mitgezogen" (AG3-064); no separate bugfix story in the index. → finding #1 ownership.
- `src/agentkit/verify_system/structural/checks/` — directory listing: `are_gate`/`artifact_checks`/`branch_checks`/`build_test_checks`/`hygiene_checks`/`impact_violation`/`meta_checks`/`recurring_guards`; **no** `bugfix_*` check (grep `bugfix` over `structural/` = 0 hits). The only `bugfix-reproducer` mention is prose in a skill bundle (`resources/skill_bundles/.../SKILL.md`). → Ist-Zustand: no body exists today.
- `src/agentkit/verify_system/structural/checker.py:350-357` — `_run_stage` raises `KeyError` for any registered stage without a dispatch entry. → registration-only crashes `evaluate()`.
- `src/agentkit/verify_system/structural/checker.py:382-456` — `_dispatch` table (closure-bound ports, e.g. `bt`/build-test); the five `bugfix.*` entries + a `bugfix_port` init param go here. → finding #1 dispatch wiring.
- `src/agentkit/verify_system/structural/checks/build_test_checks.py:43-93` — `BuildTestEvidence` + `BuildTestEvidencePort` + `ABSENT_BUILD_TEST_PORT` (port pattern, productive subprocess adapter at the composition root). → exact template for `BugfixEvidencePort`.

## Scope discipline

Stayed within AG3-064's cut per `_STORY_INDEX.md:55`/`:159` (registry buildout + override config stanza + fail-open warning consumption + the Bugfix-Red-Green Layer-1 check). The body addition is the minimal change to make the Layer-1 check the index already assigned here actually executable — it adds one check module + dispatch wiring + tests in the established `build_test_checks` pattern, no new BC and no new vocabulary. No other story's scope was pulled in; the productive subprocess adapter behind the port stays at the composition-root/worker-runtime seam (same boundary as `BuildTestEvidencePort`). The other body-level carve-outs (context_sufficiency builder → AG3-067, adversarial runtime → AG3-079, doc_fidelity_impl logic → AG3-063, concept/research subcheck bodies → dedicated check story) are unchanged; the FK-prose `id`→`stage_id` alignment remains doc-only AG3-102.

## Files written
- `stories/AG3-064-stage-registry-full-buildout/story.md` (Quell-Konzepte + Ist-Zustand + §2.1-6 + §2.2 Out-of-Scope + AC5 + ZERO-DEBT guardrail + Hints + "done"-evidence line rewritten for the full Bugfix-Red-Green check; AG3-057 template structure preserved: Typ/Groesse/BC/Quell-Konzepte → §1 Kontext → §2 Scope (In/Out) → §3 AC → §4 DoD → §5 Guardrails → §6 Hints).
- `stories/AG3-064-stage-registry-full-buildout/remediation-r3.md` (this report).
- `stories/AG3-064-stage-registry-full-buildout/status.yaml` — **not modified** (re-checked; `title` already carries "Bugfix-Red-Green", `type`/`size: L`/`status`/`phase`/deps unchanged by an in-cut scope growth; no field genuinely wrong).

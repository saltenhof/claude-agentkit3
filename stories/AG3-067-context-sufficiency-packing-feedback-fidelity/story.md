# AG3-067: ContextSufficiencyBuilder + Section-aware Packing + feedback_fidelity (Ebene 4)

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `verify-system` / Verify-Context & QA-Bundle-Vorbereitung (BC3) — der Pre-Layer-2-Schritt (Kontext-Hinlaenglichkeit), das section-aware Packing des Layer-2-Bundles und der post-merge Rueckkopplungstreue-Evaluator (Ebene 4).
**Quell-Konzepte (autoritativ):**
- `FK-37 §37.1` — sechsfeldriges `ContextBundle` (story_spec, diff_summary, concept_excerpt, handover, arch_references, evidence_manifest)
- `FK-37 §37.2` (`§37.2.2`/`§37.2.4` Loader-Vollstaendigkeit als Invariante REF-035) — `ContextSufficiencyBuilder`/`SufficiencyLevel`, vier Builder-Loader (`_load_story_spec`/`_load_handover`/`_load_concept_excerpt`/`_load_arch_references`) plus zwei caller-seitige Bezugswege (`diff_summary`/`evidence_manifest` aus `context.json`), `context_sufficiency.json`, Pre-Step vor Layer 2
- `FK-37 §37.3` — Section-aware Packing (`pack_markdown`/`pack_code`, `_pack_and_convert()`, `truncate_bundle()`-Dispatcher, `BUNDLE_TOKEN_LIMIT` 32000, konfigurierbar in `pipeline.yaml`)
- `FK-38 §38.3` — Dokumententreue Ebene 4 (Rueckkopplungstreue) nach Merge, vor Postflight; prompt `doc-fidelity-feedback.md`, expected_checks `feedback_fidelity`; FAIL = Warnung + Failure-Corpus-Incident-Kandidat
- `FK-38 §38.1.4` — Mandatory-Target-Rueckkopplung (`adversarial_mandatory_target`-Findings aus `mandatory_target_results`; Status nicht in (`TESTED`,`UNRESOLVABLE`) → FAIL)

---

## 1. Kontext / Ist-Zustand (belegt)

Der gesamte Pre-Layer-2-Apparat (Sufficiency/Packing) fehlt, das Layer-2-Textinput-Modell (`Layer2ReviewInput`, FK-27 §27.5.2) ist vierfeldrig (story_spec/diff_summary/concept_excerpt/handover), und der reale Ebene-4-Evaluator/Prompt hinter dem bereits verdrahteten Closure-Port fehlt. Belegt (gap-fk-36-45.md §FK-37/§FK-38):

- `ContextSufficiencyBuilder`/`SufficiencyLevel`: Glob `**/context_sufficiency*.py` → keine Datei; `src/agentkit/verify_system/llm_evaluator/bundle.py:9-16` deklariert Builder + Section-aware-Packing + EvidenceAssembler explizit als „out of scope for this first wave“.
- Section-aware Packing: Glob `**/packing*.py` → keine Datei; statt `pack_markdown`/`pack_code`/`truncate_bundle` (BUNDLE_TOKEN_LIMIT 32000) implementiert `bundle.py` nur eine simple Byte-Trunkierung (`_truncate_text`, `MAX_DIFF_CONTENT_BYTES=100KB`, `MAX_BUNDLE_TOTAL_BYTES=200KB`).
- `ContextBundle` (sechs Felder): das produktive Layer-2-Eingabemodell ist `ReviewBundle` (`src/agentkit/verify_system/llm_evaluator/bundle.py:44-69`, acht Felder: story_id, story_brief_excerpt, acceptance_criteria, diff_summary, diff_content, concept_refs, previous_findings, qa_cycle_round) — die FK-37-`ContextBundle`-Kontextfelder `arch_references` und `evidence_manifest` fehlen als getypte Bundle-Felder. (`ReviewBundle` traegt neben den Kontextfeldern bereits operative Metadaten — story_id, acceptance_criteria, previous_findings, qa_cycle_round.)
- Ebene 4 (`feedback_fidelity`/`doc-fidelity-feedback.md`): Closure-Port und Aufruf existieren bereits — `DocFidelityFeedbackPort.evaluate_feedback_fidelity` (`src/agentkit/closure/post_merge_finalization/finalization.py:63-67`) wird in `run_post_merge_finalization` als Schritt 6 vor Postflight aufgerufen (`:147-150`), und ein produktiver, nicht-blockierender Stub-Port liefert ihn (`src/agentkit/closure/runtime_ports.py:208-218`). Was fehlt, ist der echte post-merge-Evaluator und der Prompt `doc-fidelity-feedback.md` (expected_check `feedback_fidelity`) hinter diesem Port — der Stub gibt jede Runde nur eine Warnung „kein produktiver feedback-Callable" zurueck.
- Mandatory-Target-Rueckkopplung (`mandatory_target_results` → `feedback.json`): die FAIL-Einspeisung ab Runde 2 fehlt. Das reale `Finding` (`src/agentkit/verify_system/protocols.py:206-213`) hat KEIN `source`-Feld, sondern `layer`/`check`/`severity`/`message`/`trust_class`/`file_path`/`line_number`/`suggestion`; `adversarial_mandatory_target` existiert nur in FK-38 §38.1.4-Pseudocode (`source`/`check_id`/`status`) und im Glossar, nicht im Code. Das Mapping auf das reale Modell ist Teil dieser Story (siehe AC8).

**Konfliktcheck (Kontext-Sinnhaftigkeit):** `ReviewBundle`/`build_review_bundle` (`src/agentkit/verify_system/llm_evaluator/bundle.py:130`) ist die existierende Layer-2-Maschinerie; sie wird im realen Layer-2-Pfad verdrahtet: `run_layer2_llm` ruft `build_review_bundle` (`src/agentkit/verify_system/llm_evaluator/layer2_integration.py:87-93`), und `VerifySystem` geht ueber `run_layer2_llm_failclosed` (`src/agentkit/verify_system/system.py:1653-1660`). Diese Story erweitert das Packing und Bundle **dort** — der Sufficiency-Pre-Step und das section-aware Packing werden **vor** `runner.run(...)` im realen Layer-2-Pfad (`layer2_integration.py`/`run_layer2_llm`) verdrahtet, ohne einen zweiten Bundle-Builder zu schaffen. Die FK-37-`ContextBundle`-Naming-Drift gegenueber dem realen `ReviewBundle` ist bewusst (bc-cut-decisions, AG3-101 doc-only) — diese Story zieht **kein** Konzept nach, sondern fuegt die fehlenden getypten Felder (`arch_references`, `evidence_manifest`) hinzu und implementiert das FK-37-Packing. `context_sufficiency` ist als **Stage** in AG3-064 registriert; diese Story liefert den **Builder/Producer** der `context_sufficiency.json`-Artefakte (Schnitt: Stage dort, Builder hier). Ebene 4 (`feedback_fidelity`) wird hinter dem vorhandenen Closure-`DocFidelityFeedbackPort` verdrahtet. Die gemeinsame Fassade ist `ConformanceService.check_fidelity(level="feedback", evaluator=..., context=...)` (FK-32 §32.3, expected_checks `[f"{level}_fidelity"]`) — **Owner der Fassade ist AG3-063**. Da AG3-063 im `_STORY_INDEX.md` nicht als `depends_on` von AG3-067 gefuehrt ist (autoritativ: AG3-043/AG3-053/AG3-061), wird Ebene 4 hier gegen einen **Port/Adapter** programmiert (den vorhandenen `DocFidelityFeedbackPort`), nicht gegen eine harte AG3-063-Voraussetzung: faellt der `ConformanceService` aus AG3-063 ein, wird er hinter dem Port verdrahtet; faellt er noch nicht ein, liefert diese Story die `feedback`-spezifische post-merge-Evaluator-Logik (Prompt + expected_check `feedback_fidelity`) direkt hinter dem Port. In keinem Fall entsteht eine zweite Doc-Fidelity-Logik. Mandatory-Target-Rueckkopplung (FK-38 §38.1.4) ist Federfuehrung dieser Story.

## 2. Scope

### 2.1 In Scope
1. **`ContextSufficiencyBuilder` + `SufficiencyLevel`** (FK-37 §37.2): Pre-Step **vor** Layer 2; alle sechs `ContextBundle`-Felder erhalten einen kanonischen Bezugsweg (Loader-Vollstaendigkeit als Invariante, FK-37 §37.2.2 REF-035): vier dedizierte Builder-Loader `_load_story_spec` (`{story_dir}/story.md`), `_load_handover` (`{story_dir}/handover.json`), `_load_concept_excerpt` (`concept_paths` aus `context.json`), `_load_arch_references` (`concept_paths`/`external_sources` aus `context.json`); plus die zwei caller-seitigen Bezugswege `diff_summary` und `evidence_manifest`, die aus `context.json` uebernommen und in den Builder eingespeist werden (kein eigener Loader, FK-37 §37.2.2-Tabelle). Bewertet die Kontext-Hinlaenglichkeit (`SufficiencyLevel`, englische Werte gem. FK: `sufficient`/`reviewable_with_gaps`/`partially_reviewable`); persistiert `context_sufficiency.json` (Producer `qa-context-sufficiency`/ArtifactClass gem. FK §37.2.5). Speist die fail-open Policy-Warning aus AG3-064 (FK-33 §33.7.4).
2. **Section-aware Packing** (FK-37 §37.3): `pack_markdown`/`pack_code`, `_pack_and_convert()`, `truncate_bundle()`-Dispatcher; `BUNDLE_TOKEN_LIMIT` (Default 32000) konfigurierbar in `pipeline.yaml`. Ersetzt die simple Byte-Trunkierung in `bundle.py` (kein zweiter Mechanismus daneben) bzw. integriert sauber, mit dokumentiertem Kuerzungsprotokoll (FK-11 §11.4.5-konform).
3. **Sechsfeldriges Bundle** (FK-37 §37.1): die fehlenden getypten Kontextfelder `arch_references` und `evidence_manifest` werden am Layer-2-Eingabemodell (`ReviewBundle`/`build_review_bundle`) ergaenzt — d. h. die sechs **semantischen `ContextBundle`-Kontextfelder** zusaetzlich zu den bestehenden operativen `ReviewBundle`-Metadaten (story_id, acceptance_criteria, previous_findings, qa_cycle_round); `evidence_manifest` bezieht sich auf das `BundleManifest` aus AG3-061 (gegen dessen Typ programmieren).
4. **Ebene-4-Evaluator `feedback_fidelity`** (FK-38 §38.3): post-merge, vor Postflight; Prompt `doc-fidelity-feedback.md` (context `final_diff`/`existing_docs`); FAIL = **Warnung + Failure-Corpus-Incident-Kandidat** (nicht-blockierend, Severity-Semantik beachten). Verdrahtet hinter dem vorhandenen Closure-`DocFidelityFeedbackPort` (`finalization.py:63-67`). Fassade ist `ConformanceService.check_fidelity(level="feedback", evaluator=..., context=...)` (FK-32 §32.3, expected_check `feedback_fidelity`) — Owner AG3-063, hier gegen Port/Adapter abstrahiert (kein hartes AG3-063-`depends_on`; siehe §2.2).
5. **Mandatory-Target-Rueckkopplung** (FK-38 §38.1.4): Findings aus `mandatory_target_results` mit Status nicht in (`TESTED`,`UNRESOLVABLE`) → FAIL, eingespeist in `feedback.json`/`RemediationFeedback` ab Runde 2. Mapping auf das **reale** `Finding`-Modell (`protocols.py:206-213`), NICHT auf den FK-38-Pseudocode (`source`/`check_id`/`status` existieren im Code nicht): `layer="adversarial"`, `check=<target_id>`, `severity=Severity.BLOCKING`, `trust_class` der Adversarial-Quelle, `message` mit dem unerfuellten Target. Die FAIL-Findings landen als `blocking_findings` in `RemediationFeedback` (`remediation/feedback.py:25`). (Die Adversarial-Runtime, die `mandatory_target_results` **erzeugt**, ist AG3-079; hier nur der Konsum/die Rueckkopplung — gegen das Artefakt-Schema `adversarial.json` abstrahieren.)
6. **Negativpfade** als Tests: unzureichender Kontext → `SufficiencyLevel != sufficient` + Policy-Warning; oversized Bundle → section-aware Packing kuerzt deterministisch mit Protokoll (kein stiller Cut); Ebene-4-FAIL → Warnung + Incident-Kandidat (keine Closure-Blockade); Mandatory-Target nicht `TESTED`/`UNRESOLVABLE` → FAIL ab Runde 2.

### 2.2 In/Out-Abgrenzung (mit Owner)
- **`context_sufficiency`-Stage-Registrierung + fail-open Policy-Warning-Pfad** — **AG3-064** (FK-33). Hier wird der Builder/Producer geliefert und der Warning **gespeist**; die Stage/Engine-Verdrahtung kommt aus AG3-064.
- **`ConformanceService.check_fidelity(level, evaluator, context)`-Fassade + Manifest-Indexer + 3-Tier-Size-Control** — **AG3-063** (FK-32 §32.3). Ebene 4 wird hinter diese Fassade gehaengt (Aufruf mit `level="feedback"`); AG3-063 ist **nicht** als hartes `depends_on` von AG3-067 gefuehrt (autoritativ `_STORY_INDEX.md`: AG3-043/AG3-053/AG3-061), daher abstrahiert diese Story gegen den vorhandenen `DocFidelityFeedbackPort`-Adapter statt gegen die AG3-063-Klasse. Die `feedback`-spezifische post-merge-Logik + Mandatory-Target-Rueckkopplung ist hier.
- **EvidenceAssembler/`BundleManifest`** (Quelle von `evidence_manifest`) — **AG3-061** (FK-28).
- **Adversarial-Runtime / `adversarial.json` / `mandatory_target_results`-Erzeugung** — **AG3-079** (FK-48). Hier nur konsumiert.
- **FK-37/FK-38-Naming-Prosa (`ContextBundle`/`VerifyContext`)** — doc-only **AG3-101**; hier KEINE Konzept-Aenderung.

## 3. Akzeptanzkriterien
1. `ContextSufficiencyBuilder` + `SufficiencyLevel` existieren; der Builder laeuft als Pre-Step vor Layer 2, nutzt die vier Builder-Loader (`_load_story_spec`/`_load_handover`/`_load_concept_excerpt`/`_load_arch_references`) und persistiert `context_sufficiency.json` (Test inkl. Producer `qa-context-sufficiency`/ArtifactClass).
2. **Sechs-Feld-Bezugswege vollstaendig (FK-37 §37.2.2 REF-035):** `diff_summary` und `evidence_manifest` werden caller-seitig aus `context.json` uebernommen, an den Builder eingespeist und bewertet; fehlt der caller-seitige Bezug, wird das Feld als `missing` klassifiziert (Negativtest je Feld: vorhanden → `present`, caller-seitig nicht eingespeist → `missing`, obwohl Daten auf Disk laegen).
3. `SufficiencyLevel != sufficient` speist eine fail-open Policy-Warning (Integrationstest gegen den AG3-064-Warning-Pfad; bei fehlender Stage gegen das Artefakt-Schema testen) — keine Blockade. Trunkierung allein → `reviewable_with_gaps`; fehlende Pflichtfelder → `partially_reviewable` (FK-37 §37.2.3 Klassifikationsregel).
4. Section-aware Packing: `pack_markdown`/`pack_code`/`truncate_bundle` implementiert; `BUNDLE_TOKEN_LIMIT` (Default 32000) aus `pipeline.yaml` konfigurierbar; ein oversized Bundle wird deterministisch mit dokumentiertem Kuerzungsprotokoll gekuerzt (Tests: Markdown- und Code-Packing, Limit-Kuerzung).
5. Das Layer-2-Bundle traegt die getypten Kontextfelder `arch_references` und `evidence_manifest` (die sechs semantischen `ContextBundle`-Kontextfelder gem. FK-37 §37.1, zusaetzlich zu den bestehenden operativen `ReviewBundle`-Metadaten); die simple Byte-Trunkierung (`_truncate_text`/`MAX_*_BYTES`) ist durch das section-aware Packing abgeloest (kein zweiter Trunkier-Mechanismus daneben) (Test).
6. **Realer Layer-2-Einbaupunkt:** Sufficiency-Pre-Step + section-aware Packing sind **vor** `runner.run(...)` im realen Layer-2-Pfad verdrahtet (`llm_evaluator/layer2_integration.py` `run_layer2_llm:87-93`, erreicht ueber `system.py` `run_layer2_llm_failclosed:1653-1660`) — nicht nur isoliert in `bundle.py` (Test/Assertion gegen den realen Aufrufpfad).
7. Ebene-4-`feedback_fidelity`: post-merge Evaluator laeuft hinter `DocFidelityFeedbackPort.evaluate_feedback_fidelity` (Schritt 6, `finalization.py:147-150`), vor Postflight; FAIL → Warnung + Failure-Corpus-Incident-Kandidat, **nicht** blockierend (Test: FAIL blockiert Closure nicht, erzeugt aber Incident-Kandidat).
8. Mandatory-Target-Rueckkopplung gegen das **reale** `Finding`-Modell: Targets mit Status nicht in (`TESTED`,`UNRESOLVABLE`) → ein `Finding(layer="adversarial", check=<target_id>, severity=Severity.BLOCKING, …)` als `blocking_findings` in `RemediationFeedback`/`feedback.json` ab Runde 2; `TESTED`/`UNRESOLVABLE` → kein Finding (zwei Tests, Assertion auf die realen `Finding`-Felder, nicht auf `source`/`check_id`/`status`).
9. Kein zweiter Bundle-Builder/Trunkier-Pfad: kein neues produktives `*bundle*builder*`/`*pack*`-Duplikat neben `build_review_bundle`; `ReviewBundle`/`build_review_bundle` wird erweitert, und der reale Layer-2-Pfad (AC6) nutzt genau den erweiterten Builder (Test: Modul-/Symbol-Assertion + Aufrufpfad-Assertion, kein freitextliches „Review").
10. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage ≥ 85 %.

## 4. Definition of Done
- AK 1–10 erfuellt; giftige Codex-Review PASS; Implementierung/Commit erst nach Execution-Plan-Freigabe.

## 5. Guardrail-Referenzen
- **FAIL CLOSED / fail-open korrekt unterschieden:** Kontext-Hinlaenglichkeit ist fail-**open** (Warning, FK-33 §33.7.4); Ebene-4-FAIL ist nicht-blockierend (Warnung + Incident-Kandidat, FK-38 §38.3); Packing kuerzt nie still (dokumentiertes Protokoll).
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** ein Layer-2-Bundle-Builder (`ReviewBundle` erweitert), ein Packing-Mechanismus, kein paralleles Trunkieren; Ebene 4 hinter der `ConformanceService`-Fassade, nicht als zweite Doc-Fidelity-Logik.
- **TYPISIERT STATT STRINGS:** `SufficiencyLevel`-Enum; getypte Bundle-Felder; typisierter Mandatory-Target-Status.
- **ARCH-55:** alle Identifier/Enum-Werte/Wire-Keys/Dateinamen/Config-Keys englisch.
- **SEVERITY-SEMANTIK:** der Ebene-4-Warning/Incident-Kandidat wird aktiv erzeugt, nicht still verschluckt.
- **ZERO DEBT:** Sufficiency + Packing + Ebene 4 + Mandatory-Target real; kein „first wave OOS“ mehr.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- `ReviewBundle`/`build_review_bundle` (`llm_evaluator/bundle.py:44`/`:130`) **erweitern**, nicht duplizieren; die simple `_truncate_text`/`MAX_*_BYTES`-Trunkierung (`bundle.py:109`/`:36-39`) durch das FK-37-Packing ersetzen (oder sauber integrieren) — kein zweiter Trunkier-Pfad. Den Pre-Step/Packing im realen Layer-2-Pfad verdrahten (`layer2_integration.py:87-93`), nicht nur in `bundle.py`.
- `context_sufficiency` ist die **Stage** in AG3-064; hier den **Builder/Producer** liefern und den fail-open Warning speisen. Schnitt sauber halten; bei fehlender Stage gegen das Artefakt-Schema arbeiten und melden.
- Ebene 4 hinter den vorhandenen Closure-`DocFidelityFeedbackPort` (`closure/post_merge_finalization/finalization.py:63-67`, aufgerufen `:147-150`; produktiver Stub-Port `closure/runtime_ports.py:208-218`) und die Fassade `ConformanceService.check_fidelity(level="feedback", evaluator, context)` (FK-32 §32.3, AG3-063) haengen — gegen Port/Adapter abstrahieren (AG3-063 ist KEIN hartes `depends_on`), keine dritte Doc-Fidelity-Implementierung.
- Mandatory-Target-Findings auf das **reale** `Finding` (`verify_system/protocols.py:206-213`) mappen (`layer`/`check`/`severity`/`message`/`trust_class`), NICHT auf den FK-38-Pseudocode (`source`/`check_id`/`status`); `mandatory_target_results` wird von AG3-079 erzeugt, hier nur konsumieren — gegen das Artefakt-Schema (`adversarial.json`) abstrahieren, nicht die Adversarial-Runtime mitbauen.
- `evidence_manifest`-Feld bezieht das `BundleManifest` aus AG3-061; gegen dessen Typ programmieren.
- FK-37/FK-38-Naming-Drift (`ContextBundle`/`VerifyContext`) ist bewusst superseded — **keine** Konzept-Prosa aendern (das ist doc-only AG3-101).
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done“ nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Testnamen (Sufficiency inkl. caller-seitige `diff_summary`/`evidence_manifest`-Bezugswege, pack_markdown/pack_code/truncate, 6-Feld-Bundle, realer Layer-2-Einbaupunkt, Ebene-4-Warning, Mandatory-Target gegen reales `Finding`).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

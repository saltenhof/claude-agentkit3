story_id: AG3-148
title: "Transfer-Kern: takeover-request/-confirm (Challenge-Confirm-CAS), persistente Approval-Queue, pending_human_approval, atomarer Vollzug mit takeover_base_sha"
type: implementation
status: completed
phase: closure
created: "2026-07-02"
size: L
# NOTE (2026-07-10): completed after a model-fix re-architecture. Four
# instance-fix rounds (R1-R4) repeatedly hit the same finding class
# (client-attested decision inputs, incomplete ownership-basis compares,
# fail-open/write-free invalidation). Root cause was a missing data model,
# not architecture. Resolved by reifying the invariant (decision-record §7,
# reports/AG3-148-model-fix-design.md v7, Codex-APPROVED over 7 design rounds):
# `OwnershipBasis` as the single CAS-basis model (one full compare + one weaker
# reissue anchor), server-truth confirm (selector+audit wire DTO with
# extra="forbid"; identity attested at boundary), approval resolved by
# challenge_ref (UNIQUE), reissue two-step (no blind transfer), every basis
# mismatch → terminalizing UoW, canonical row-lock order across all takeover
# UoWs, human-exclusive confirm/deny. Implemented R5 (e1662943) + R6 (6e133f9b)
# + R7 (c74969a3). Final Fable finale: APPROVE — finding class closed by
# construction; F1/F2/F5, GAP1/2, §6.1-6.7, §7 all closed; no ERROR. Non-
# blocking cleanup (Note B) tracked separately. Green on main c74969a3,
# Jenkins #1766, Sonar 0/0/0, coverage 92.46% local / 85%+ CI.
# Herkunft: GAP-ST-07a (gap-analyse-session-ownership.md v4, Codex-APPROVE).
# Kern des Ownership-Transfers: Challenge-Confirm-Protokoll, Approval-Queue
# (persistent/verfallbar/benutzeruebergreifend), pending_human_approval,
# atomarer Vollzug (ownership_epoch+1 + takeover_base_sha je Repo im
# takeover-transfer-record — KEIN Snapshot). Ping-Pong/Disown = AG3-149,
# Freeze-Invalidierung = AG3-150, Reconcile = AG3-151, UI = AG3-153, CLI = AG3-154.
depends_on: ["AG3-141", "AG3-142", "AG3-147"]
unblocks: ["AG3-149", "AG3-151", "AG3-153", "AG3-154"]

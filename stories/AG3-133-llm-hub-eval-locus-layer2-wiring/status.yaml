story_id: AG3-133
title: "LLM-Hub-Eval-Locus in den Kern + produktive Verify-Layer-2-Anbindung (I2)"
type: implementation
status: ready
phase: setup
created: "2026-06-29"
size: L
# WP-C (Abweichungskarte Zentralisierung): zwei direkte Dev->Hub-Kanten
# (Story-Creation Conflict-Adjudication runtime_factory.py:221; Worker-Health-
# Sidecar sidecar.py:48) muessen in den Kern wandern -> FK-10 §10.1.4 "keine
# direkte Dev->Hub-Kante". Zusaetzlich PO-Mandat: Verify-Layer-2 defaultet auf
# FailClosedLlmClient (composition_root.py:1128) und ist damit produktiv inaktiv;
# muss produktiv + fehlerfrei (fail-closed-korrekt) mit einem realen HubLlmClient
# verdrahtet werden. Transport (HubClient = FK-75-Adapter) ist konform; der
# Ausfuehrungsort ist der Verstoss. C2 (Exploration-Fine-Design) -> AG3-125.
# NEU-ERDEN (Downstream-Grounding nach Commit 95d5ac1): C5 Layer-2-Wiring bleibt
# in-process (composition_root FailClosedLlmClient->HubLlmClient) und wird durch
# reconciled FK-72 ("Verify = in-process, kein HTTP-Owner") bestaetigt; C1/C3-Dev->Hub-
# Loci in den Kern; etwaige Eval-Anforderungsroute auf control_plane_http (kein
# rueckgebauter Mount). Dep AG3-125 superseded entfernt; AG3-129 (Sibling) bleibt.
depends_on:
  - AG3-129
unblocks: []

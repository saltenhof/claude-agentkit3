# AG3-068: VektorDB-Laufzeit + zweistufiger Story-Abgleich + repo_affinity + Export

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `knowledge-access` / Wissenszugriff & Story-Creation (cross-cutting Infra). Die AK3-seitige Laufzeit, die die Story-Knowledge-Base (Weaviate) konsumiert, den zweistufigen Abgleich bei Story-Erstellung fuehrt und das `vectordb_conflict_resolved`-Signal fuer die Modus-Ermittlung (AG3-057) erzeugt.
**Quell-Konzepte (autoritativ):**
- `FK-13 §13.2` — Weaviate ist Pflicht-Infrastruktur; AK3 spricht ueber `weaviate-client` + deployten Story-Knowledge-Base-MCP-Server; Pipeline blockiert fail-closed bei Ausfall.
- `FK-13 §13.5` — Zweistufiger Abgleich bei Story-Erstellung: `story_search(limit=20)` → Similarity-Filter (`similarity_threshold=0.7`) → LLM-Konfliktbewertung Top-5 (`max_llm_candidates=5`, `StructuredEvaluator(role=story_creation_review)`).
- `FK-21 §21.4` — VektorDB-Abgleich als Pflichtschritt der Story-Erstellung (`story_search(search_mode="hybrid", project_id=…, limit=20)`); Schwellenwert-Filter; LLM-Konfliktbewertung `role="story_creation_review"`, Check `conflict_assessment`; fail-closed bei Weaviate-Ausfall (§21.4.3); Protokollierung (§21.4.2).
- `FK-21 §21.9` — Repo-Affinitaet via `resolve_repo_affinity`: Strong-Evidence-Parser nur ueber `## Betroffene Dateien` (§21.9.1), Longest-Prefix-Match gegen `project.yaml`-`repositories[]` (§21.9.2), Root/Docs-Fallback ueber das Module-Feld (§21.9.3), deterministische Sortierung „Treffer absteigend, dann lexikographisch" mit erstem Eintrag als Spawn-CWD-Anker (§21.9.5).
- `FK-21 §21.11` — Deterministischer `story.md`-Export (`agentkit export-story-md`, Modul `agentkit.backend.story_creation.story_md_export`, `StoryMdExportResult` als `@dataclass(frozen=True)`, Frontmatter-Erzeugung, >500-Byte-Validierung, automatische Weaviate-Indizierung als harter Blocker, §21.11.4) + `agentkit repair-story-md` (§21.11.6). Weaviate-Readiness-Entrypoint `agentkit.backend.vectordb.wait_for_weaviate` (§21.11.4).
- `FK-21 §21.12` — Story-Flag `vectordb_conflict_resolved: true` erzwingt Exploration; gesetzt, wenn ein erkannter Konflikt geklaert und die Story angepasst (nicht verworfen) wurde; Modus-Ermittlung prueft das Flag.
- `FK-11 §11.5.1` — Rollen-/Template-/Check-Tabelle des `StructuredEvaluator`: `story_creation_review` mit Template `prompts/vectordb-conflict.md`, 1 Check (Duplikat/Ueberschneidung).

---

## 1. Kontext / Ist-Zustand (belegt)

Die FK-13/FK-21-Wissenszugriffs-Laufzeit fehlt fast vollstaendig im Produktionscode:

- `src/agentkit/integrations/vectordb/__init__.py` ist **leer (0 Byte)** — kein Weaviate-Client, keine `story_search`/`story_sync`-Tools, keine fail-closed-Blockade, kein `wait_for_weaviate`-Readiness-Entrypoint (Gap FK-13 §13.2 / FK-21 §21.11.4).
- Kein `vectordb.similarity_threshold` / `vectordb.max_llm_candidates` im Config-Modell (`src/agentkit/config/models.py` — Grep `similarity_threshold|max_llm_candidates|vectordb` ohne Treffer); kein zweistufiger Abgleich im Code (Gap FK-13 §13.5 / FK-21 §21.4). Der Schritt existiert nur als Skill-Prosa (`create-userstory`), nicht als AK3-Python-Komponente.
- Kein Verzeichnis `src/agentkit/story_creation/`; Grep `export_story_md|story_md_export|repair-story-md|resolve_repo_affinity|repo_affinity` ueber `src/agentkit/**.py` ohne Treffer (Gap FK-21 §21.9/§21.11). `participating_repos` wird nur konsumiert (`src/agentkit/governance/setup_preflight_gate/context_builder.py:174` issue-Pfad, `:255` story-Pfad), die Ableitungslogik fehlt.
- Kein `vectordb_conflict_resolved`-Story-Feld am autoritativen Story-Modell (`src/agentkit/story/models.py`, `src/agentkit/story_context_manager/models.py`/`story_model.py`); das in FK-22-Pseudocode referenzierte `context.vectordb_conflict` existiert nicht (Gap FK-21 §21.12). **AG3-057 konsumiert** dieses Flag (fail-closed-Default), **AG3-068 ist der Produzent** — das ist die im Index festgelegte Schnittentscheidung (`var/concept-gap-analysis/_STORY_INDEX.md:152`: „Der `vectordb_conflict`-Konsument bleibt bei AG3-057, der Produzent ist AG3-068").
- Anknuepfung `StructuredEvaluator` existiert (`src/agentkit/verify_system/llm_evaluator/structured_evaluator.py`), kennt aber aktuell nur die drei `ReviewerRole`-Werte `qa_review`, `semantic_review`, `doc_fidelity` (`structured_evaluator.py:126-137`); die Rolle `story_creation_review` und ihr Check `conflict_assessment` fehlen im Code, obwohl FK-11 §11.5.1 (`11_llm_provider_browser_pools_prompt_execution.md:486`) und FK-21 §21.4.1 sie fordern. Die Role-Maps `_ROLE_CHECK_IDS`/`_ROLE_TEMPLATE` (`structured_evaluator.py:165-176`) und die `evaluate(role: ReviewerRole, …)`-Signatur (`structured_evaluator.py:299-301`) sind hart auf den Enum getypt.
- `VECTORDB_SEARCH`-EventType existiert bereits im Enum (`src/agentkit/telemetry/events.py:78`) mit fixem Pflicht-Payload-Contract `total_hits`, `hits_above_threshold`, `hits_classified_conflict`, `threshold_value` (`events.py:186-191`) — die Such-Telemetrie dockt hier an, kein neuer Eventtyp, keine neuen Pflichtfelder.

## 2. Scope

### 2.1 In Scope
1. **Weaviate-Laufzeit-Adapter** unter `src/agentkit/integrations/vectordb/` (duenner Adapter, ARCH: keine Fachlogik in `integrations/`): `weaviate-client`-basierter Client mit `story_search(query, *, search_mode="hybrid", project_id, limit=20)` und `story_sync(...)` gegen die deployte Story-Knowledge-Base. **Fail-closed**: Bei Weaviate-Ausfall/nicht-erreichbar wird die Story-Erstellung blockiert (ERROR), nie still mit leeren Ergebnissen fortgesetzt (FK-21 §21.4.3).
2. **Weaviate-Readiness-Entrypoint** unter dem **kanonischen FK-Modulpfad** `agentkit.backend.vectordb.wait_for_weaviate` (FK-21 §21.11.4): CLI-invokierbar als `python -m agentkit.backend.vectordb.wait_for_weaviate --timeout 10`, Exit 0 = bereit, Exit 1 = nicht erreichbar (fail-closed). Dieser Entrypoint ist ein duenner App-Layer-Shim, der den Adapter aus (1) konsumiert — die Fachregel „bereit/nicht-bereit" liegt nicht in `integrations/`.
3. **Zweistufiger Story-Abgleich** als deterministische Fachkomponente (App-Layer unter `story_creation/`, nicht in `integrations/`): Stufe 1 = `story_search(search_mode="hybrid", project_id, limit=20)` + Similarity-Filter gegen `vectordb.similarity_threshold` (Default 0.7); Stufe 2 = LLM-Konfliktbewertung der Top-`max_llm_candidates` (Default 5) ueber den `StructuredEvaluator` mit Rolle `story_creation_review`, Check `conflict_assessment`, Template `prompts/vectordb-conflict.md` (FK-11 §11.5.1, FK-21 §21.4.1). Ergebnis: PASS (kein Konflikt) | FAIL (Duplikat/Ueberschneidung) als typisiertes Verdikt.
4. **`story_creation_review`-Rolle im bestehenden `StructuredEvaluator` modellieren** (verify-system BC): Enum-Wert `ReviewerRole.STORY_CREATION_REVIEW = "story_creation_review"` (`structured_evaluator.py:126`), Check-Id-Whitelist `{"conflict_assessment"}` (analog `QA_REVIEW_CHECK_IDS`, `structured_evaluator.py:143`), Eintraege in `_ROLE_CHECK_IDS`/`_ROLE_TEMPLATE` (`:165`/`:172`, logischer Template-Name `vectordb-conflict`). Keinen zweiten LLM-Evaluator-Pfad bauen. Die Contract-/Golden-Tests des Evaluators sind mitzuziehen (neue Rolle in Whitelist/Template-Map).
5. **`vectordb_conflict_resolved`-Flag** als typisiertes, persistiertes Story-Attribut (englischer Wire-Key, ARCH-55) am autoritativen Story-Modell (`story/models.py` und der Story-Context-Persistenz). **Setzregel (FK-21 §21.12, §21.4.1):** Das Flag wird `true`, wenn die Stufe-2-Bewertung `FAIL` (Konflikt erkannt) liefert **und** der Konflikt geklaert wurde, indem die Story angepasst (nicht verworfen) wurde; bestaetigt wird die Klaerung durch den Story-Ersteller im Rahmen der obligatorischen Story-Erstellung. Bei `PASS` (kein Konflikt) bleibt das Flag `false`/absent. Dieses Flag ist der **Produzenten-Output** fuer `determine_mode` (AG3-057) — Single Source of Truth, kein Schattenfeld (FIX-THE-MODEL). Schema-Version ziehen falls noetig.
6. **`resolve_repo_affinity(story_body, pipeline_config) -> RepoAffinityResult`** (FK-21 §21.9):
   - **Strong-Evidence-Parser:** Nur Dateipfade unter der Story-Sektion `## Betroffene Dateien` zaehlen als Evidenz; Pfade aus Logs, Beispielen oder Referenzen werden ignoriert (§21.9.1).
   - **Longest-Prefix-Match** der gelisteten Pfade gegen `pipeline_config.repositories[]` (§21.9.2).
   - **Root/Docs-Fallback:** Sind keine direkten Pfade gelistet (Root-/Docs-Repos), wird der Scope ueber das Module-Feld der Story abgeleitet (§21.9.3).
   - **Deterministische Sortierung:** Treffer pro Repo absteigend, bei Gleichstand lexikographisch; alle Repos fachlich gleichberechtigt; erster Eintrag = reiner Spawn-CWD-Anker (FK-22 §22.6.4), keine fachliche Sonderrolle (§21.9.5).
   - **Human-Correction:** Das Ergebnis ist ein Vorschlag; die menschliche Korrektur des `participating_repos`-Attributs bleibt moeglich (§21.9.2) — keine harte Ueberschreibung einer manuell gesetzten Affinitaet ohne Re-Run.
   Speist `participating_repos` an der autoritativen Quelle, ersetzt den heutigen reinen Konsum.
7. **Deterministischer `story.md`-Export**: Modul `agentkit.backend.story_creation.story_md_export` mit `export_story_md(story_id: str, story_dir: Path) -> StoryMdExportResult`; YAML-Frontmatter (`story_id`, `labels`, `exported_at = datetime.now(timezone.utc).isoformat()`, §21.11.3), Title als H1, deterministisches Markdown-Rendering der strukturierten Story-Attribute, Validierung (Datei existiert, >500 Byte, Frontmatter mit `story_id`+`exported_at`, §21.11.5); CLI `agentkit export-story-md --story-id … --story-dir …` + `agentkit repair-story-md` (Batch-Scan/Validate/Reexport, §21.11.6); **automatische inkrementelle Weaviate-Indizierung nach dem Schreiben als harter Blocker** (Export gilt nur als erfolgreich, wenn Indizierung bestaetigt; Indizierungsfehler = fail-closed ERROR, kein Warning/Nachholpfad, §21.11.4).
8. **`StoryMdExportResult`** FK-konform als **`@dataclass(frozen=True)`** (FK-21 §21.11, `21_story_creation_pipeline.md:687`) mit exakt: `success: bool`, `story_md_path: str`, `file_size_bytes: int`, `error: str` (englische Felder). Fehlersemantik: bei Erfolg `success=True`, `error=""`; bei jedem Blocker (Schreibfehler, <500 Byte, fehlende Frontmatter, Indizierungsfehler) `success=False` und `error` traegt die Ursache, `file_size_bytes` den tatsaechlichen Stand.
9. **Config-Konsum `vectordb`-Stanza** (`similarity_threshold`, `max_llm_candidates`, Endpoint/Connection): **Feld-Owner ist ausschliesslich AG3-070** (Config-Modell-Vollausbau; `_STORY_INDEX.md:66` listet die `vectordb`-Stanza dort). `status.yaml` traegt die **harte** `depends_on: AG3-070`. Diese Story **konsumiert** die Stanza und liefert die fachlichen Defaults (0.7 / 5) als Konzept-Referenz an AG3-070 — sie modelliert die Stanza **nicht** selbst (kein zweiter Owner, FIX-THE-MODEL). Ist AG3-070 noch nicht gemerged, ist diese Story blockiert (depends_on), nicht durch eigene Minimal-Modellierung zu entkoppeln.
10. **Telemetrie/Protokollierung** des Abgleichs ausschliesslich ueber den bestehenden `VECTORDB_SEARCH`-Event mit dem **existierenden Pflicht-Payload-Contract** (`events.py:186-191`): `total_hits`, `hits_above_threshold`, `hits_classified_conflict`, `threshold_value`. Kein neuer Eventtyp, keine neuen Pflichtfelder, keine zweite Log-Datei. Dieser typisierte Event ist die SSOT der Abgleich-Protokollierung (FK-21 §21.4.2); die FK-Prosa-JSON-Felder (`above_threshold`/`sent_to_llm`/`llm_conflicts`/`threshold_used`/`search_mode`) sind durch den Code-Contract superseded und in der doc-only-Konzeptpflege anzugleichen (siehe §2.2).
11. **Negativpfade**: Weaviate-Ausfall blockiert (Test), `wait_for_weaviate` Exit 1 bei nicht-erreichbarem Weaviate (Test), `FAIL`-Verdikt ohne Story-Anpassung setzt das Flag nicht, Indizierungsfehler blockiert den Export (Test), `resolve_repo_affinity` deterministisch bei Mehrfach-/Gleichstand-Treffern (Test), Strong-Evidence-Parser ignoriert Pfade ausserhalb `## Betroffene Dateien` (Test).

### 2.2 Out of Scope (mit Owner)
- **Konsum** des `vectordb_conflict_resolved`-Flags in der Modus-Entscheidung — **AG3-057** (`determine_mode`). Diese Story liefert nur den Produzenten. **Bekannte Vorbedingung/Follow-up:** AG3-057 `story.md:49` nennt noch `AG3-066` als Produzenten, waehrend der Index (`_STORY_INDEX.md:152`) AG3-068 setzt — diese stale Owner-Referenz ist in AG3-057 zu bereinigen (gehoert nicht in diese story.md; hier nur als Vorbedingung gemeldet).
- **`vectordb`-Config-Stanza + Feature-Flag** (`similarity_threshold`/`max_llm_candidates`/Endpoint) — **AG3-070** (Config-Modell-Vollausbau, harte `depends_on`). Diese Story konsumiert ausschliesslich.
- **Concept-Corpus-Freshness-Preflight** (FK-21 §21.11.4 Step 0 Check 2, Kap. 13.9.9): prueft `concept_graph.json`/`corpus_revision` und ist **deterministisch und unabhaengig von der VektorDB**. Gehoert zum Concept-Graph-/Dev-Tooling (`agentkit concept build`, `tools/concept_*`), **nicht** zur FK-13-Story-Knowledge-Base-Laufzeit dieser Story. Out of Scope **mit Owner = ConceptContext/Concept-KB-Dev-Tooling** (siehe naechster Punkt). Diese Story uebernimmt von §21.11.4 nur die **Weaviate-Readiness-Haelfte** (Scope §2.1.2).
- **ConceptContext / `concept_search` / `concept_validate`-Suite + Dev-seitiges Concept-KB-Tooling** (FK-13 §13.9, `tools/concept_mcp`, `tools/concept_ingester`) — bewusst getrennt, kein Owner-Wechsel; traegt auch die Concept-Corpus-Freshness-Pruefung.
- **Story-Split-Reindexierung** (FK-54) — **AG3-072** (`depends_on AG3-068`).
- **Multi-LLM-Hub-Wiring** des `StructuredEvaluator`-Transports (FK-11/FK-75) — **AG3-065**; hier wird der bestehende Evaluator-Transport als Black-Box konsumiert (nur die Rollen-/Check-/Template-Erweiterung aus §2.1.4 gehoert hierher, nicht das Transport-Wiring).

## 3. Akzeptanzkriterien
1. `story_search`/`story_sync` sprechen ueber `weaviate-client` gegen die Story-Knowledge-Base; `story_search` uebergibt `search_mode="hybrid"`, `project_id` und `limit=20` (FK-21 §21.4.1). Bei Weaviate-Ausfall blockiert die Story-Erstellung fail-closed mit ERROR (Negativtest), nie stiller Leer-Fallback.
2. `python -m agentkit.backend.vectordb.wait_for_weaviate --timeout 10` liefert Exit 0 bei erreichbarem, Exit 1 bei nicht-erreichbarem Weaviate (Negativtest); der Modulpfad ist exakt `agentkit.backend.vectordb.wait_for_weaviate` (FK-21 §21.11.4).
3. Der zweistufige Abgleich filtert Stufe 1 gegen `similarity_threshold` (Default 0.7) und bewertet Stufe 2 maximal `max_llm_candidates` (Default 5) Kandidaten ueber `StructuredEvaluator(role=story_creation_review)` mit Check `conflict_assessment` (Test: >5 Kandidaten ueber Schwelle → genau 5 bewertet).
4. `ReviewerRole.STORY_CREATION_REVIEW` existiert im Enum, ist in `_ROLE_CHECK_IDS` (`{"conflict_assessment"}`) und `_ROLE_TEMPLATE` (`vectordb-conflict`) eingetragen; `evaluate(role=ReviewerRole.STORY_CREATION_REVIEW, …)` validiert den Check `conflict_assessment` (Contract-Test); kein zweiter Evaluator-Pfad.
5. Ein Stufe-2-`FAIL` mit anschliessender Story-Anpassung setzt `vectordb_conflict_resolved=true` am autoritativen Story-Record; ein `PASS` (kein Konflikt) bzw. ein nicht geklaerter/nicht angepasster Konflikt laesst es `false`/absent (zwei Tests). Das Flag ist als typisiertes, persistiertes Feld nachweisbar (kein Schattenfeld).
6. `resolve_repo_affinity` parst ausschliesslich Pfade unter `## Betroffene Dateien` (Strong Evidence; Test: Pfade aus Logs/Beispielen werden ignoriert), matcht per Longest-Prefix gegen `repositories[]`, faellt bei fehlenden Pfaden auf das Module-Feld zurueck (Root/Docs-Test) und liefert deterministisch sortierte Ergebnisse (Treffer absteigend, dann lexikographisch; Test mit ueberlappenden Prefixen + Gleichstand). Erster Eintrag = Spawn-CWD-Anker ohne fachliche Sonderrolle.
7. `agentkit export-story-md` erzeugt `story.md` mit YAML-Frontmatter (`story_id`, `labels`, `exported_at`), validiert >500 Byte und Frontmatter-Pflichtfelder und indiziert anschliessend in Weaviate; fehlgeschlagene Indizierung blockiert den Export fail-closed (Negativtest), kein Nachholpfad. `repair-story-md` scannt `stories/{prefix}-*/story.md`, validiert und reexportiert defekte/fehlende Dateien deterministisch (Report N/M/K).
8. `StoryMdExportResult` ist ein `@dataclass(frozen=True)` mit exakt `success: bool`, `story_md_path: str`, `file_size_bytes: int`, `error: str` (englische Felder, FK-21 §21.11); bei Blockern `success=False` mit gesetztem `error` (Test).
9. `VECTORDB_SEARCH`-Telemetrie wird beim Abgleich emittiert und traegt exakt die bestehenden Pflichtfelder `total_hits`, `hits_above_threshold`, `hits_classified_conflict`, `threshold_value` (`events.py:186-191`); kein neuer Eventtyp, kein zweites Log-Format (Test gegen den Event-Contract).
10. Alle neuen Bezeichner/Enum-Werte/Wire-Keys/CLI-Befehle/DB-Spalten sind englisch (ARCH-55).
11. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage ≥ 85 %.

## 4. Definition of Done
- AK 1–11 erfuellt; giftige Codex-Review PASS; Implementierung/Commit erst nach Execution-Plan-Freigabe — diese Story wird zunaechst nur autorisiert/reviewt.

## 5. Guardrail-Referenzen
- **FAIL CLOSED:** Weaviate-Ausfall, `wait_for_weaviate` Exit 1 und fehlgeschlagene Indizierung blockieren, nie stiller Leer-/Skip-/Nachhol-Fallback.
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** `vectordb_conflict_resolved` und `participating_repos` an der autoritativen Story-Wahrheit, kein zweites Steuermodell, kein Schattenfeld; `vectordb`-Config-Owner ist allein AG3-070 (harte Abhaengigkeit), nicht doppelt modelliert; Schema-Version ziehen falls noetig.
- **TYPISIERT STATT STRINGS:** `StoryMdExportResult` (`@dataclass(frozen=True)`, FK-konform), Konflikt-Verdikt (`LlmVerdict`), `RepoAffinityResult` und das konsumierte `vectordb`-Config-Modell typisiert.
- **ARCH-55:** alle neuen Felder/Keys/Enum-Werte/CLI-Befehle englisch.
- **MOCKS-AUSNAHME:** echter Weaviate-Pfad ist Standard; Mock nur an der LLM-/Weaviate-Adaptergrenze im isolierten Unit-Test, sonst echte Integration.
- **`integrations/` bleibt duenn:** Weaviate-Client ist Adapter; der zweistufige Abgleich + `wait_for_weaviate`-Shim + repo_affinity + Export sind Fachlogik im App-Layer (`story_creation/` bzw. `agentkit.backend.vectordb`), NICHT in `integrations/`.
- **CONTRACT-DISZIPLIN:** `VECTORDB_SEARCH`-Payload und `StoryMdExportResult`-Form folgen dem bestehenden Code-Contract; abweichende FK-Prosa wird doc-only nachgezogen, nicht der Code an die Prosa gebogen.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- Kritischer Schnitt: AG3-068 ist **Produzent** des `vectordb_conflict_resolved`-Flags, AG3-057 ist **Konsument** — keine Modus-Entscheidung hier bauen. AG3-057s stale `AG3-066`-Produzentenverweis (`AG3-057 story.md:49`) ist dort zu bereinigen, nicht hier.
- `StructuredEvaluator` wiederverwenden und um `story_creation_review` (+Check `conflict_assessment`, +Template `vectordb-conflict`) erweitern (`structured_evaluator.py:126/143/165/172`); Evaluator-Contract-/Golden-Tests mitziehen. Keinen zweiten LLM-Evaluator-Pfad anlegen.
- Die `vectordb`-Config-Stanza gehoert ausschliesslich AG3-070 (harte `depends_on`); nur konsumieren, nicht selbst modellieren — bei fehlendem AG3-070-Merge ist diese Story blockiert, nicht zu entkoppeln.
- Concept-Corpus-Freshness (§21.11.4 Step 0 #2) ist **nicht** Teil dieser Story — Owner: Concept-Graph/Concept-KB-Dev-Tooling. Hier nur die Weaviate-Readiness-Haelfte (`agentkit.backend.vectordb.wait_for_weaviate`).
- `weaviate-client` ist optionale Dependency (`pyproject.toml [project.optional-dependencies]`) — Import-Guard sauber halten; fehlendes Feature/Flag fail-closed melden, nicht still ueberspringen.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle (genaue Ausgaben), Test-Namen der Negativ-/Determinismus-Pfade.

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

story_id: AG3-160
title: "Konzept-Scope-Konsistenz-Sweep (W3): LLM-Sweep pro authority_over-Scope — alle Aussagen-Chunks eines Scopes als geschlossenes Set auf Widersprüche prüfen (nightly, Baseline wie W2)"
type: implementation
status: completed
# Completed 2026-07-14: deterministic authority-scope inversion and stable
# non-truncating partitions feed exactly one structured Hub call per partition.
# The deterministic policy validates quoted loci against the closed input and is
# the sole owner of SCOPE_CONTRADICTION severity. W2 and W3 share the version-1
# justified baseline; W3 entries require related loci plus the mandatory P4
# formalization_check yes/no decision and reason. Transport, parse, prompt-I/O,
# foreign-quote, and incomplete paths are named and discard partial results.
# Nightly wiring is non-blocking and scope-filtered pre-merge use is documented.
# Fixed-answer tests cover AC1-AC6/AC8, including exact call-count equality and
# no cross-scope pairs. Bounded real-Hub smoke evidence is at
# var/evidence/AG3-160-real-hub-smoke.log: final successful one-set/one-partition
# runs on chatgpt, gemini, grok, and qwen; kimi was intentionally excluded.
# Baseline remains empty. Full ex-e2e: 9538 passed, 14 skipped; coverage 91.70%;
# Ruff, native/Linux mypy, W1/W4 and all concept gates green.
# CLOSED 2026-07-14 at main@5bf6a0ea6c4e18907f6d7433cb859aa8229b27ef (origin matches).
# ORCHESTRATOR ADJUDICATION (independent): "the LLM never decides" is STRUCTURALLY
# enforced -- the classification input model (scope_contracts.py:45) deliberately has
# NO verdict field, so the LLM cannot express PASS/ERROR; scope_policy.py owns
# SCOPE_CONTRADICTION deterministically; the regex fallback (parser.py:59) rejects
# verdict tokens. Bounded-by-design confirmed: exactly one Hub call per scope-set/
# partition, never chunk-pairs, never cross-scope. Shared W2/W3 baseline is empty
# (honest start). AC7 proven by a bounded real-hub SMOKE TEST (4 scope-sets, one per
# backend chatgpt/gemini/grok/qwen, kimi excluded) at
# var/evidence/AG3-160-real-hub-smoke.log; two iterative first attempts fail-closed
# correctly against real LLM misbehavior (gemini invalid chunk_id -> strict
# validation; qwen fabricated quote -> policy stayed strict, retry passed) -- the
# "a handful more calls" model, NOT a full all-scopes sweep as a gate (AG3-159 lesson
# applied up front). A real parallel Postgres catalog race surfaced by the full suite
# was fixed at its schema owner + a migration test. Independent CI: Jenkins #1921
# SUCCESS (built exact SHA), Sonar gate OK / 0 / 0 / 0; local W2+W3 50 passed + 6
# concept gates green (Windows). CONCEPT-GATE SEQUENCE COMPLETE: W1 (AG3-157) / W4
# (AG3-158) / W2 (AG3-159) / W3 (AG3-160) all landed.
phase: closure
created: "2026-07-02"
completed_at: "2026-07-14T13:43:04Z"
size: M
# Herkunft: META-CONCEPT-CONSISTENCY (concept/_meta/konzept-konsistenz-
# governance.md) §5/W3, §6 Betriebsmodell, §7 Fahrplan Punkt 4; PO-Freigabe
# 2026-07-02. Widerspruchssuche kollabiert auf kleine Mengen: pro
# authority_over-Scope werden alle Aussagen-Chunks des Scopes als
# geschlossenes Set auf Widersprueche geprueft. Setzt die W2-Infrastruktur
# (AG3-159: Chunk-Klassifikation, Baseline-Mechanik) TECHNISCH voraus —
# ohne sie degeneriert die Pruefung zu O(n^2) ueber den Gesamtkorpus.
# Betrieb nightly, Baseline nur mit begruendeten Eintraegen (wie W2).
# Chunk-Quelle: deterministische repo-lokale Discovery (kein externer
# Weaviate-/MCP-Index als Pruef-Grundlage).
depends_on: ["AG3-159"]
unblocks: []

# AG3-117: Fact-Column-Reconciliation nach FK-62 §62.2

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `kpi-and-dashboard` / Aggregations-Schema (BC16) + `state_backend` (Fact-Store/Migration). Die fuenf `fact_*`-Tabellen werden auf den FK-62-Sollspaltensatz gebracht: Renames, neue Spalten, Drops und ein Typwechsel — konsistent ueber alle realen Wahrheitsorte und alle ausgelieferten Backend-Konsumenten.
**Quell-Konzepte (autoritativ):**
- `FK-62 §62.2.1-§62.2.5` (`concept/technical-design/62_kpi_aggregation.md:101-289`) — Sollstand der fuenf Fact-Tabellen-Spaltensaetze (DDL verbatim, inkl. PK-Namen `guard_key`/`pool_key` und Typ `are_gate_passed INTEGER`).
- `FK-62 §62.4` (`62_kpi_aggregation.md:582-612`) — Migrations-/Versionierungsstrategie (`schema_version`-Cursor; FK-62 §62.4.3 PK `(version)` so jede Version genau einmal). Diese Story fuehrt eine neue Versionsstufe ein (siehe §2.1.4).
- `FK-60 §60 P3` (`60_kpi_katalog_und_architektur.md:167`) Rollup-Semantik — „Analytics ist aus den Rohdaten jederzeit neu berechenbar"; §60 P3b (`:169-175`) und der Refresh-Worker-Lauf („Pro Dirty Set: Betroffene Slices **komplett neu berechnen**", `:366`) belegen: die `fact_*`-Tabellen sind **recomputable Rollups**, also wegwerf-/neu-aufbaubar — **kein** Datenbestand, der migriert werden muesste.

---

## 1. Kontext / Ist-Zustand (belegt)

Die heutige Fact-Wahrheit weicht von FK-62 §62.2 in **Namen**, **Spaltenmenge** und in **einem Typ** ab. Die Spaltenwahrheit liegt an fuenf realen Orten (vier spalten-tragende Dateien + die Mapper-Schicht):

- **Typisierte Records:** `src/agentkit/kpi_analytics/fact_store/models.py:25-120` (`FactStory:25-53`, `FactGuardPeriod:56-69`, `FactPoolPeriod:72-87`, `FactPipelinePeriod:90-104`, `FactCorpusPeriod:107-120`). `frozen, extra="forbid"` (`models.py:22`) — jeder Rename bricht die Konstruktion.
- **Postgres-DDL:** `src/agentkit/state_backend/postgres_schema.sql:974-1048` (`fact_story:974`, `fact_guard_period:1001` PK `guard_id:1008`, `fact_pool_period:1014` PK `llm_role:1023`, `fact_pipeline_period:1029`, `fact_corpus_period:1040`). TIMESTAMPTZ/BIGINT.
- **SQLite-Migrations-DDL:** Die heutige SQLite-Spaltenwahrheit liegt in `src/agentkit/state_backend/migration/versions/v_3_4_analytics.sql:18-118` (PK `guard_id:52`, `llm_role:67`). Hinweis: `sqlite_store._ensure_analytics_tables` traegt **keine** Inline-Spalten-DDL — es delegiert an den `MigrationRunner` (siehe unten). Editieren von Spalten dort ist ein No-Op; die SQLite-Spalten leben in den Migrationsdateien. **Diese Story aendert `v_3_4_analytics.sql` NICHT in place**, sondern fuehrt das FK-62-Schema ueber eine **neue** Versionsdatei ein (siehe §2.1.4).
- **Column-Listen / Mapper:** `src/agentkit/state_backend/store/_fact_sql.py:39-128` (`_build_fact_sql`, Konstanten `fact_*_columns`/`fact_*_update`/`fact_*_conflict`) **+** `src/agentkit/state_backend/store/fact_repository.py` (Mapper `_fact_*_params`/`_row_to_fact_*` und die `row["…"]`-Zugriffe). Das sind **zwei** Dateien (die Column-Strings wurden aus `fact_repository.py` in das Sibling `_fact_sql.py` extrahiert — LOC-Budget).

`MigrationRunner` (`src/agentkit/state_backend/migration/migration_runner.py`) registriert die Versionen in `_MIGRATIONS` (`:40-43`: `("3.4","v_3_4_analytics.sql")`, `("3.5","v_3_5_compaction_epochs.sql")`) und **ueberspringt** bereits angewandte Versionen: `run()` `if version in already: continue` (`:126-128`). **Konsequenz (gegen das fruehere „v_3_4 in place editieren"):** Eine in-place-Editierung von `v_3_4_analytics.sql` ist **unsicher**. Eine DB, die `3.4` bereits in `schema_versions` eingetragen hat, ueberspringt die Datei beim naechsten `run()` fuer immer (`:127`) und behaelt das **alte** Schema dauerhaft — die Spaltenaenderung erreicht solche DBs nie. Eine bereits existierende `3.5` (`:42`, `versions/v_3_5_compaction_epochs.sql`) belegt, dass das Versionsmodell additiv-vorwaerts genutzt wird. Bedeutung fuer diese Story: es braucht eine **neue Versionsdatei**, nicht eine In-place-Editierung — siehe Migrations-Entscheidung in §2.1.4.

**FK-62-Abweichungen (verifiziert gegen `62_kpi_aggregation.md`):**
- Renames (alle in FK-62 §62.2 belegt): `story_mode→pipeline_mode` (`:112`), `started_at→opened_at` (`:113`), `completed_at→closed_at` (`:114`), `qa_rounds→qa_round_count` (`:119`), `adversarial_findings→adversarial_findings_count` (`:128`), `are_gate_status→are_gate_passed` (`:139`, zusaetzlich **Typwechsel** str→bool/int), `guard_id→guard_key` (PK, `:169`/`:185`), `llm_role→pool_key` (PK, `:196`/`:216`), `incidents_recorded→new_incident_count` (`:281`), `patterns_promoted→patterns_total_count` (`:282`), `checks_approved→patterns_with_active_check` (`:283`, Semantikverschiebung, nicht reiner Label-Rename).
- **Wichtig (Korrektur ggue. dem Bauchgefuehl „period_end ueberall"):** FK-62 §62.2 fuehrt **kein** `period_end` mehr (alle Period-Tabellen tragen `period_start` + `period_grain`, nie `period_end`). Heute existiert `period_end` auf den vier Period-Tabellen (`fact_guard_period`/`fact_pool_period`/`fact_pipeline_period`/`fact_corpus_period`) — auf `fact_story` gibt es kein `period_end`. `period_end` ist also ein **Drop** auf vier Tabellen.
- Semantische Renames (nicht Drops): `stories_completed→story_count_closed` (FK-62 §62.2.4 `:232`), `avg_qa_rounds→qa_round_avg` (`:262`). `avg_phase_implementation_ms` hat in FK-62 §62.2.4 kein 1:1-Aequivalent; die Pipeline-Tabelle ist eine Shape-Ersetzung (siehe §2.1, Zaehlung).
- Echte Drops (in keiner FK-62-Tabelle vorhanden, von ausgeliefertem Code aber geschrieben): `token_input_total`, `token_output_total`, `avg_latency_ms` (FactPoolPeriod), `agentkit_version`, `agentkit_commit` (FactStory), `period_end` (×4 Period-Tabellen), `stories_escalated` (FactPipelinePeriod — kein FK-62-Ziel).

**Ausgelieferte Konsumenten (muessen mitgezogen werden, durch User autorisiert):**
- `src/agentkit/kpi_analytics/aggregation/worker.py:325` — `keys = [(r.project_key, r.llm_role, r.period_start) for r in rows]` (bricht auf `llm_role→pool_key`).
- `src/agentkit/kpi_analytics/top.py:72` (`fact.guard_id == ...`), `:80` (`fact.llm_role == ...`), `:329`/`:337` (Fehlertexte nennen `guard_id`/`llm_role`-Spalten).
- `src/agentkit/kpi_analytics/dashboard/service.py:189` (`fact.completed_at`), `:197` (`fact.are_gate_status or "UNKNOWN"` — Typ-/Semantikfolge des bool-Wechsels), `:201` (`fact.qa_rounds`), `:203`/`:206` (`completed_at`).
- `src/agentkit/state_backend/store/analytics_source.py` — der AG3-082-Adapter, der **jeden** Fact-Record konstruiert; nutzt alte Feldnamen UND FK-62-fremde Felder: `:177` `llm_role=`, `:179`/`:204`/`:216`/`:236`/`:243`/`:266`/`:271` `period_end=`, `:181` `token_input_total=`, `:217` `stories_completed=`, `:219` `avg_qa_rounds=`, `:244` `incidents_recorded=`, `:245` `patterns_promoted=`, `:246` `checks_approved=`, `:269` `guard_id=guard_key`, `:384` `story_mode=`, `:385` `started_at=`, `:387` `qa_rounds=`, `:392` `agentkit_version=`. Hinweis: `analytics_source.py:319` `max(metrics, key=lambda r: r.completed_at)` und die `_parse_iso(record.completed_at)`-Stellen (`:156-158`, `:201-205`, `:377-378`) lesen das **Runtime**-`StoryMetricsRecord` (anderes Modell, **nicht** der Fact-Record) — diese sind **nicht** vom Fact-Rename betroffen; nur die Fact-Record-Konstruktion downstream.
- `fact_repository.py` Read-Pfad: `list_fact_guards` ordert nach `guard_id`, `list_fact_pool` nach `llm_role`; `_row_to_fact_*` lesen `row["guard_id"]`/`row["llm_role"]`/`row["started_at"]`/`row["completed_at"]`/`row["qa_rounds"]`/`row["are_gate_status"]` usw.

**Wire-Schild bereits vorhanden:** Durch **AG3-116** (depends_on) existiert das KPI-Wire-DTO am HTTP-Rand, **und** das Frontend liest bereits ausschliesslich die FK-62-geformten DTO-Wire-Keys. AG3-116 hat dabei nicht nur die **umbenannten** Felder, sondern auch die hier **gedroppten** Felder aus der UI entfernt (die UI liest in AG3-116 keinen der Felder mehr, die FK-62 fallen laesst — `token_input_total`/`token_output_total`/`avg_latency_ms`/`agentkit_version`/`agentkit_commit`/`period_end`/`stories_escalated`; siehe AG3-116 §2.1). Konsequenz fuer diese Story: Die internen Renames **und** Drops erreichen das Frontend **nicht** mehr; das DTO-Mapping fuer die umbenannten Felder wird zur **Identitaet** (interner FK-62-Feldname == FK-62-Wire-Key) und die gedroppten Felder waren wire-seitig durch AG3-116 bereits entfernt. Deshalb wird das **Frontend in dieser Story NICHT angefasst** (explizit, siehe §2.2) — und die Behauptung „Frontend untouched" ist korrekt, weil AG3-116 die UI vorab von allen umbenannten/gedroppten Rohfeldern entkoppelt hat.

## 2. Scope

### 2.1 In Scope
1. **Die fuenf `fact_*`-Tabellen auf den FK-62-Sollspaltensatz bringen** (FK-62 §62.2.1-§62.2.5 als verbindliche Namens-/Spaltenwahrheit), bestehend aus:
   - **Die 11 bestaetigten Renames** (siehe §1, alle FK-62-belegt): `story_mode→pipeline_mode`, `started_at→opened_at`, `completed_at→closed_at`, `qa_rounds→qa_round_count`, `adversarial_findings→adversarial_findings_count`, `are_gate_status→are_gate_passed`, `guard_id→guard_key` (PK), `llm_role→pool_key` (PK), `incidents_recorded→new_incident_count`, `patterns_promoted→patterns_total_count`, `checks_approved→patterns_with_active_check`.
   - **Der Typwechsel** `are_gate_status` (str) → `are_gate_passed` (echtes bool/INTEGER, FK-62 §62.2.1 `:139`, Entscheidung gesperrt). Konsumenten, die den String lesen (`dashboard/service.py:197`), werden auf die bool-Semantik umgestellt.
   - **Spalten je Tabelle, die NEU hinzukommen** (FK-62-Zielspalten, die heute weder als Spalte noch als Rename-Quelle existieren; Schluesselspalten + reine Renames sind ausgenommen, semantische-Rename-Ziele sind separat markiert):
     - `fact_story` (+14 echt neu): `processing_time_ms`, `blocked_ac_count`, `blocked_ac_detail_json`, `adversarial_hit_rate`, `findings_fully_resolved`, `findings_partially_resolved`, `findings_not_resolved`, `final_status`, `are_total_requirements`, `are_covered_requirements`, `increment_count`, `phase_exploration_ms`, `phase_verify_ms`, `computed_at`.
     - `fact_guard_period` (+7 echt neu): `period_grain`, `violation_rate`, `violation_stage_escape`, `violation_stage_schema`, `violation_stage_template`, `escape_detection_count`, `computed_at`.
     - `fact_pool_period` (+9 echt neu): `period_grain`, `response_time_p50_ms`, `verdict_adopted_count`, `verdict_total_count`, `finding_true_positive_count`, `finding_false_positive_count`, `quorum_triggered_count`, `template_finding_rate_json`, `computed_at`. (**P95 explizit NICHT**, FK-62 §62.2.3 `:203` INVENTAR.)
     - `fact_pipeline_period` (24 FK-62-Nicht-Schluessel-Zielspalten, davon **22 echt neu + 2 semantische-Rename-Ziele**): `period_grain`, `story_count`, **`story_count_closed`** (← Rename `stories_completed`), `execution_count`, `exploration_count`, `stage_miss_count`, `stage_miss_detail_json`, `impact_violation_count`, `impact_check_count`, `integrity_gate_block_count`, `integrity_gate_total_count`, `doc_fidelity_conflict_by_level_json`, `first_pass_count`, `finding_survival_count`, `finding_total_count`, `effective_check_ids_json`, `vectordb_total_hits`, `vectordb_above_threshold`, `vectordb_classified_conflict`, `vectordb_duplicate_detected`, `processing_time_avg_ms`, `processing_time_variance_ms2`, **`qa_round_avg`** (← Rename `avg_qa_rounds`), `computed_at`. (Die Pipeline-Tabelle ist effektiv eine Shape-Ersetzung; `stories_completed`→`story_count_closed` und `avg_qa_rounds`→`qa_round_avg` sind semantische Renames, kein Doppelsatz — sie zaehlen daher NICHT zu den „echt neuen" Spalten.)
     - `fact_corpus_period` (+2 echt neu): `period_grain`, `computed_at`.
     - **Summe echt neue Spalten (ohne Schluessel, ohne Renames, ohne semantische-Rename-Ziele): 14+7+9+22+2 = 54.** (Die FK-62-Nicht-Schluessel-Zielspalten der Pipeline-Tabelle sind 24; 2 davon — `story_count_closed`, `qa_round_avg` — sind semantische-Rename-Ziele und in den 11 Renames bzw. den semantischen Renames bereits erfasst, daher hier abgezogen.)
   - **Die Drops** aller Nicht-FK-62-Spalten (Entscheidung gesperrt — FK-62 ist die Wahrheit, kein Konzept-Amendment zur Rettung): `token_input_total`, `token_output_total`, `avg_latency_ms` (FactPoolPeriod), `agentkit_version`, `agentkit_commit` (FactStory), `period_end` (auf allen vier Period-Tabellen), `stories_escalated` (FactPipelinePeriod). `avg_qa_rounds`→`qa_round_avg` und `stories_completed`→`story_count_closed` sind **semantische Renames, keine Drops**; `avg_phase_implementation_ms` hat kein FK-62-Ziel und entfaellt (in `processing_time_avg_ms` aufgehend ist eine Fuelllogik-Entscheidung von AG3-082, hier nur die Spalte gemaess FK-62).
   - **Konzept-Konflikt + Auflösung (FK-61 ↔ FK-62, verbindlich):** FK-61 §61.4.2 (`61_kpi_erhebung_nach_domaenen.md:172`) benennt die Guard-Stage-Zielspalten mit `*_count`-Suffix (`violation_stage_escape_count` / `…schema_count` / `…template_count`), waehrend FK-62 §62.2.2 (`62_kpi_aggregation.md:177-179`) sie **ohne** Suffix fuehrt (`violation_stage_escape` / `violation_stage_schema` / `violation_stage_template`). **Auflösung (gesperrt):** FK-62-Spaltennamen sind die **autoritative** physische Zielspalten-Wahrheit (konsistent mit der bereits vereinbarten Linie „FK-62 = einzige Namens-/Spalten-Wahrheit"). Diese Story baut daher die FK-62-Namen (**ohne** `_count`-Suffix). Die FK-61-§61.4.2-Namen mit `_count`-Suffix sind eine **Dokumentations-Drift**, die ueber eine **separate, reine Doc-Concept-Reconciliation** an den FK-61-Owner korrigiert wird (FK-61 wird in **dieser** Story NICHT editiert). **Vollstaendiger Divergenz-Scan (alle FK-61 `→ fact_*`-Targets gegen das FK-62-Sollschema geprueft):** die einzige Target-Namens-Divergenz ist die Guard-Stage-Triple oben (`:172`); alle uebrigen FK-61-Zielspalten (§61.2-§61.11, Zeilen `:121-343`) loesen 1:1 ins FK-62-Schema auf. Hinweis: FK-61 nennt einige Targets nur teil-qualifiziert via Ellipse (z. B. `:172` `...schema_count`/`...template_count`, `:291` `...above_threshold`/`...classified_conflict`) — bei `:291` ist der Prefix `vectordb_` aus dem ersten Listeneintrag korrekt expandiert FK-62-konform (`vectordb_above_threshold`/`vectordb_classified_conflict`); nur die Guard-Stage-Ellipse `:172` divergiert (Suffix `_count`).
2. **Konsistente Umstellung ueber alle realen Wahrheitsorte** (Korrektur der Mislabelung der alten AG3-083-Story): typisierte Records `fact_store/models.py`; Column-Listen/Conflict/Update `_fact_sql.py`; Row-/Model-Mapper `fact_repository.py`; Postgres-DDL `postgres_schema.sql:974-1048`; SQLite-Schemawahrheit als **neue Migrationsdatei** (siehe §2.1.4), **nicht** durch Editieren von `v_3_4_analytics.sql:18-118`. `sqlite_store._ensure_analytics_tables` traegt **keine** Spalten-DDL (delegiert an MigrationRunner) — dort nichts spalten-aenderndes. Drift an einem Ort = Contract-Test rot.
3. **Ausgelieferte Backend-Konsumenten mitziehen (autorisiert):** `aggregation/worker.py:325`, `top.py:72`/`:80`/`:329`/`:337`, `dashboard/service.py:189`/`:197`/`:201`/`:203`/`:206`, `state_backend/store/analytics_source.py` (Record-Konstruktion auf neue Namen + entfallende Felder entfernen + neue Spalten — Fuelllogik bleibt AG3-082-Territorium, hier nur Shape-Treue) und `fact_repository.py` Read-Pfad (Order-by + `_row_to_fact_*`). Die zugehoerigen Backend-Tests werden angepasst (alte Feldnamen → FK-62-Namen).
4. **Migrations-Entscheidung (gesperrt): KEINE Datenmigration, aber NEUE Versionsstufe `3.6`.** AK3 ist greenfield; die `fact_*`-Tabellen sind **recomputable Rollups** (FK-60 §60 P3 „aus den Rohdaten jederzeit neu berechenbar", `:167`; Refresh-Worker rechnet pro Dirty Set „komplett neu", `:366`), also wegwerf-/neu-aufbaubar. Daraus folgt die Mechanik:
   - **Neue Migrationsdatei `versions/v_3_6_fact_reconciliation.sql`** (Versionsslug `3.6`; `3.5` ist bereits durch `v_3_5_compaction_epochs.sql` belegt). Die Datei **dropt und baut** die fuenf `fact_*`-Tabellen mit dem FK-62-Spaltensatz neu auf (`DROP TABLE IF EXISTS fact_* ` + `CREATE TABLE` je Tabelle; portabel auf SQLite **und** Postgres, wie die bestehenden Versionsdateien). Kein `RENAME COLUMN`, kein PK-`ALTER` (SQLite kann den PK ohnehin nicht in-place altern — Drop+Rebuild ist hier sauberer als ALTER-Akrobatik).
   - **KEINE Datenmigration:** vorhandene Rollup-Zeilen werden beim Drop **verworfen** und vom Refresh-Worker aus den Events neu berechnet (FK-60 §60 P3/§366) — sie sind kein zu erhaltender Datenbestand, sondern eine ableitbare Projektion. Das wird in der Datei und der Story explizit als Begruendung festgehalten.
   - **Registrierung in `migration_runner._MIGRATIONS`** (`:40-43`): die neue Stufe `("3.6","v_3_6_fact_reconciliation.sql")` wird **angehaengt** (additiv-vorwaerts, wie `3.5`). `v_3_4_analytics.sql` und `v_3_5_compaction_epochs.sql` bleiben **unveraendert** (frische DBs durchlaufen 3.4→3.5→3.6 und landen direkt auf dem FK-62-Schema; bereits-3.4/3.5-DBs bekommen 3.6 sauber nachgezogen). **Begruendung der Aenderung gegen das fruehere In-place-Modell:** `run()` ueberspringt bereits angewandte Versionen (`:127`), eine In-place-Editierung von `v_3_4` wuerde auf bestehenden DBs nie wirksam (siehe §1).
   - **`schema_version`-Head wird `3.6`.** Es existiert kein separater Head-Konstanten-Ort im Produktionscode (Grep: der einzige Versions-Registry ist `_MIGRATIONS`); der „Head" ergibt sich aus dem letzten `_MIGRATIONS`-Eintrag.
5. **Spec-Check (Implementer-Pflicht): Tests auf den neuen `3.6`-Head + FK-62-Namen umstellen.** Drei Stellen in `tests/contract/state_backend/test_analytics_schema.py` muessen mit:
   - `test_postgres_init_runs_migration_and_records_schema_version` (`:412-434`) pinnt die **eingetragene** Version hart: Query `WHERE version = '3.4'` (`:430`) + Assertion `== ["3.4"]` (`:434`) + Docstring (`:415`/`:419`). Nach dieser Story ist der recorded Head **`3.6`**; der Test muss belegen, dass `3.6` (sowie die vorgelagerten `3.4`/`3.5`) in `schema_versions` steht — also auf `3.6` umstellen (nicht nur 3.4/3.5).
   - `_REQUIRED_COLUMNS` (`:53-108`) pinnt die **alten** Spaltennamen (`started_at`, `qa_rounds`, `agentkit_version`, …) und MUSS auf den FK-62-Spaltensatz umgeschrieben werden.
   - der Postgres-Roundtrip (`:163-234`) konstruiert Records mit alten Feldern und MUSS auf die FK-62-Namen umgestellt werden.
   Alle drei sind Teil dieser Story. Implementer-Pflicht: vor dem Schreiben verifizieren, dass kein **weiterer** Golden/Snapshot-Test einen alten `3.4`/`3.5`-Head oder alte Spaltennamen anpinnt (sonst mitziehen).
6. **AKTIV-P50-Zielspalte:** `fact_pool_period.response_time_p50_ms` existiert im Sollschema; `response_time_p95_ms` existiert **nicht** und bleibt INVENTAR (FK-62 §62.2.3 `:203`).

### 2.2 Out of Scope (mit Owner)
- **Frontend (`client.ts` WireFact*, `AnalyticsSlot.tsx`)** — **NICHT in dieser Story** (explizit). AG3-116 hat das Frontend bereits vollstaendig auf den FK-62-geformten DTO-Vertrag umgestellt (umbenannte Felder + Entfernung der hier gedroppten Felder aus der UI, siehe §1 und AG3-116 §2.1); das DTO-Mapping wird fuer die umbenannten Keys zur **Identitaet** (interner FK-62-Feldname == FK-62-Wire-Key). Das Frontend bleibt daher unveraendert. (DTO-Owner: AG3-116.)
- **KPI-Katalog-Population (40 KPIs) + FK-61-Mapping + Contract-Tests** — **AG3-118** (`unblocks` dieser Story; AG3-118 braucht die FK-62-Spalten als fail-closed-Aufloesungsziel).
- **RefreshWorker-Fuelllogik** (welche Werte in die neuen Spalten gerechnet werden, `sync_analytics`/`_percentile`/Dirty-Sets) — **AG3-082** (`completed`). Diese Story liefert die **Shape** (Spalten/Typen) und haelt `analytics_source.py` konstruktions-konsistent; die fachliche Befuellung neuer Spalten ueber Default hinaus ist AG3-082-Territorium.
- **Schema-Versionierungsstrategie (Side-by-Side `ak3_v<slug>`)** — unveraendert; diese Story aendert nur den `fact_*`-Spaltensatz und fuehrt ihn ueber eine **neue Migrationsstufe `3.6`** (`v_3_6_fact_reconciliation.sql`, drop+rebuild) ein; die historischen Stufen `v_3_4_analytics.sql`/`v_3_5_compaction_epochs.sql` bleiben unangetastet.
- **P95-Aktivierung** — eigene zukuenftige Story; hier strikt INVENTAR.

## 3. Akzeptanzkriterien
1. Die fuenf `fact_*`-Tabellen tragen **exakt** den FK-62-Sollspaltensatz (§62.2.1-§62.2.5) mit den FK-62-Namen als einziger Wahrheit; alle 11 Renames sind umgestellt, alle 54 echt neuen Spalten ergaenzt (zzgl. der 2 semantischen-Rename-Ziele `story_count_closed`/`qa_round_avg`, siehe §2.1.1), alle Nicht-FK-62-Spalten gedroppt; `are_gate_passed` ist echtes bool/INTEGER.
2. **Identische FK-62-Spaltensaetze ueber alle realen Wahrheitsorte des finalen Schemas** (`fact_store/models.py`, `_fact_sql.py`, `fact_repository.py`-Mapper, `postgres_schema.sql`, **die neue `versions/v_3_6_fact_reconciliation.sql`**) — der Contract-Test belegt Identitaet pro Tabelle; Drift an einem Ort = Test rot. `v_3_4_analytics.sql`/`v_3_5_compaction_epochs.sql` bleiben **unveraendert historische** Versionsschritte (sie bilden das alte Schema ab, das `v_3_6` per drop+rebuild auf FK-62 ueberfuehrt) und sind **nicht** der finale Schema-Wahrheitsort. `_REQUIRED_COLUMNS` (`test_analytics_schema.py:53-108`) ist auf die FK-62-Namen umgeschrieben.
3. **Alle ausgelieferten Backend-Reader/-Konstruktoren aktualisiert** (`worker.py`, `top.py`, `dashboard/service.py`, `analytics_source.py`, `fact_repository.py`); kein Verweis mehr auf einen gedroppten/umbenannten alten Feldnamen im Produktionscode (Grep-belegt).
4. **Frontend unangetastet:** Diff zeigt keine Aenderung an `frontend/` (das DTO aus AG3-116 schirmt ab; AC explizit, um die Trennung zu beweisen).
5. **Migration als neue Versionsstufe, recompute-disposabel:** eine neue Datei `versions/v_3_6_fact_reconciliation.sql` dropt+baut die fuenf `fact_*`-Tabellen auf FK-62 neu auf; `("3.6", …)` ist in `migration_runner._MIGRATIONS` angehaengt; `v_3_4_analytics.sql`/`v_3_5_compaction_epochs.sql` bleiben unveraendert; der recorded `schema_version`-Head ist `3.6`; **keine** Datenmigration (Rollups werden recomputed, FK-60 §60 P3). Der Spec-Check aus §2.1.5 ist erfuellt (`test_postgres_init_runs_migration_and_records_schema_version` ist auf `3.6` umgestellt und gruen; kein weiterer Golden-Test pinnt einen alten Head/alte Spalten).
6. **Postgres-Paritaet:** beide DDL-Dialekte (TIMESTAMPTZ/BIGINT vs. TEXT/INTEGER/REAL) und die Mapper tragen denselben FK-62-Spaltensatz; der Postgres-Roundtrip-Test (`test_analytics_schema.py:163-234`) ist auf die FK-62-Namen umgestellt und gruen.
7. `fact_pool_period.response_time_p50_ms` existiert; `response_time_p95_ms` existiert nicht (Test belegt Abwesenheit).
8. Alle Spalten-/Feld-/Enum-Namen englisch (ARCH-55).
9. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates (GAC-1 conformance); Coverage >= 85 %; Postgres-Paritaet gruen.

## 4. Definition of Done
- AK 1–9 erfuellt; QA-Gate ist die giftige Codex-Review (alleiniges Code-Gate) **PASS** + die Standard-Pflichtbefehle + Jenkins + Sonar. Vorgesehen fuer Implementierung nach Codex-Spec-Review + User-Go-Ahead.
- Hinweis (autorisiert durch User): diese Story aendert bewusst Code zweier bereits `completed` Stories (AG3-082 `analytics_source.py`/`worker.py`; AG3-084 `top.py`/`dashboard/service.py`/`fact_repository.py`) — freigegeben.

## 5. Guardrail-Referenzen
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** FK-62 ist die einzige Spalten-/Namenswahrheit; kein paralleler Spaltensatz, kein „Code-Realitaet"-Ausnahmepfad. Drift ueber die Wahrheitsorte ist Test rot.
- **FAIL-CLOSED:** inkonsistente Spaltensaetze, fehlende Spalten oder ein gedropptes Feld, das ein Reader noch liest, sind Fehler (nicht weggeklickt). `are_gate_passed` ist typisiert bool, kein String-`or "UNKNOWN"`-Notbehelf mehr.
- **ZERO DEBT:** keine halben Renames, kein „spaeter aufraeumen"; alte Feldnamen verschwinden vollstaendig aus Produktionscode und Tests.
- **TYPISIERT STATT STRINGS:** Records als typisierte Modelle; Typwechsel str→bool sauber durchgezogen.
- **ARCH-55:** alle Spalten/Felder/Enums englisch.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- FK-62 §62.2 DDL (`62_kpi_aggregation.md:107-289`) ist die verbatim-Spaltenwahrheit — pro Tabelle 1:1 uebernehmen (inkl. `period_grain`, **kein** `period_end`, PK `guard_key`/`pool_key`, `are_gate_passed INTEGER`).
- Renames+Adds+Drops MUESSEN ueber **alle** Orte gemeinsam laufen: `fact_store/models.py:25-120`, `_fact_sql.py:39-128`, `fact_repository.py` (Mapper + `row["…"]`), `postgres_schema.sql:974-1048` und die **neue** SQLite-Migrationsdatei `versions/v_3_6_fact_reconciliation.sql` (NICHT `v_3_4_analytics.sql`). `sqlite_store._ensure_analytics_tables` NICHT als Spaltenort behandeln (delegiert an MigrationRunner).
- Migration: **neue Versionsdatei `versions/v_3_6_fact_reconciliation.sql`** (Drop & Rebuild der fuenf `fact_*` auf FK-62, portabel SQLite+Postgres), `("3.6","v_3_6_fact_reconciliation.sql")` an `migration_runner._MIGRATIONS` (`:40-43`) **anhaengen**; `v_3_4`/`v_3_5` unveraendert lassen. KEINE Datenmigration — Rollups sind recompute-disposabel (FK-60 §60 P3, `60_kpi_katalog_und_architektur.md:167`/`:366`); in der Datei begruenden. Grund gegen In-place: `run()` ueberspringt bereits angewandte Versionen (`migration_runner.py:127`), eine `v_3_4`-Editierung wuerde auf bestehenden DBs nie wirksam.
- Spec-Check zwingend: `tests/contract/state_backend/test_analytics_schema.py` — `_REQUIRED_COLUMNS` (`:53-108`) + Postgres-Roundtrip (`:163-234`) auf FK-62-Namen umstellen; `test_postgres_init_runs_migration_and_records_schema_version` (`:412-434`, pinnt heute `'3.4'`) auf den neuen `3.6`-Head umstellen. Auch `tests/unit/state_backend/migration/test_migration_runner.py`, `tests/unit/kpi_analytics/fact_store/test_models.py`/`test_store.py`, `tests/contract/kpi_analytics/test_ag3_084_contracts.py`, `tests/integration/kpi_analytics/test_fact_store_roundtrip.py`, `tests/integration/kpi_analytics/test_kpi_endpoints.py`, `tests/unit/state_backend/store/test_analytics_source.py`, `tests/unit/kpi_analytics/test_top.py`, `tests/unit/kpi_analytics/dashboard/test_service.py` ziehen die Feldnamen mit.
- Achtung Verwechslungsgefahr: `analytics_source.py` liest `record.completed_at` vom **Runtime**-`StoryMetricsRecord` (`:156-158`/`:201-205`/`:319`/`:377-378`) — das ist **nicht** der Fact-Record und wird **nicht** umbenannt. Nur die Fact-Record-Konstruktion (`:177`/`:269`/`:384` ff.) anpassen.
- Frontend NICHT anfassen (AG3-116-DTO schirmt ab; DTO-Mapping wird Identitaet). Wenn das Frontend bricht, ist das ein Zeichen, dass AG3-116 nicht/falsch vorausgesetzt ist — melden, nicht das FE „nachziehen".
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff, gruene Pflichtbefehle, Postgres-Paritaet, Test-Namen des Fact-Spalten-Contract-Tests ueber alle Orte + des Migrations-/Schema-Version-Tests.

## 7. Vorbedingungen
- **depends_on AG3-038** (Fact-Schema/Records/Repository — die zu reconcilende Basis), **AG3-081** (angereicherte `integrity_gate_result`/`are_gate_result`-Payloads, deren Ziel-Spalten hier im Sollschema vorhanden sein muessen), **AG3-116** (das Wire-DTO muss den Browser abschirmen, bevor die internen Renames laufen).
- **unblocks AG3-118** — die Katalog-Population braucht die FK-62-Spalten als fail-closed-Aufloesungsziel (AC3 dort).
- **Cross-cutting (doc-only, nicht hier editieren):** `var/concept-gap-analysis/_STORY_INDEX.md` + AG3-082 `status.yaml` (`unblocks: [AG3-083]`) zeigen die alte 082↔083-Reihenfolge falsch herum; da 082/084 `completed`, ist das nur Bookkeeping — Routing an die Owner als Doc-Bereinigung mit Verweis auf AG3-116/117/118 (ersetzen AG3-083).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

# AG3-062: Import-Resolver + Request-DSL + Preflight-Turn

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `verify-system` / Evidence-Assembly (BC3) — die Stufe-2-Kontextanreicherung (Import-Aufloesung) und der nachgelagerte Reviewer-Request-Dialog (Preflight-Turn), aufbauend auf dem Evidence-Assembler-Fundament.
**Quell-Konzepte (autoritativ):**
- `FK-46 §46.2-§46.5` — `ImportResolver` (sprachspezifisch: Python/TS/JS/JSX/TSX/Java + Spring), `ResolvedImport`, `ConfidenceLabel` (StrEnum, 6 Werte), `CONFIDENCE_PRIORITY`-Tabelle, Stufe-2-Eingliederung als `SECONDARY_CONTEXT` in den Assembler; Mehrdeutigkeit → `UNRESOLVED_DYNAMIC`
- `FK-47 §47.2` — `RequestType`-StrEnum (7 Werte: NEED_FILE, NEED_SCHEMA, NEED_CALLSITE, NEED_RUNTIME_BINDING, NEED_TEST_EVIDENCE, NEED_CONCEPT_SOURCE, NEED_DIFF_EXPANSION), `ReviewerRequest`/`RequestResult` (Pydantic)
- `FK-47 §47.3` — `RequestResolver` mit 7 Handler-Methoden (Multi-Repo), `MAX_REQUESTS=8`, `REQUEST_TIMEOUT_S=30`, `parse_preflight_response` (Parse-Fehler → `requests=[]` + WARNING, Review laeuft weiter)
- `FK-47 §47.4` — Mehrdeutigkeitsregel D3 (1 Treffer RESOLVED; mehrere/0 UNRESOLVED)
- `FK-47 §47.5/§47.6` — Preflight-Turn-Ablauf (assemble → preflight-send → resolve_all → extended_paths → review), Fehlertoleranz (Parse-Fehler/alle UNRESOLVED/Timeout → Review laeuft weiter), Template `prompts/sparring/review-preflight.md` mit Sentinel `[PREFLIGHT:review-preflight-v1:{story_id}]`

---

## 1. Kontext / Ist-Zustand (belegt)

FK-46 ist vollstaendig FEHLEND, FK-47 ueberwiegend FEHLEND (nur die Telemetrie-Vertragsseite existiert). Belegt:

- Grep `ImportResolver|ConfidenceLabel|ResolvedImport|RESOLVED_IMPORT|SAME_PACKAGE_HEURISTIC` ueber `src/` → 0 Treffer (gap-fk-46-56.md §FK-46).
- Grep `RequestType|ReviewerRequest|NEED_FILE|RequestResolver|parse_preflight_response` → keine Request-DSL-Implementierung; kein `review-preflight`-Template (gap-fk-46-56.md §FK-47).
- **Vorhandene Telemetrie-Vertragsseite** (NICHT die Request-DSL): `src/agentkit/telemetry/contract/preflight_sentinel.py` (`PreflightSentinel`, FK-68 §68.9 Balance-Regel; `PREFLIGHT_BALANCE_RULE_ID`/`PREFLIGHT_MISSING`/`PREFLIGHT_NOT_COMPLIANT` ab `:41`, Klasse `PreflightSentinel` `:50`) + EventTypes `PREFLIGHT_REQUEST/RESPONSE/COMPLIANT` in `telemetry/events.py:61-63`. **Latenter Konsistenzdruck**: die Balance-Regel kann real nie erfuellt werden, solange niemand Preflight-Events emittiert — diese Story schliesst die produzierende Seite.
- Das Fundament (EvidenceAssembler/BundleManifest/`merge_paths`/Stufe-2-Eingang) kommt aus **AG3-061** und ist Vorbedingung. Der Stub-Ort ist real: `src/agentkit/verify_system/evidence/__init__.py` ist ein **Leerstub** (nur `from __future__ import annotations`, 1 Zeile) — hier wird AG3-062 additiv einhaengen, sobald AG3-061 das Fundament gelegt hat.

**Modulpfad (FK-§ vs. AK3-BC-Schnitt, belegt):** FK-46 §46.6 (Code-Block ab `46_import_resolver.md:329`, `# agentkit/evidence/import_resolver.py`) und FK-47 §47.2/§47.3 (`47_request_dsl_und_preflight_turn.md:55`/`:119`, `# agentkit/evidence/request_types.py`/`request_resolver.py`) nennen das **konzeptuelle** Package `agentkit/evidence/`. Im realen AK3-BC-Schnitt liegt dieses Package unter `agentkit/verify_system/evidence/` (das ist der vorhandene Leerstub-Ort, identisch zur AG3-061-Abweichungsregel in `AG3-061 story.md:69`). AG3-062 nutzt **denselben** Ort; **kein** zweites Top-Level-`evidence/`. Die konkreten Zieldateien stehen in Scope 2.1.

**Konfliktcheck (Kontext-Sinnhaftigkeit):** Es gibt keine konkurrierende Resolver-/Request-Implementierung. Wichtig: Der **Setup**-Preflight-Gate (`governance/setup_preflight_gate/preflight.py`) ist ein **anderes** Konzept (Story-Modus-Preflight) und darf nicht damit vermischt werden — die FK-47-„Preflight"-Begrifflichkeit meint den Reviewer-Request-Turn vor dem eigentlichen Review. Die hier emittierten `PREFLIGHT_REQUEST/RESPONSE/COMPLIANT`-Events muessen exakt zur bestehenden `PreflightSentinel`-Balance-Regel passen (kein zweites Sentinel-Format).

**Transport-Abgrenzung zu AG3-065 (belegt, FIX-THE-MODEL):** FK-47 §47.5 (`47_request_dsl_und_preflight_turn.md:275-281`) sagt **ausdruecklich**, der Preflight-Turn laeuft **NICHT** ueber den `LlmEvaluator`/`StructuredEvaluator` (FK-11), sondern als **direkter MCP-Pool-Call** (`chatgpt_send(prompt, merge_paths)`, Sequenzdiagramm `:293`/`:304`). Der vorhandene `LlmClient`-Port (`verify_system/llm_evaluator/llm_client.py:55`, `complete(*, role, prompt) -> str`) ist **bewusst** datei-frei (kein `merge_paths`/`file_paths`) und gehoert dem Layer-2-Evaluator — er ist fuer den Preflight-Send **nicht** der richtige Port. AG3-065 (`AG3-065 story.md:34`/`:80`/`:82`/`:121`) bestaetigt **beides** explizit: (a) AG3-062 ist **kein garantierter Konsument** der dortigen `HubLlmClient`-Klasse (Index ordnet AG3-062 vor AG3-065, `depends_on: AG3-061`, ohne AG3-065-Dependency), und (b) AG3-065 liefert **kein** Datei-Handling (`merge_paths`/`file_paths`) und erweitert weder den schmalen Port noch `HubClient.send`. **Folge fuer den Schnitt:** AG3-062 darf den Preflight-Send **nicht** an einen AG3-065-Port haengen, der die noetige Dateiuebergabe gar nicht traegt. Stattdessen definiert AG3-062 seinen **eigenen, schmalen, datei-faehigen Preflight-Send-Port** (siehe Scope 2.1.5/2.1.6) und bleibt ohne injizierten produktiven Transport fail-closed — analog zum `FailClosedLlmClient`-Muster (`llm_client.py:77-112`). Damit ist AG3-062 **unabhaengig** von AG3-065 lieferbar; AG3-065 ist **keine** harte Vorbedingung.

## 2. Scope

### 2.1 In Scope

Alle neuen Python-Module liegen unter **`src/agentkit/verify_system/evidence/`** (vorhandener Stub-Ort, kein zweites Top-Level-`evidence/`; Modulpfad-Abweichung siehe §1):

1. **`verify_system/evidence/import_resolver.py`** (FK-46): `ImportResolver` mit sprachspezifischen Resolvern fuer Python/TS/JS/JSX/TSX/Java (inkl. Spring-Heuristik). Datenklassen `ResolvedImport` und `ConfidenceLabel` (StrEnum, **genau 6 Werte** gem. FK-46 §46.5, englisch), `CONFIDENCE_PRIORITY`-Tabelle (Ordnung der Labels). Mehrfachtreffer → `UNRESOLVED_DYNAMIC` (FK-46/FK-47 §47.4).
2. **Stufe-2-Eingliederung in den Assembler**: aufgeloeste Imports werden als `SECONDARY_CONTEXT` (FK-46 §46.1/§46.5; Eingliederung in den `BundleEntry`-Strom) ueber den von AG3-061 bereitgestellten Stufe-2-Eingang in das Bundle/Manifest eingespeist — kein Umbau des Assembler-Kerns.
3. **`verify_system/evidence/request_types.py`** (FK-47 §47.2): `RequestType`-StrEnum (7 Werte, englisch), `ReviewerRequest` und `RequestResult` als Pydantic-v2-Modelle (frozen, `extra="forbid"`).
4. **`verify_system/evidence/request_resolver.py`** (FK-47 §47.3): `RequestResolver` mit **7 Handler-Methoden** (eine pro `RequestType`), Multi-Repo-faehig (nutzt `RepoContext` aus AG3-061), Limits `MAX_REQUESTS=8` und `REQUEST_TIMEOUT_S=30` als benannte Konstanten, `resolve_all(...)`-Aggregation, Mehrdeutigkeitsregel D3 (1 Treffer → RESOLVED; mehrere/0 → UNRESOLVED). Modul-Funktion `parse_preflight_response(raw_response: str) -> list[ReviewerRequest]` exakt nach FK-47 §47.3 (`47_request_dsl_und_preflight_turn.md:138-149`): bei Parse-Fehler (`json.JSONDecodeError`/`KeyError`/`TypeError`/`ValueError`) → **leere Liste + WARNING geloggt**, **kein** fail-closed Abbruch; bei Erfolg → `ReviewerRequest`-Objekte aus `raw_requests[:MAX_REQUESTS]` (deterministische Begrenzung auf 8, siehe 2.1.4b).
   - **4b. `MAX_REQUESTS`-Ueberlauf (FK-normiert, deterministische Kappung):** Liefert die Reviewer-Antwort **mehr** als `MAX_REQUESTS=8` Requests, werden gem. FK-47 §47.3 (`raw_requests[:MAX_REQUESTS]`, `:147`) und `resolve_all` (`requests[:MAX_REQUESTS]`, `:179`) deterministisch die **ersten 8** verarbeitet; der Ueberschuss wird **verworfen** und als **WARNING** gespiegelt (CLAUDE.md SEVERITY-SEMANTIK — kein stilles Wegwerfen). Dies ist **kein** fail-closed Abbruch: die Kappung ist die FK-normierte Determinismus-Regel, nicht ein Fehlerzustand. Der WARNING ist dem Auftraggeber zu spiegeln, falls Ueberlauf in der Praxis haeufig auftritt.
5. **Preflight-Turn-Ablauf** (FK-47 §47.5): die deterministische Orchestrierung assemble → preflight-send → `parse_preflight_response` → (falls Requests) `resolve_all` → `extended_paths` → Review-Bundle-Anreicherung, exakt nach dem FK-47-§47.5-Ablaufschema (`47_request_dsl_und_preflight_turn.md:311-326`). Der LLM-Send geht ueber den in 2.1.6 definierten **eigenen** Preflight-Send-Port (datei-faehig), **nicht** ueber den datei-freien Layer-2-`LlmClient`-Port und **nicht** ueber AG3-065 (siehe §1 Transport-Abgrenzung). **Fehlertoleranz exakt nach FK-47 §47.5 (`:328-335`):** Parse-Fehler → `requests=[]` + WARNING, Review laeuft mit Original-Bundle weiter; alle Requests UNRESOLVED → Review laeuft mit Original-Bundle weiter (Reviewer wird ueber unaufloesbare Requests informiert); Timeout bei `NEED_TEST_EVIDENCE` → `status="TIMEOUT"` fuer diesen Request, **andere Requests werden trotzdem aufgeloest**. Es gibt im Preflight-Turn **keinen** fail-closed Gesamtabbruch wegen invalider Reviewer-Antwort.
6. **Eigener Preflight-Send-Port** (`verify_system/evidence/`): ein **schmaler, datei-faehiger** Port (z. B. `PreflightReviewSender`-Protocol mit einer Methode `send(*, prompt: str, merge_paths: Sequence[Path]) -> str`), den der Preflight-Turn konsumiert. Dieser Port traegt — anders als der Layer-2-`LlmClient`-Port — die **Dateiuebergabe** (`merge_paths`), die FK-47 §47.5 (`chatgpt_send(prompt, merge_paths)`) verlangt. AG3-062 liefert den **Port** + einen **fail-closed Default-Adapter** (`FailClosedPreflightReviewSender`, analog `FailClosedLlmClient` `llm_client.py:77-112`): solange kein produktiver MCP-Pool-Transport injiziert ist, schlaegt der Send **hart** fehl (kein stiller Skip, kein Degradieren auf Stub-Reviewer). Ein konkreter MCP-Pool-/Hub-Adapter hinter diesem Port ist **Out of Scope** (siehe 2.2) — AG3-062 baut den **Turn** und den **Port**, nicht den konkreten Transport. **Kein** zweiter Routing-/Transport-Wahrheitskanal: der Port bleibt schmal und injizierbar.
7. **Preflight-Telemetrie produzieren**: Emission von `PREFLIGHT_REQUEST`/`PREFLIGHT_RESPONSE`/`PREFLIGHT_COMPLIANT` (`telemetry/events.py:61-63`) passend zur bestehenden `PreflightSentinel`-Balance-Regel (FK-68 §68.9, `telemetry/contract/preflight_sentinel.py:50`) — schliesst den latenten Konsistenzdruck.
8. **Template `prompts/sparring/review-preflight.md`** mit eigenem Sentinel `[PREFLIGHT:review-preflight-v1:{story_id}]` (Template-Datei unter `resources/`; Python-Producer/Sentinel-Konstante im Code). Sentinel-Format konsistent zum `[PREFLIGHT:...]`-Praefix (FK-47 §47.6, `47_request_dsl_und_preflight_turn.md:394`) und bewusst **nicht** vom `[TEMPLATE:...]`-Review-Guard-Regex erfasst (FK-30/FK-68-Isolation). **Manifest-Registrierung (Pflicht):** das neue Template wird im Prompt-Manifest `src/agentkit/resources/internal/prompts/manifest.json` (heute Version `"2"`, Eintraege `worker-*`/`qa-*`, `:4-45`) als neuer `templates`-Eintrag mit `relpath` + `sha256`-Hash registriert; die `bundle_version` wird entsprechend hochgezogen. Hinweis: die bestehenden Bundles liegen unter `internal/prompts/`; `prompts/sparring/` ist der FK-47-Soll-Pfad fuer Sparring-Templates — der konkrete `relpath` folgt der real im Repo verwendeten Prompt-Bundle-Konvention (am bestehenden Manifest-Muster ausrichten, kein zweites Manifest).
9. **Negativ-/Grenzpfade** als Tests (siehe AK):
   - dynamischer/mehrdeutiger Import → `UNRESOLVED_DYNAMIC`/UNRESOLVED (FK-46/§47.4 D3),
   - `> MAX_REQUESTS` Requests → **deterministische Kappung auf 8 + WARNING** (FK-normiert, **kein** fail-closed Abbruch),
   - Timeout bei `NEED_TEST_EVIDENCE` → `status="TIMEOUT"` fuer diesen Request, **andere Requests laufen weiter** (FK-47 §47.5),
   - schema-invalide Preflight-Antwort → **`parse_preflight_response` liefert leere Liste + WARNING; der Preflight-Turn laeuft mit Original-Bundle weiter** (FK-47 §47.3/§47.5),
   - fehlender produktiver Preflight-Send-Transport → **fail-closed** ueber den Default-Adapter (kein stiller Skip).

### 2.2 Out of Scope (mit Owner)
- **EvidenceAssembler/BundleManifest/AuthorityClass/`RepoContext`/`merge_paths`/CLI-Fundament** — **AG3-061** (FK-28). Vorbedingung, wird hier nur konsumiert/erweitert (Stufe-2-Eingang, `RepoContext`).
- **Konkreter MCP-Pool-/Hub-Transport-Adapter hinter dem Preflight-Send-Port (Pool-Acquire/Send/Release, dreistufige Antwortverarbeitung, Timeouts-Verdrahtung)** — **AG3-065** (FK-11) bzw. eine Transport-Story. AG3-065 liefert den Layer-2-`HubLlmClient` (datei-frei) und ist laut eigenem Cut **kein** Lieferant der hier benoetigten datei-faehigen Send-Oberflaeche (`AG3-065 story.md:80`/`:82`). AG3-062 definiert daher seinen **eigenen** schmalen Preflight-Send-Port (2.1.6) und bleibt ohne injizierten produktiven Adapter fail-closed; der konkrete Adapter ist **nicht** Teil dieses Cuts. AG3-065 ist deshalb **keine** harte Dependency (siehe Cross-Story-Voraussetzungen / Cut-Hinweis unten).
- **Adversarial-Sparring-Runtime / Test-Promotion** — AG3-079 (FK-48).

**Cut-Hinweis zur Transport-Dependency:** Der Master-Index (`_STORY_INDEX.md:53`) schneidet AG3-062 mit `depends_on: AG3-061` (kein AG3-065). AG3-065 (`status: draft`, `depends_on: AG3-043, AG3-075`) bestaetigt spiegelbildlich, dass AG3-062 kein garantierter Konsument ist und AG3-065 kein Datei-Handling liefert. Der hier gewaehlte **eigene** Preflight-Send-Port haelt AG3-062 damit **konsistent** zum Index-Schnitt: keine erfundene AG3-065-Dependency, kein Anspruch auf eine Faehigkeit (datei-faehiger Send), die AG3-065 laut Scope **nicht** liefert.

## 3. Akzeptanzkriterien
1. `ImportResolver` loest Importe fuer Python/TS/JS/JSX/TSX/Java (inkl. Spring) auf; `ConfidenceLabel` hat **genau 6 englische Werte**; `CONFIDENCE_PRIORITY` ordnet sie (Test je Sprache + Prioritaetsordnung). Module liegen unter `src/agentkit/verify_system/evidence/`.
2. Mehrdeutiger/dynamischer Import → `UNRESOLVED_DYNAMIC`; eindeutiger → `ResolvedImport` mit Label (Test fuer beide Faelle).
3. Aufgeloeste Imports erscheinen als `SECONDARY_CONTEXT` im von AG3-061 gebauten Bundle/Manifest (Integrationstest gegen den Stufe-2-Eingang, kein Assembler-Kern-Umbau).
4. `RequestType` hat **genau 7 englische Werte**; `ReviewerRequest`/`RequestResult` sind frozen Pydantic-Modelle (Test inkl. `extra="forbid"`/Frozen).
5. `RequestResolver` hat **7 Handler** (einen pro Typ), respektiert `MAX_REQUESTS=8` und `REQUEST_TIMEOUT_S=30`:
   - Test: jeder Handler einmal aufgerufen;
   - **`> MAX_REQUESTS` → deterministische Kappung auf die ersten 8 + WARNING** (Test belegt: 9.+ Request wird **nicht** verarbeitet, WARNING wird emittiert; **kein** fail-closed Abbruch — FK-47 §47.3 `requests[:MAX_REQUESTS]`);
   - **`NEED_TEST_EVIDENCE`-Timeout → `RequestResult.status="TIMEOUT"`** fuer diesen Request, **andere Requests im selben `resolve_all` werden trotzdem aufgeloest** (Test — FK-47 §47.5 `:334-335`).
6. Mehrdeutigkeitsregel D3: 1 Treffer → RESOLVED, mehrere/0 → UNRESOLVED (Test).
7. Preflight-Turn-Ablauf orchestriert assemble → preflight-send → `parse_preflight_response` → `resolve_all` → `extended_paths` deterministisch (FK-47 §47.5):
   - **gueltige** JSON-Antwort (`{"requests": [...]}`) → korrekte `list[ReviewerRequest]`, Requests werden aufgeloest, `extended_paths` enthaelt die RESOLVED-Pfade (Test);
   - **schema-invalide** Antwort (kein JSON / falsches Schema / fehlender `requests`-Key) → `parse_preflight_response` liefert **leere Liste + WARNING**, der Preflight-Turn **laeuft mit dem Original-Bundle weiter** (kein fail-closed Parse-Abbruch; der WARNING wird emittiert und **nicht** still verschluckt) — Test belegt beides (leere Liste **und** Weiterlauf **und** emittierter WARNING; FK-47 §47.3/§47.5);
   - **alle Requests UNRESOLVED** → Review laeuft mit Original-Bundle weiter (Test, FK-47 §47.5 `:332-333`).
8. `PREFLIGHT_REQUEST/RESPONSE/COMPLIANT`-Events werden so emittiert, dass die `PreflightSentinel`-Balance-Regel erfuellbar wird (Test gegen `telemetry/contract/preflight_sentinel.py`); Sentinel `[PREFLIGHT:review-preflight-v1:{story_id}]` ist konsistent und wird **nicht** vom `[TEMPLATE:...]`-Review-Guard-Regex erfasst.
9. **Template + Manifest:** `prompts/sparring/review-preflight.md` existiert (mit Preflight-Sentinel) **und** ist im Prompt-Manifest `src/agentkit/resources/internal/prompts/manifest.json` mit `relpath` + korrektem `sha256`-Hash registriert (`bundle_version` hochgezogen); Contract-Test belegt, dass der Manifest-Hash zum Template-Inhalt passt (analog zum bestehenden Manifest-Muster `:4-45`).
10. **Preflight-Send-Port:** Der datei-faehige Preflight-Send-Port (`send(*, prompt, merge_paths)`) existiert; ohne injizierten produktiven Transport schlaegt der `FailClosedPreflightReviewSender` **hart fehl** (Test: fail-closed, kein stiller Skip). Der Layer-2-`LlmClient`-Port (`llm_client.py`) bleibt **unveraendert** datei-frei (kein `merge_paths`-Leak in den Layer-2-Port).
11. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage ≥ 85 %.

## 4. Definition of Done
- AK 1–11 erfuellt; giftige Codex-Review PASS; Implementierung/Commit erst nach Execution-Plan-Freigabe.

## 5. Guardrail-Referenzen
- **FAIL CLOSED (wo FK es vorsieht):** dynamische/mehrdeutige Importe → `UNRESOLVED_DYNAMIC`/UNRESOLVED; fehlender produktiver Preflight-Send-Transport → harter Stopp ueber den Default-Adapter (kein stiller Skip, keine Degradierung auf Stub-Reviewer). **Abgrenzung (FK-normiert, NICHT fail-closed):** Parse-Fehler/schema-invalide Reviewer-Antwort, `MAX_REQUESTS`-Ueberlauf und `NEED_TEST_EVIDENCE`-Timeout sind **deterministische Toleranz-Pfade** (leere Liste + WARNING / Kappung + WARNING / `status="TIMEOUT"`), der Review laeuft jeweils weiter (FK-47 §47.3/§47.5). Diese Befunde sind **WARNING** (Handlungsauftrag mit aufschiebender Wirkung, dem Auftraggeber zu spiegeln), **kein** ERROR-Abbruch — so verlangt es die autoritative FK-47-Norm.
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** Stufe-2-Anreicherung ueber den AG3-061-Eingang, kein zweites Bundle/Manifest; ein Preflight-Sentinel-Format (das vorhandene), kein zweites; ein **eigener, schmaler** Preflight-Send-Port (datei-faehig) statt Missbrauch des datei-freien Layer-2-`LlmClient`-Ports — kein `merge_paths`-Leak in den Layer-2-Port, keine erfundene AG3-065-Dependency.
- **TYPISIERT STATT STRINGS:** `ConfidenceLabel`/`RequestType` als Enums; `ReviewerRequest`/`RequestResult`/`ResolvedImport` typisiert; der Preflight-Send-Port als Protocol.
- **ARCH-55:** alle Identifier/Enum-Werte/Wire-Keys englisch; das Template-Sentinel ist eine technische Konstante (englisch).
- **ZERO DEBT:** alle 7 Request-Handler + alle 6 Confidence-Labels real implementiert; kein „spaeter" fuer einzelne Sprachen/Handler; der WARNING-Pfad (Parse-Fehler/Ueberlauf) wird real emittiert, nicht still verschluckt.

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- **Module unter `src/agentkit/verify_system/evidence/`** anlegen (`import_resolver.py`, `request_types.py`, `request_resolver.py`, Preflight-Turn + Port). FK nennt `agentkit/evidence/` — der reale AK3-BC-Ort ist `verify_system/evidence/` (vorhandener Leerstub, identisch zur AG3-061-Regel). **Kein** zweites Top-Level-`evidence/`.
- **`parse_preflight_response` exakt FK-47 §47.3 (`47_request_dsl_und_preflight_turn.md:138-149`):** Parse-Fehler → **leere Liste + WARNING geloggt**, **NICHT** fail-closed werfen. Der Preflight-Turn laeuft dann ohne Preflight-Ergaenzung mit dem Original-Bundle weiter (FK-47 §47.5 `:330-331`). Den WARNING real emittieren (CLAUDE.md SEVERITY-SEMANTIK), nicht still verschlucken.
- **`MAX_REQUESTS`-Ueberlauf:** FK-47 kappt deterministisch via `raw_requests[:MAX_REQUESTS]` (`:147`) bzw. `requests[:MAX_REQUESTS]` (`:179`) — **erste 8 verarbeiten, Rest verwerfen + WARNING**, **kein** harter Abbruch.
- **Preflight-Send NICHT ueber den Layer-2-`LlmClient`-Port** (`verify_system/llm_evaluator/llm_client.py:55`, `complete(*, role, prompt) -> str` — datei-frei, gehoert dem StructuredEvaluator). FK-47 §47.5 (`:275-281`) sagt: direkter MCP-Pool-Call mit `merge_paths`, **nicht** `LlmEvaluator`/`StructuredEvaluator`. Einen **eigenen** schmalen, datei-faehigen Preflight-Send-Port (`send(*, prompt, merge_paths)`) + `FailClosed`-Default bauen (Muster: `FailClosedLlmClient` `llm_client.py:77-112`). Den konkreten MCP-Pool-Adapter **nicht** hier bauen (Out of Scope) und **keine** AG3-065-Dependency setzen — AG3-065 liefert laut eigenem Cut **kein** Datei-Handling (`AG3-065 story.md:80`/`:82`).
- Das Sentinel/Event-Format MUSS zu `telemetry/contract/preflight_sentinel.py` (`:50`) + `telemetry/events.py` (`PREFLIGHT_*`, `:61-63`) passen — nichts Paralleles bauen; diese Story ist der fehlende Producer fuer die schon vorhandene FK-68-Balance-Regel.
- **Template-Manifest:** das neue `review-preflight.md` im Prompt-Manifest `src/agentkit/resources/internal/prompts/manifest.json` (`:4-45`) mit `relpath` + `sha256` registrieren und `bundle_version` hochziehen; am bestehenden Eintrags-Muster ausrichten, kein zweites Manifest. Contract-Test gegen den Hash.
- **Nicht** mit dem Setup-Preflight-Gate (`governance/setup_preflight_gate/preflight.py`) verwechseln — das ist Story-Modus-Preflight, ein anderes Konzept.
- AG3-061 ist harte Vorbedingung (Stufe-2-Eingang, `RepoContext`). Wenn der Eingang fehlt/unpassend ist: melden, nicht den Assembler hier umbauen.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle, Testnamen (Sprachen, 7 Handler, D3, Preflight-Turn mit gueltiger UND invalider Antwort, `MAX_REQUESTS`-Kappung+WARNING, `NEED_TEST_EVIDENCE`-Timeout-mit-Weiterlauf, Telemetrie-Balance, fail-closed Send-Port, Manifest-Hash).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

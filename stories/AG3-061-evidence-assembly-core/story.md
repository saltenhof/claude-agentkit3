# AG3-061: Evidence-Assembly Core (EvidenceAssembler / AuthorityClass / BundleManifest / CLI)

**Typ:** Implementation
**Groesse:** L
**Bounded Context:** `verify-system` / Evidence-Assembly (BC3) — die deterministische Vorbereitung des Review-Kontexts. Liefert dem Worker die `merge_paths` fuer den Review-Versand, statt dass der Worker den Kontext selbst kuratiert.
**Quell-Konzepte (autoritativ):**
- `FK-28 §28.2` — Package `agentkit/evidence/` (im AK3-BC-Schnitt: `verify_system/evidence/`)
- `FK-28 §28.3` — `EvidenceAssembler` (3-Stufen-Assembly: Stufe 1 deterministischer Kern, Stufe 2 Import-Extraktion (FK-46, hier nur Eingang), Stufe 3 Worker-Hints), `RepoContext` (Multi-Repo, §28.3.2), 350-KB-Limit + Priorisierung (§28.3.6)
- `FK-28 §28.5` — `AuthorityClass` (IntEnum, vier Stufen, §28.5.1), `BundleEntry` (§28.5.2), `BundleManifest` (`evidence_epoch`/`manifest_hash`/`render_prompt_header`, §28.5.3), Evidence-Epoch (§28.5.4)
- `FK-28 §28.7` — CLI `agentkit evidence assemble` (§28.7.1)
- `FK-28 §28.8` — Integration in den Review-Flow; in Scope ist hier **nur** der Python-Producer `render_prompt_header()` (FK-28 §28.5.3, der Header-Text-Owner). Der in §28.8.3 normierte Template-Platzhalter `{{BUNDLE_MANIFEST_HEADER}}` und die fuenf `prompts/sparring/review-*.md`-Dateien sind im AK3-Cut **nicht buildbar** (FK-vs-Code-Drift, s. §1 „Header-Producer vs. Review-Template-Drift") und werden als doc-only-Nachzug geroutet — **kein** Resource-/Hydration-Auftrag dieser Story.
- `FK-28 §28.9` — Test-Strategie (Authority/BundleManifest = Unit, Assembler = Integration mit Git-Repo-Fixture, Multi-Repo-Fixture)
- `FK-26 §26.5a` — Worker kuratiert keine `merge_paths` mehr selbst; der `EvidenceAssembler` liefert sie
- `FK-26 §26.5b` — Preflight-Turn baut auf dem assemblierten Bundle auf (hier nur Fundament; Turn selbst = AG3-062)

---

## 1. Kontext / Ist-Zustand (belegt)

Die gesamte Evidence-Assembly-Capability fehlt. Belegt:

- `src/agentkit/verify_system/evidence/__init__.py` ist ein **Leerstub** (nur `from __future__ import annotations`, 1 Zeile). Kein `assembler.py`, `authority.py`, `bundle_manifest.py`, `repo_context.py`.
- Grep `class EvidenceAssembler|class AuthorityClass|class BundleManifest|class BundleEntry|class RepoContext` ueber `src/` → 0 Treffer (gap-fk-26-35.md §FK-28).
- `src/agentkit/verify_system/llm_evaluator/bundle.py:9-20` deklariert den `EvidenceAssembler` und `ContextSufficiencyBuilder` explizit als **out of scope for this first wave**; `build_review_bundle` (`bundle.py:130-196`) liest aus `Layer2ReviewInput` (die vorhandene SSOT der vier FK-27 §27.5.2-Textinputs) und trunkiert simpel (`MAX_DIFF_CONTENT_BYTES=100KB` `bundle.py:37`, `MAX_BUNDLE_TOTAL_BYTES=200KB` `bundle.py:39`).
- Im Code vorkommende Begriffe sind **andere** Konzepte, **nicht** das FK-28-BundleManifest, und duerfen damit nicht verwechselt/dupliziert werden:
  - `evidence_fingerprint` (`story_context_manager/models.py:144`, `:185-187`; `implementation/phase.py:237/758`) ist der QA-Cycle-SHA-256-Fingerprint.
  - `evidence_epoch` existiert **bereits** als `datetime`-Feld auf dem QA-Cycle/`ImplementationPayload` (`implementation/phase.py:236/738/757` „UTC-aware timestamp of the cycle's last mutation"; `exploration/review/design_review.py:365`). Das FK-28-`BundleManifest.evidence_epoch` (ISO-8601-**String** der Assembly, FK-28 §28.5.4) ist ein **anderes** Feld in einem anderen Modell — gleicher Name, andere Wahrheit. Es wird hier **nicht** an das bestehende Cycle-Feld angedockt.
  - `evidence_manifest` kommt im Produktivcode **nicht** vor (Grep `evidence_manifest` ueber `src/agentkit` ohne `.pyc` → 0 Treffer).
- CLI: `src/agentkit/cli/main.py` kennt nur `install`/`uninstall`/`run-story`/`doctor`/`serve-control-plane` (`subparsers.add_parser` Zeilen 41/104/110/131/134; Dispatch `:151-160`). Kein `evidence`-Subkommando.

**Konfliktcheck (Kontext-Sinnhaftigkeit):** Es gibt **keinen** konkurrierenden EvidenceAssembler; das neue Package ist additiv. Wichtig: `build_review_bundle` (`llm_evaluator/bundle.py`) ist die existierende Layer-2-Bundle-Maschinerie und darf **nicht** dupliziert werden. Das hier gebaute `BundleManifest`/`AuthorityClass` ist die **Review-Versand-Vorbereitung** (welche Pfade gehen an den Reviewer), nicht das Layer-2-Eingabemodell. Der Section-aware-Packing-/`ContextBundle`-Teil von FK-37 ist **NICHT** hier (das ist AG3-067). Diese Story liefert das Assembly-Fundament + Manifest + CLI; den Import-Resolver und die Request-DSL/Preflight-Turn liefert AG3-062.

**Diff-/Git-Owner (FIX THE MODEL — kein zweiter git-diff-Reader):** FK-28 §28.3.6 nennt als „benoetigte Erweiterung" einen `GitOperations.diff_name_only()` in `agentkit/core/git.py`. Diese Klasse und dieses Modul **existieren im AK3-Tree nicht** (Grep `class GitOperations|diff_name_only|def diff` ueber `src/agentkit` → 0 Treffer; Glob `src/agentkit/**/git*.py` → nur `utils/git.py` (reine Worktree-Helfer, **keine** Diff-API) und `installer/github_coordinates.py`). Den SYSTEM-`git diff --name-only`-Owner gibt es im AK3-Cut aber bereits: die Read-Port-Abstraktion `ChangeEvidencePort` mit dem Value-Object `ChangeEvidence` (`verify_system/structural/system_evidence.py:43-118`; `changed_files: tuple[str, ...]` = „All changed paths in the diff (`git diff --name-only` since the base ref)", `:68-69`), produktiv verdrahtet vom Composition-Root via `_SubprocessGitChangeEvidenceProvider` (`bootstrap/composition_root.py:662`, Klasse `:779-835`, `git diff --name-only {base}..HEAD` `:830-835`, `merge-base`-Aufloesung inbegriffen). `verify_system` bleibt dabei subprocess-/closure-frei (BC-Topologie, AG3-035). Der Stufe-1-Changed-File-Anteil des Assemblers konsumiert deshalb **diesen** vorhandenen Port — kein `core/git.py`/`GitOperations`-Neubau und **kein** zweiter Diff-Reader (siehe In-Scope 1 + Cross-Story/Doc-only-Nachzug).

**Header-Producer vs. Review-Template-Drift (FIX THE MODEL — kein unbuildbarer Resource-Auftrag):** FK-28 §28.8.3 (`28_evidence_assembly_review_vorbereitung.md:936-951`) verlangt prosalisch, dass „alle Review-Templates im Prompt-Bundle unter `prompts/sparring/`" den Platzhalter `{{BUNDLE_MANIFEST_HEADER}}` erhalten, und nennt fuenf Dateien (`review-consolidated.md`, `review-spec-compliance.md`, `review-implementation.md`, `review-test-sparring.md`, `review-synthesis.md`). **Beides existiert im AK3-Cut nicht** und ist so **nicht buildbar**:
  - Es gibt **kein** `prompts/sparring/`-Verzeichnis und **keine** dieser fuenf Dateien (Glob `resources/**/prompts/**/*.md` → nur flache `internal/prompts/worker-*.md` + `qa-*.md`; Glob `**/sparring/**` → 0 Treffer). Die realen Review-Templates sind `qa-review`/`qa-semantic-review`/`qa-doc-fidelity`/`qa-adversarial-review` (`resources/internal/prompts/manifest.json:29/33/37/41`, role-mapping `llm_evaluator/structured_evaluator.py:172-176`).
  - Die reale Platzhalter-**Hydration** ist **nicht** eine `{{...}}`-Doppelklammer-Substitution, sondern `str.format_map` mit Einfach-Klammer-Platzhaltern aus `_build_placeholder_map` (`prompt_runtime/composer.py:123-146/288-290`); Owner ist der **prompt-runtime-BC**, nicht der Evidence-Assembler und nicht ein Review-Turn.
  Damit ist der reale Owner des Header-**Texts** der Python-Producer `BundleManifest.render_prompt_header()` (FK-28 §28.5.3, real im Konzept `28_…:746`) — der gehoert **in** AG3-061 und ist hier voll in Scope. Das **Verdrahten** dieses Header-Texts in ein konkretes Template (egal ob als `format_map`-Key oder Doppelklammer) ist Sache des prompt-runtime-Renderers; die FK-28-§28.8.3-Prosa (Pfad `prompts/sparring/`, Dateinamen, `{{…}}`-Konvention) ist FK-vs-Code-Drift und gehoert — analog zur §28.3.6-Diff-Owner-Drift — in den **doc-only Konzept-Nachzug** (Welle 10), **nicht** in diesen Code-Cut. AG3-061 liefert den Producer und meldet den Drift; es legt **keine** nicht existenten Template-Dateien an und beansprucht **keine** Turn-Hydration (siehe Out-of-Scope + Cross-Story).

## 2. Scope

### 2.1 In Scope
1. **Neues Package `verify_system/evidence/`** (Python-frei-Regel betrifft nur `resources/`; hier ist Produktionscode korrekt) mit den FK-28-Modulen:
   - `authority.py`: `AuthorityClass` als **IntEnum mit vier Stufen** in der FK-28-§28.5.1-Wertordnung (`WORKER_ASSERTION = 0` < `SECONDARY_CONTEXT = 1` < `PRIMARY_IMPLEMENTATION = 2` < `PRIMARY_NORMATIVE = 3`; hoehere Stufe = autoritativer); englische Enum-Member. `BundleEntry` (frozen, FK-28 §28.5.2) mit den Feldern `repo_id` (Multi-Repo-Zuordnung), `path`, `authority`, `confidence` (`None` fuer Stufe 1+3), `reason`, `size`, `content` sowie der `sort_key`-Property fuer die deterministische Priorisierung.
   - `repo_context.py`: `RepoContext` (Multi-Repo-Laufzeit-Datenklasse: `repo_id`, `repo_path`, `git_base_branch`, `role`, `affected`) gem. FK-28 §28.3.2. **Git-Anbindung:** statt einer `GitOperations`-Instanz (existiert nicht, s. §1) traegt der Assembler den vorhandenen `ChangeEvidencePort` (bzw. dessen `changed_files`-Ergebnis) als Eingang; `RepoContext` haelt nur die repo-skopierten Metadaten.
   - `bundle_manifest.py`: `BundleManifest` (frozen) mit den Pflichtfeldern `evidence_epoch` (ISO-8601-**String** der Assembly), `manifest_hash` (SHA-256 ueber die sortierten Eintraege `f"{repo_id}:{path}:{size}"`, sortiert nach `(repo_id, str(path))`, FK-28 §28.5.4) und der Methode `render_prompt_header() -> str` (deterministischer Header-Text fuer den Review-Prompt, FK-28 §28.5.3). Factory `from_entries(...)`.
   - `assembler.py`: `EvidenceAssembler` mit der **3-Stufen-Assembly exakt nach FK-28 §28.3.1**:
     - **Stufe 1 — deterministischer Kern** (`_stage1_deterministic`): geaenderte Dateien (`PRIMARY_IMPLEMENTATION`) aus dem vorhandenen `ChangeEvidence.changed_files`-Port (§1), Modul-Nachbarn (`SECONDARY_CONTEXT`), normative Quellen Story-Spec/Concepts/Guardrails (`PRIMARY_NORMATIVE`), YAML/JSON-Configs (`SECONDARY_CONTEXT`).
     - **Stufe 2 — Import-Extraktion** (`_stage2_imports`): hier nur der **erweiterbare Eingang** (sauberer Port/Parameter), den AG3-062 mit dem `ImportResolver` befuellt (`SECONDARY_CONTEXT`, FK-46). Diese Story implementiert den Resolver **nicht**, baut aber den Andockpunkt so, dass AG3-062 ihn ohne Umbau einhaengt.
     - **Stufe 3 — Worker-Hints** (`_stage3_worker_hints`): liest `handover.json` und `worker-manifest.json`, markiert vorgeschlagene Dateien als **niedrigste** Autoritaet `WORKER_ASSERTION`. Es gelten die FK-28-§28.3.5-Regeln: (a) Hints sind **rein additiv** — sie koennen Stufe-1/2-Dateien weder entfernen noch herabstufen; (b) bereits im Bundle enthaltene Dateien werden **nicht dupliziert** (Hint ignoriert); (c) Dateien, die der Worker selbst geaendert hat, erzeugen ein **Self-Reference-WARNING** (`_check_self_reference`).
     - danach `_deduplicate` (bei gleicher Datei gewinnt die hoehere Authority), `_enforce_size_limit`, `BundleManifest.from_entries`.
     Liefert `merge_paths` (FK-26 §26.5a) als deterministisch sortierte, dedupizierte Pfadliste (aus `BundleManifest.file_paths`).
2. **350-KB-Limit** (FK-28 §28.3.6): Gesamt-Bundle-Groessenkontrolle mit deterministischem, dokumentiertem Verhalten beim Ueberschreiten — Priorisierung nach `AuthorityClass` (niedrigste Autoritaet faellt zuerst weg: `WORKER_ASSERTION` → `SECONDARY_CONTEXT` → `PRIMARY_IMPLEMENTATION` → `PRIMARY_NORMATIVE`), innerhalb einer Klasse nach `sort_key`; keine stille Trunkierung mitten in einem Pflicht-Artefakt; jede Ausschluss-Entscheidung wird als WARNING protokolliert (`truncated`-Flag + Warnliste im Manifest/Result). Der Grenzwert ist eine benannte Konstante (`BUNDLE_SIZE_LIMIT = 350 * 1024`).
3. **Deterministik & Hash (sauber gegen `evidence_epoch` abgegrenzt):**
   - `manifest_hash` ist reproduzierbar: SHA-256 ueber die nach `(repo_id, str(path))` **sortierten** Eintraege (`f"{repo_id}:{path}:{size}"`), unabhaengig von der Reihenfolge der Assembly-Stufen (FK-28 §28.5.4). Gleicher Eintrags-Inhalt → identischer `manifest_hash` und identische, sortierte `merge_paths`.
   - `evidence_epoch` ist der **Assembly-Zeitpunkt** (ISO-8601) und damit per Definition **nicht** Teil der Reproduzierbarkeit (FK-28 §28.5.4: Audit-Bindung, nicht Determinismus). Der „byte-identisch"-Determinismus gilt deshalb fuer `manifest_hash` + sortierte Entries/`merge_paths`. Damit das **volle** Manifest reproduzierbar wird (Contract-/Golden-Tests), ist die Uhr **injizierbar**: `evidence_epoch` wird ueber einen optionalen Clock-/Epoch-Parameter (Default `datetime.now(timezone.utc)`) gesetzt — **gleicher Input + gleicher Epoch → byte-identisches Manifest**. Kein verstecktes `now()` mitten in der Hash-/Render-Logik.
4. **CLI `agentkit evidence assemble`** (FK-28 §28.7.1): neues `evidence`-Subkommando in `cli/main.py` mit `assemble`-Aktion und den FK-28-Parametern `--story-id` (Pflicht), `--story-dir` (Pflicht), `--output-dir` (Pflicht), `--config` (optional). Andockt an das bestehende manuelle Subparser-Muster (`subparsers.add_parser`, Dispatch `:151-160`, FK-43) — kein neuer CLI-Dispatcher. Ruft den Assembler, schreibt `bundle_manifest.json` nach `--output-dir`, gibt Manifest + `merge_paths` aus, Exit 0 bei Erfolg / nicht-null fail-closed bei fehlender Pflicht-Evidenz.
5. **`__init__.py`-Exporte** statt Leerstub: `EvidenceAssembler`, `AuthorityClass`, `BundleEntry`, `BundleManifest`, `RepoContext`.
6. **Header-Producer `render_prompt_header()`** (FK-28 §28.5.3): Der **Python-Producer** `BundleManifest.render_prompt_header() -> str` ist hier voll in Scope und implementiert (er ist der **Owner des Header-Texts** und gehoert in die `BundleManifest`-Klasse, die diese Story besitzt). Er liefert einen deterministischen, strukturierten Header-Text fuer den Review-Prompt. **Nicht** in Scope ist das Anlegen/Editieren von Review-Template-Dateien und die Turn-seitige Hydration: Die in FK-28 §28.8.3 genannten fuenf `prompts/sparring/review-*.md`-Dateien und die `{{BUNDLE_MANIFEST_HEADER}}`-Doppelklammer-Konvention **existieren im AK3-Cut nicht** (s. §1 „Header-Producer vs. Review-Template-Drift": reale Review-Templates sind die flachen `qa-*` in `internal/prompts/`; Hydration ist `format_map` im prompt-runtime-BC, nicht eine `{{…}}`-Substitution im Evidence-Assembler). Das Verdrahten des Header-Texts in ein konkretes Template ist Sache des prompt-runtime-Renderers; die FK-28-§28.8.3-Prosa ist FK-vs-Code-Drift und wird als **doc-only Konzept-Nachzug** (Welle 10) geroutet — kein Resource-/Hydration-Auftrag dieser Story. AG3-061 liefert den Producer, **keine** unbuildbaren Template-Edits.
7. **Negativpfade** als Tests: fehlende Pflicht-Evidenz → fail-closed Fehler (kein leeres Bundle stillschweigend); Bundle > 350 KB → deterministische Autoritaets-basierte Reduktion mit dokumentiertem Protokoll; nicht-existenter Pfad in einem Eintrag → Fehler.

### 2.2 Out of Scope (mit Owner)
- **Import-Resolver (Stufe-2-Befuellung) + Request-DSL + Preflight-Turn + `review-preflight.md`-Template (im Prompt-Manifest) + Preflight-Sentinel** — **AG3-062** (FK-46/FK-47, `AG3-062 story.md:44/74/95`). Diese Story stellt nur den erweiterbaren Stufe-2-Eingang und den Header-Producer (`render_prompt_header`) bereit. **Wichtig (Owner-Treue):** AG3-062 owned laut eigenem Cut ausschliesslich `review-preflight.md` + dessen Manifest-Eintrag — **nicht** die `{{BUNDLE_MANIFEST_HEADER}}`-Substitution oder die fuenf FK-28-§28.8.3-`review-*`-Templates; diese werden hier daher **nicht** AG3-062 zugeschrieben.
- **`{{BUNDLE_MANIFEST_HEADER}}`-Platzhalter-Einbau + Turn-Hydration + die fuenf `prompts/sparring/review-*.md`-Templates (FK-28 §28.8.3)** — **NICHT als Code in AK3 buildbar** (s. §1 „Header-Producer vs. Review-Template-Drift"): die fuenf Dateien und das `prompts/sparring/`-Verzeichnis existieren nicht, die realen Review-Templates sind die flachen `qa-*` (`manifest.json:29-44`), und die Platzhalter-Hydration ist `format_map` im prompt-runtime-BC (`prompt_runtime/composer.py:288-290`), keine `{{…}}`-Substitution. Die FK-28-§28.8.3-Prosa ist FK-vs-Code-Drift und gehoert — wie die §28.3.6-Diff-Owner-Drift — in den **doc-only Konzept-Nachzug** (Welle 10, `_STORY_INDEX.md:138-145`, FK-28-zustaendige doc-only-Einheit). AG3-061 liefert nur den Python-Producer `render_prompt_header()` (sein Header-Text) und meldet den Drift; kein Code-Auftrag dieser Story (siehe Cross-Story-Voraussetzungen).
- **Section-aware Packing (`pack_markdown`/`pack_code`/`truncate_bundle`, `BUNDLE_TOKEN_LIMIT`) + sechsfeldriges `ContextBundle` + ContextSufficiencyBuilder** — **AG3-067** (FK-37/FK-38). Das hier ist Review-Versand-Vorbereitung, nicht das Layer-2-Eingabe-Packing.
- **ConformanceService-Nutzung des Manifest-Indexers** — **AG3-063** (FK-32).
- **Eigenstaendiger `GitOperations`/`core/git.py`-Aufbau mit `diff_name_only`/`diff_stat`/`diff_full`** — **NICHT in AK3 gebaut**; der reale Diff-Owner ist der vorhandene `ChangeEvidencePort` (`system_evidence.py`, `composition_root._SubprocessGitChangeEvidenceProvider`). Die FK-28-§28.3.6-Prosa (die `core/git.py` als noetige Erweiterung nennt) ist FK-vs-Code-Drift und gehoert als **doc-only Konzept-Nachzug** in Welle 10 (`_STORY_INDEX.md` AG3-101..104-Muster, FK-28-zustaendige doc-only-Einheit) — kein Code-Auftrag dieser Story (siehe Cross-Story-Voraussetzungen).

## 3. Akzeptanzkriterien
1. `verify_system/evidence/` enthaelt `authority.py`, `repo_context.py`, `bundle_manifest.py`, `assembler.py`; `__init__.py` exportiert die fuenf Kernklassen (Leerstub abgeloest).
2. `AuthorityClass` ist ein **IntEnum mit genau vier Stufen** in der FK-28-§28.5.1-Ordnung (`WORKER_ASSERTION=0`<`SECONDARY_CONTEXT=1`<`PRIMARY_IMPLEMENTATION=2`<`PRIMARY_NORMATIVE=3`, englische Member); `BundleEntry` traegt `authority` als `AuthorityClass`-Feld und `repo_id` (Test: Vergleichsordnung `low < high`, `sort_key`-Priorisierung).
3. `EvidenceAssembler` fuehrt die 3-Stufen-Assembly in der FK-28-Reihenfolge aus (Stufe 1 deterministischer Kern → Stufe 2 erweiterbarer Import-Eingang → Stufe 3 Worker-Hints → Dedup → Limit → Manifest) und liefert deterministisch sortierte, dedupizierte `merge_paths` (Test: gleicher Input → identische `merge_paths`).
4. **Worker-Hint-Regeln (FK-28 §28.3.5) verprobt:** Stufe 3 liest `handover.json`/`worker-manifest.json` und markiert Hints als `WORKER_ASSERTION`; (a) ein Hint kann keine Stufe-1/2-Datei entfernen oder herabstufen (additiv), (b) ein Hint auf eine bereits enthaltene Datei wird nicht dupliziert, (c) ein Hint auf eine vom Worker selbst geaenderte Datei erzeugt ein Self-Reference-WARNING (vier Tests: Marker `WORKER_ASSERTION`, Additivitaet/keine Herabstufung, keine Duplikate, Self-Reference-Warning).
5. `BundleManifest.render_prompt_header()` ist deterministisch; `manifest_hash` ist reproduzierbar (SHA-256 ueber sortierte `repo_id:path:size`-Eintraege) und aendert sich bei geaenderter Eintrags-Menge (zwei Tests: stabil bei gleichem Input, verschieden bei geaendertem Input).
6. **Determinismus vs. `evidence_epoch`:** Der reproduzierbare Teil ist `manifest_hash` + sortierte Entries/`merge_paths`; `evidence_epoch` ist der injizierbare Assembly-Zeitpunkt. Test: gleicher Input mit zwei verschiedenen Epochs → gleicher `manifest_hash`/gleiche `merge_paths`, aber unterschiedliche `evidence_epoch`; gleicher Input + gleicher (injizierter) Epoch → **byte-identisches** Manifest.
7. 350-KB-Limit: ein Bundle ueber dem Limit wird **fail-closed** entlang `AuthorityClass`-Prioritaet reduziert (niedrigste Autoritaet zuerst), nie mitten in Pflicht-Evidenz trunkiert; das Reduktionsprotokoll (`truncated`-Flag + Warnungen) ist im Manifest/Result nachvollziehbar (Test mit oversized Input).
8. Multi-Repo: `RepoContext`-Eintraege aus mehreren Repos landen korrekt zugeordnet im Manifest (`BundleEntry.repo_id`) (Test mit zwei `RepoContext`).
9. CLI `agentkit evidence assemble` ist registriert (`--story-id`/`--story-dir`/`--output-dir`/`--config`), ruft den Assembler, schreibt `bundle_manifest.json` und gibt Manifest + `merge_paths` aus; fehlende Pflicht-Evidenz → nicht-null Exit / fail-closed (Test ueber den Subkommando-Pfad).
10. Negativpfade: fehlende Pflicht-Evidenz und nicht-existenter Eintragspfad erzeugen je einen fail-closed Fehler (zwei Tests).
11. Stufe-1-Changed-Files werden aus dem vorhandenen `ChangeEvidencePort`/`ChangeEvidence.changed_files` konsumiert; es wird **kein** zweiter git-diff-Reader und **kein** `GitOperations`/`core/git.py` eingefuehrt (Test/Beleg: der Assembler nimmt die Changed-Files als Port-Eingang entgegen, ruft selbst kein `git`/subprocess).
12. **Pflichtbefehle gruen:** pytest unit/integration/contract (in Chunks, `-n0`); mypy default + `--platform linux`; ruff; vier Konzept-Gates; Coverage ≥ 85 %.

## 4. Definition of Done
- AK 1–12 erfuellt; giftige Codex-Review PASS; Implementierung/Commit erst nach Execution-Plan-Freigabe (diese Story wird zunaechst nur autorisiert/reviewt).

## 5. Guardrail-Referenzen
- **FAIL CLOSED:** fehlende Pflicht-Evidenz, oversized Bundle, nicht-existente Pfade → harter Fehler bzw. dokumentierte autoritaetsgeleitete Reduktion, nie stilles Leer-/Teil-Bundle.
- **FIX THE MODEL / SINGLE SOURCE OF TRUTH:** kein zweiter git-diff-Reader — Stufe 1 konsumiert den **vorhandenen** `ChangeEvidencePort` (`system_evidence.py` / `composition_root._SubprocessGitChangeEvidenceProvider`), kein `GitOperations`/`core/git.py`-Neubau; `build_review_bundle`/`Layer2ReviewInput` bleibt unberuehrt (Layer-2-Input, nicht Review-Versand). `BundleManifest` ist der eine Owner der Review-Versand-Wahrheit, kein Schattenfeld; das gleichnamige, bestehende Cycle-`evidence_epoch` (`implementation/phase.py`) bleibt unangetastet.
- **TYPISIERT STATT STRINGS:** `AuthorityClass`-IntEnum statt String-Prioritaeten; `BundleEntry`/`BundleManifest`/`RepoContext` als typisierte Modelle.
- **ARCH-55:** alle Klassen/Felder/Enum-Member/Wire-Keys/CLI-Argumente englisch.
- **ZERO DEBT:** kein Stub-als-Done; `__init__.py`-Leerstub wird real abgeloest, das Limit-/Reduktionsverhalten und die Worker-Hint-Regeln sind vollstaendig implementiert (kein „spaeter").

## 6. Hinweise fuer den Sub-Agent
- Read T:/codebase/claude-agentkit3/CLAUDE.md first — all project rules apply to you.
- FK-28 §28.2 nennt das Package `agentkit/evidence/` — im AK3-BC-Schnitt liegt es unter `agentkit/verify_system/evidence/` (das ist der vorhandene Stub-Ort; **nicht** ein zweites Top-Level-`evidence/` anlegen).
- **Diff-Owner:** FK-28 §28.3.6 verlangt prosalisch `GitOperations.diff_name_only()` in `agentkit/core/git.py` — beides existiert im AK3-Tree **nicht**. Der reale SYSTEM-`git diff --name-only`-Owner ist `ChangeEvidencePort`/`ChangeEvidence.changed_files` (`system_evidence.py:43-118`), produktiv via `_SubprocessGitChangeEvidenceProvider` (`composition_root.py:779-835`). Stufe 1 konsumiert diesen Port; **kein** `core/git.py`-Neubau, **kein** zweiter Diff-Reader. Die FK-28-Prosa als doc-only-Drift melden, nicht im Code „korrigieren".
- **Header-Producer vs. Template-Drift:** `render_prompt_header()` als Producer in `BundleManifest` voll bauen (Header-Text-Owner, FK-28 §28.5.3) — aber **keine** der fuenf FK-28-§28.8.3-`prompts/sparring/review-*.md`-Dateien anlegen/editieren und **keine** `{{BUNDLE_MANIFEST_HEADER}}`-Substitution bauen: das Verzeichnis und die Dateien existieren nicht (reale Review-Templates = flache `qa-*` in `internal/prompts/`, `manifest.json:29-44`), und die Platzhalter-Hydration ist `format_map` im prompt-runtime-BC (`prompt_runtime/composer.py:288-290`). Die §28.8.3-Prosa als doc-only-Drift (Welle 10) melden, **nicht** im Code „erfuellen". **Nicht** AG3-062 zuschreiben — AG3-062 owned nur `review-preflight.md` (`AG3-062 story.md:44/74`).
- `build_review_bundle`/`ReviewBundle` (`llm_evaluator/bundle.py`) NICHT duplizieren — das ist Layer-2-Input, nicht der Review-Versand. Klar abgrenzen.
- Der Stufe-2-Sekundaerkontext-Eingang muss so geschnitten sein, dass AG3-062 den Import-Resolver ohne Umbau einhaengen kann (sauberer Port/Parameter, kein „spaeter umschreiben").
- `evidence_epoch` injizierbar halten (optionaler Clock-/Epoch-Parameter, Default UTC-`now`), damit das volle Manifest fuer Contract-/Golden-Tests reproduzierbar ist; den Determinismus-Beweis auf `manifest_hash` + sortierte Entries fuehren.
- Falls das Befuellen des Stufe-1-Inputs eine StoryService-/Handover-Erweiterung erzwingt, die ueber dieses Package hinausgeht: **melden**, nicht still ausweiten.
- AK2 NICHT veraendern. `.mcp.json` NICHT anfassen. **Kein Commit** ohne expliziten Auftrag.
- „done" nur mit Beleg: Diff-Zusammenfassung, gruene Pflichtbefehle (Ausgaben), Testnamen (3-Stufen-Reihenfolge, Worker-Hint-Regeln, Determinismus vs. Epoch, Limit-Reduktion, Multi-Repo, CLI, Negativpfade).

---

## Globale Akzeptanzkriterien (verbindlich)

Zusaetzlich zu den obigen Akzeptanzkriterien gelten die **globalen Akzeptanzkriterien**
aus `stories/_GLOBAL_ACCEPTANCE.md` (Single Source of Truth):

- **GAC-1:** `scripts/ci/check_architecture_conformance.py` laeuft mit **0 Errors**
  (Exit 0, fail-closed) — `PYTHONPATH=src .venv\Scripts\python scripts/ci/check_architecture_conformance.py`.
- **GAC-2:** Die Architektur-Guardrails `guardrails/architecture-guardrails.md`
  (ARCH-NN) werden eingehalten; Konflikt = hart stoppen und melden.

story_id: AG3-154
title: "CLI/Admin-Kommandos + Edge-Tool: takeover-request/-confirm (human_cli, direkter Challenge-Dialog), echtes recover-story (acquired_via=recovery, neuer Run auf bestehendem Worktree), Agent-Kommandos im deployten projectedge.py"
type: implementation
status: ready
phase: setup
# Design freeze: reports/AG3-154-cli-admin-design.md (2 increments), after a
# Fable design ruling. SETTLED: recovery = new run + acquired_via=recovery,
# supersede-the-one-active-record, freeze/obligation hard-refuse, human_cli-only;
# CLI confirm via a genuine strategist session (FK-15 15.10.3), AG3-148 gates
# untouched. SOLL-091 co-sign-free same-harness-identity self-rebind: SATISFIED
# BY CONSTRUCTION, nothing to build/strike/escalate. AK3's owner_session_id IS the
# Claude Code hook session_id (harness_adapters/claude_code.py:147 maps every event
# session_id=claude_event.session_id); a `/resume` preserves that session_id, so the
# resumed session still OWNS the story (ownership fence matches owner_session_id@epoch)
# and just continues -- an interrupted op reconciles via op_id idempotency (FK-91
# rule 14/16). No recovery event, no co-sign. That IS FK-56 §56.13g. `recover-story`
# (human_cli, new run) is ONLY the fresh-session fallback (transcript lost / clean
# slate / a takeover happened while down -> AG3-149 disowned path). Agent recovery
# stays fail-closed correctly: invoking recover-story is by definition NOT the resume
# path (a fresh session), so it is a deliberate human decision. §56.13g is NOT struck.
created: "2026-07-02"
size: M
# Herkunft: GAP-ST-11 (gap-analyse-session-ownership.md v4, Codex-APPROVE).
# Operator-/Agent-Zugangswege: CLI-Adapter takeover-request/-confirm +
# echtes recover-story (existiert heute NICHT; FK-20 §20.7.3/4, FK-56
# §56.13g); deploytes Zielprojekt-Asset projectedge.py erhaelt die
# Takeover-/Abort-/Recover-Kommandos fuer Agents (Bundle-Asset-Pflicht).
# Human-CLI-admin-abort + Endpoint = AG3-138; Transfer-Kern = AG3-148.
depends_on: ["AG3-138", "AG3-145", "AG3-148"]
unblocks: ["AG3-155"]

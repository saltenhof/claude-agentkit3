"""Deployable, stdlib-only concept toolchain for AgentKit target projects.

Single source of truth of the generic concept gates defined in FK-78
(section 78.14): SMY parsing, governance configuration, document scanning,
frontmatter/reference/formal checks, the W4 decision-record gate, the
incubator/promotion/projection/semantic-status checks (read-only
``check.py``), and the mutating semantic-gate mechanics
(``semantic_gate.py``: unit derivation, request packs, receipt import).
"""
